# NEXUS Multi-Browser A2A Collab — Cycle 1 (shared mission)

**Cycle ID:** NEXUS_A2A_C1  
**Orchestrator:** Hermes + Grok Build on authenticated CDP Chrome :9224  
**Goal:** Produce a *usable* multi-agent collaboration protocol for NEXUS where browser lanes specialize, hand off evidence, and combine into one best output — not parallel recaps.

## Hard constraints (all lanes)
1. Stay in this existing conversation/tab. Do not ask the operator to open a new chat.
2. Prefer concrete, testable steps over theory.
3. Name what only *you* can do well (your unique surface).
4. End with a short section: `## HANDOFF` listing 2–5 artifacts or next actions other lanes should consume.
5. Keep the first line of your answer starting with the lane token given in your role prompt.

## Shared problem statement
NEXUS has a stabilized multi-browser local collab stack (CDP :9224). We need:
- Scout/intel (cloud Linux + free/fast models + Hermes hunters)
- Deep research multi-agent reports
- Canvas / cloud project sharing
- Implementation with MCP + sandboxes (Grok, GPT, GLM Next.js, Qwen HTML/JSON)
- Continuity receipts in `NEXUScontinuity_runs.jsonl` (orchestration evidence; chat text stays in tabs)

Design the protocol + first joint deliverable idea that exploits each surface without thrashing models or opening duplicate tabs.
