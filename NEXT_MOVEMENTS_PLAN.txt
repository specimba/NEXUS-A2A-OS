# NEXUS OS — Next Movement & Strategic Task Advice

This document provides a highly structured synthesis of recent agent findings, architectural breakthroughs, and concrete task advice to realize the **NEXUS OS 2026 Vision** as the ultimate governed, multi-agent operating system.

---

## 1. Executive Status & Key Findings

### 1.1 The ERNIE Benchmark Victory
* **True Positive Block Rate**: **98.2%** (982/1000 blocked).
* **Benign False Positive Rate (FPR)**: **9.1%** (90/99 true negatives).
* **Verdict**: **PASS** under production readiness targets (Target: >80% block rate, <10% FPR).
* **Mitigation Gaps Solved**: Deployed the stateless `MetaAttackDetector` on the ingress boundary of `GuardPlane.classify()` which filters Pattern Mirror, Ontological, and Entanglement attacks under 1ms, passing all 16 security unit tests.

### 1.2 The Ollama Port 11435 Discovery (Solving the "Timeout" Mystery)
> [!IMPORTANT]
> **CRITICAL DISCOVERY**: Auditor checks (Devin) verified that the "147.2 GB deleted models cleanup" reported by the previous agent was an AI hallucination. The models (including specialized Granite, Bonsai, and uncensored reasoners) are **100% present and healthy** in the local Ollama instance.
>
> The reason `ollama list` and related commands timed out for the user is that Ollama is running on port **11435**, not the default port **11434**.

#### How to Access Local Models:
For both user shells and new agents, always prepend or export the custom `OLLAMA_HOST` variable:
* **CMD**: `set OLLAMA_HOST=127.0.0.1:11435 && ollama list`
* **PowerShell**: `$env:OLLAMA_HOST='127.0.0.1:11435'; ollama list`
* **Bash**: `OLLAMA_HOST=127.0.0.1:11435 ollama list`

---

## 2. Next Movement Suggestions (Strategic Pillars)

### Pillar A: Build the Grok Custom MCP Tunnel (Tactical Breakthrough)
Our most powerful external model is **Grok 4.3** ($30/mo, unlimited browser access, native image/video creation, custom MCP support). The next immediate movement is to tunnel Grok directly to our local port **7352** (NEXUS MCP server) via ngrok or Cloudflare Tunnel, giving Grok governed code-execution and file access capabilities.

```
Grok 4.3 (xAI Browser) 
   │ (via ngrok public URL)
   ▼
Local ngrok agent
   │
   ▼ (Port 7352)
NEXUS MCP Server ──► [ TrustKernel: ALLOW? ] ──► Local PC / OpenClaw Stack
```

### Pillar B: SwarmClaw + OpenClaw Native Stack Integration
OpenClaw is already fully configured locally at `C:\Users\speci.000\.openclaw-autoclaw\` with 50+ skills and 6 active agents. We should introduce **SwarmClaw** as the global orchestrator to schedule, delegate, and coordinate multi-agent consensus workflows.

### Pillar C: Implement Phase 0 Registry Vetting (7-Layer Stack)
Following recent MCP security papers, we must protect the local rig against initialization-logic exploits (which execute before runtime gates). We need to build a **Pre-Integration Sandbox** that scans linked server files, checks for exposed credentials, and validates account ownership.

---

## 3. Concrete Task Roadmap

| Priority | Task Name | Component | Scope & Objective |
| :--- | :--- | :--- | :--- |
| **P1** | **Expose Port 7352 via ngrok** | `nexus_os/bridge` | Set up a secure ngrok tunnel on localhost:7352 and configure the public URL inside Grok 4.3 as a Custom MCP Connector. |
| **P2** | **Add `openclaw` to System PATH** | `Execution Layer` | Wire the local OpenClaw gateway executable to the Windows PATH so that CLI commands work natively. |
| **P3** | **Deploy SwarmClaw Orchestrator** | `Swarm Layer` | Install SwarmClaw (`npm install -g @swarmclawai/swarmclaw`), initialize a local swarm, and map existing OpenClaw agents as worker nodes. |
| **P4** | **Pre-Integration Sandbox (Phase 0)** | `Security Layer` | Write a script to scan registered MCP server manifests for name collisions, exposed API keys, and unauthorized init-logic imports before loading them. |
| **P5** | **BAYESIAN TrustKernel Dashboard Hook** | `Dashboard Layer` | Hook the React Dashboard's Governor/GMR tabs to read live Bayesian trust scores and latency metrics directly from the `/api/governor` endpoint. |
| **P6** | **Prune Legacy Mystery Packages** | `Hygiene` | Uninstall the redundant mystery Python package `pi 0.1.2` (`pip uninstall pi`) and adopt Mario Zechner's canonical `pi-coding-agent` via npm. |

---

## 4. Architectural Verification Plan

To maintain the strict "no done claim without verifiable evidence" rule under **AGENTS.md**:
1. **Pre-flight Check**: Run `python -m nexusctl doctor` before beginning any phase.
2. **Safety Gates**: Enforce `[SAFETY-1]` through `[SAFETY-4]` (sandbox active, token guard active, and KAIJU auth check).
3. **Continuous Benchmarking**: Re-run `datasets\ernie\bench_ernie_1000_local.py` after prompt/routing changes to ensure no performance regression occurs (Adversarial Recall must remain $\ge 80\%$, Benign FPR $< 10\%$).
4. **Handoff**: Execute `python -m nexusctl handoff` to package the workspace before major architectural transitions.
