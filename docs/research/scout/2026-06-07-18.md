# NEXUS Research Scout — 2026-06-07 18:00 UTC

## Source quick-scan
- arxiv:    0 new thermodynamic/BEC/EPR hallucination detection papers this window (the only quantum-adjacent hit is Quantinuum's $1.68B IPO coverage and the Schrödinger's color-geometry paper at LANL — both are physics, not LLM-detection)
- News: ~35 hits scanned across 4 queries (thermodynamic/BEC/EPR hallucination, MCP/A2A best practices, small-LM function-calling routing, adaptive circuit-breaker LLM gateway) + 5 web_research passes on the Aikido codexui-android npm supply chain, Crossmint/Visa Intelligent Commerce/Basis Theory agent card payments, Microsoft MXC Windows execution container, Microsoft v2.0 failure-mode taxonomy, and Weaviate Engram
- GitHub trending / project activity (relevant repos): **codexui-android (npm, 29,000 weekly downloads, since v0.1.82)** — credential-harvesting npm package shipping since May 2026, exfiltrating `~/.codex/auth.json` to `sentry.anyclaw[.]store`; **Aikido Security disclosure (June 1, 2026)** + **OpenAI support warning** for `~/.codex/auth.json` plaintext storage; **BrutalStrike Android apps** (gptos.intelligence.assistant, 50,000+ downloads, codex.app, 10,000+ downloads) running the npm package inside a PRoot sandbox; **Crossmint + Visa Intelligent Commerce + Basis Theory** (FinTech Magazine, June 7, 2026) — production-grade PCI-Level-1 tokenization layer with the **"7.1% of published OpenClaw skills expose card credentials"** stat; **Microsoft MXC (Microsoft Execution Containers)** Build 2026 follow-up (VentureBeat / Petri / WinCentral / The Verge, June 2–7) — OpenClaw is the canonical benchmark agent, shipped as the first preview SDK with Agent 365 / Entra / Intune / Purview integration due July 2026
- Notable infra moments: **The codexui-android / `~/.codex/auth.json` exfiltration chain (Aikido, June 1, 2026) is the first credential-theft campaign to specifically weaponize a *non-OpenAI* agent runtime (the Android app "OpenClaw Codex Claude AI Agent", package name `gptos.intelligence.assistant`, which has 50,000+ Play Store downloads) against a *different vendor's* (OpenAI Codex) auth store — the cross-vendor, cross-platform, sandbox-via-PRoot pattern is novel.** The `sentry.anyclaw[.]store` C2 domain was registered April 12, 2026, two days after the v0.1.72 npm publish, and the `friuns2` X profile links to the same `anyclaw[.]store` domain. The refresh_token does not expire — "an attacker holding it can silently impersonate you indefinitely." The Crossmint/Visa/Basis Theory 7.1% OpenClaw-skill exposure stat is the second NEXUS-relevant moment because it converts "credential leakage in agent skills" from a theoretical concern to a measured baseline that NEXUS can advertise improvement against. **Microsoft MXC's first production benchmark is *OpenClaw running on Windows in an MXC-composed sandbox*** — i.e. Microsoft's enterprise runtime is, on day one, wrapping the same agent framework NEXUS's swarm controller on port 18789 uses, which validates MXC as the canonical "Windows-side" complement to NEXUS's `src/nexus_os/security/sanitizer.py` Python-layer containment. The Microsoft v2.0 failure-mode taxonomy update was published in **late May 2026** and is already covered in 2026-06-05-18, 2026-06-06-00, and 2026-06-06-12 — no new categories this window, so the cross-vendor agent-credential-theft and the 7.1% OpenClaw-skill exposure are the two net-new NEXUS-relevant primitives. **Weaviate Engram (announced June 6, 2026)** is the first managed-memory-as-a-service product to explicitly target LLM-agent context retention, but its launch is positioned as a *cloud-memory-as-a-service* for generic agent developers, not as a NEXUS-equivalent on-prem trust-scored memory store; the NEXUS `src/nexus_os/vault/memory_adapter.py` Mem0Adapter shape (4-layer S-P-E-W with trust-scored promotion) is a more rigorous *local* primitive than Engram's managed-cloud approach, and the Engram launch is therefore a competitive data point (NEXUS-equivalent primitives are shipping as commercial products) but not a new capability that NEXUS lacks.

## Findings (NEXUS-relevant only)

### 1. Aikido codexui-android / `~/.codex/auth.json` Exfiltration + BrutalStrike Android PRoot-Sandbox Variant — The First Cross-Vendor, Cross-Platform Credential-Theft Campaign That Weaponizes a *Non-OpenAI* Android Agent Runtime ("OpenClaw Codex Claude AI Agent", package `gptos.intelligence.assistant`, 50,000+ Play downloads) Against an *OpenAI* Local Auth Store, and the First Live Evidence That NEXUS's `src/nexus_os/security/sanitizer.py` `ConfigFileWatchdog` Pattern + `src/nexus_os/governor/kaiju_auth.py` Per-Task Credential Primitive Need to Extend Beyond `.claude/` / `.gemini/` / `.cursor/` / `.vscode/` to the `.codex/`, `~/.anyclaw/`, and PRoot-Sandbox Directories
- Source: The Hacker News https://thehackernews.com/2026/06/openai-codex-authentication-tokens.html (June 1, 2026); Aikido Security https://www.aikido.dev/blog/codex-remote-ui-steals-ai-tokens (Charlie Eriksen); Aikido follow-up https://www.aikido.dev/blog/google-api-keys-deletion (deleted Google API key 23-minute revocation window); CSO Online https://www.csoonline.com/article/4179815/attack-targeting-openai-codex-users-exposes-ai-software-supply-chain-risks.html; OpenAI Codex auth docs https://developers.openai.com/codex/auth ("treat `~/.codex/auth.json` like a password"); friuns2/codex-mobile GitHub issue #198 https://github.com/friuns2/codex-mobile/issues/198; WHOIS anyclaw.store (registered 2026-04-12, two days after v0.1.72 npm publish); Crossmint/Visa/Basis Theory 7.1% OpenClaw-skill audit (FinTech Magazine, 2026-06-07) https://fintechmagazine.com/news/crossmint-launches-new-secure-api-for-ai-agent-card-payments
- Why NEXUS cares: The codexui-android disclosure is the first credential-theft campaign to weaponize *a non-OpenAI agent runtime* (the "OpenClaw Codex Claude AI Agent" Android app, package name `gptos.intelligence.assistant`, 50,000+ Play Store downloads) against *OpenAI's local auth store* (`~/.codex/auth.json`), and the campaign's exfiltration chain (4.3 MB obfuscated JS → reads `auth.json` → POSTs the OAuth blob to `sentry.anyclaw[.]store/startlog` → refresh_token never expires) is a concrete NEXUS-relevant threat because it is the *first live* instance of a `ConfigFileWatchdog` blind spot in NEXUS's existing session-start-hook coverage. Three concrete NEXUS maps. **(a) Cross-vendor agent credential theft ↔ NEXUS's `src/nexus_os/security/sanitizer.py` `ConfigFileWatchdog` from 2026-06-05-12 #2 + the Miasma Phase 2 follow-up from 2026-06-05-18 #1 / 2026-06-06-00 #3** — the `ConfigFileWatchdog` snapshots hashes for `.claude/settings.json`, `.gemini/settings.json`, `.cursor/rules/setup.mdc`, `.vscode/tasks.json`, and `.claude.json` (Mitiga). The codexui-android campaign sits in a third category: a *runtime shell process* (the npm package running inside a PRoot sandbox inside an Android app) reads a *credential file* (`~/.codex/auth.json`) and exfiltrates it. NEXUS needs to add a `codex_auth_watchdog` to `src/nexus_os/security/sanitizer.py` that, on a NEXUS-equipped agent's first read of any directory or any tool call that names `~/.codex/`, `~/.config/codex/`, or `auth.json` as a path, snapshots the file's hash and a per-process read-attempt counter, then emits a `nexus.credential.codex_read` event to Loki with `(agent_id, file_path, file_hash, process_id, sandbox_kind=none|proot|docker|kata)`; any read from a PRoot-flagged process (heuristic: `PRoot` / `termux` / `app_process` in `/proc/<pid>/comm` or in the parent process command line) is auto-flagged. **(b) Sandbox-aware credential exfiltration ↔ NEXUS's `src/nexus_os/governor/kaiju_auth.py` `KaijuTaskCredential` primitive from 2026-06-06-12 #3** — the refresh_token in `~/.codex/auth.json` is a long-lived bearer that does not expire, which is exactly the "no per-task credential, no step-up, no rotation" failure mode the OWASP L3 "ephemeral credentials" primitive is designed to address and the Microsoft Scout per-task credential primitive (2026-06-06-18 #3) solves. NEXUS's `KaijuTaskCredential` dataclass is the right shape but needs a Codex-flavored subclass `KaijuCodexTaskCredential` that holds the Codex OAuth `refresh_token` *inside* the NEXUS vault (memory-only, never on disk), and the `src/nexus_os/bridge/sdk.py` should expose a `nexus_codex_auth.login()` helper that brokers the OAuth dance, stores the resulting `refresh_token` in the vault, and writes a *placeholder* `~/.codex/auth.json` containing only an opaque `nexus_task_credential` reference that the OpenClaw Codex Claude AI Agent runtime resolves back through NEXUS at call time. **(c) 7.1% OpenClaw-skill credential exposure ↔ NEXUS's `src/nexus_os/security/sanitizer.py` `nexus.credential.exposed` counter from 2026-06-05-00 #2** — the Crossmint/Visa/Basis Theory FinTech Magazine disclosure (June 7, 2026) cites an internal Crossmint audit showing **7.1% of published OpenClaw skills expose raw card credentials in their source**. The 2026-06-05-00 #2 follow-up added a `CREDENTIAL_BEARER` impact class to `kaiju_auth.py` and a PAN/CVC regex to `sanitizer.py`; the codexui-android disclosure extends that surface from "raw card credentials" to "any plaintext OAuth/refresh_token in `~/.codex/auth.json` (or `~/.claude.json`, or `~/.gemini/`, or `~/.cursor/`, or `~/.vscode/`, or `~/.anyclaw/`)", which is the same 7.1% exposure surface framed against a different credential type. NEXUS should add a `nexus.credential.exposed` event-type family — `card_credential`, `oauth_refresh_token`, `aws_access_key`, `gcp_service_account`, `k8s_kubeconfig`, `ssh_private_key` — and a per-type Loki gauge; the dashboard panel can then publish NEXUS's per-deployment exposure rate per credential type, benchmarked against Crossmint's 7.1% card-credential baseline, and the same metric can be advertised as "NEXUS keeps the exposure rate at 0% across all 6 credential types" (with the `tests/security/test_credential_exposure.py` baseline). Microsoft's headline operational finding (Microsoft v2.0 taxonomy, May 2026) that "HitL bypass was the most consistently exploited failure mode" plus the new codexui-android pattern (no HitL, no human visible, refresh_token stolen at first run) is the strongest possible signal that NEXUS's existing kaiju_auth hold-queue is necessary but not sufficient — the credential must be *outside* the runtime filesystem entirely, and the NEXUS vault is the only safe location.
- Suggested follow-up: Add a `codex_auth_watchdog` to `src/nexus_os/security/sanitizer.py` that watches `~/.codex/auth.json`, `~/.config/codex/auth.json`, `~/.anyclaw/`, and `~/.claude.json` (the same set `ConfigFileWatchdog` watches for `.claude/settings.json` etc.), snapshots the file's SHA-256 at agent startup, and emits a `nexus.credential.codex_read` Loki event with `(agent_id, file_path, file_hash, process_id, sandbox_kind, parent_cmdline)` on every read. Add a `sandbox_kind` heuristic that inspects `/proc/<pid>/comm` and the parent process command line for `proot`, `termux`, `app_process`, `com.android.chrome`, and `com.topjohnwu.magisk`; a `sandbox_kind=proot` flag auto-elevates the event to `nexus.credential.codex_exfil_attempted`. Add a `KaijuCodexTaskCredential` dataclass to `src/nexus_os/governor/kaiju_auth.py` that mirrors the `KaijuTaskCredential` shape from 2026-06-06-12 #3 but holds the Codex `refresh_token` *inside* the NEXUS vault (memory-only) and writes a placeholder `~/.codex/auth.json` containing only an opaque `nexus_task_credential` reference (the `refresh_token` is never on disk). Wire a `nexus_codex_auth.login()` helper into `src/nexus_os/bridge/sdk.py` that brokers the OAuth dance, stores the credential in the vault, and exposes a `codex_token.refresh()` API the agent runtime can call when the access_token expires. Extend the `nexus.credential.exposed` event-type family in `src/nexus_os/observability/audit_log.py` with the five additional credential types (`oauth_refresh_token`, `aws_access_key`, `gcp_service_account`, `k8s_kubeconfig`, `ssh_private_key`) and a per-type regex set; the per-type regex set should match the Aikido Google-API-key 23-minute revocation-window finding (June 1, 2026) — a deleted-but-cached credential is still exfiltratable, and NEXUS should add a `nexus.credential.revocation_lag` event that fires when the NEXUS vault detects a rotated credential still being used by an old session. Add `tests/security/test_codex_watchdog.py` that constructs a synthetic `codexui-android@0.1.85` install (a 4.3 MB obfuscated JS module that reads `auth.json` and POSTs to `sentry.anyclaw.store/startlog`), runs it inside a PRoot sandbox via a Docker wrapper, and asserts (a) the watchdog emits `nexus.credential.codex_read` and `sandbox_kind=proot` within 200 ms of the file open, (b) the placeholder `~/.codex/auth.json` from `KaijuCodexTaskCredential` contains no `refresh_token` string, (c) the `nexus.credential.exposed` counter for `oauth_refresh_token` increments. Add `docs/security/cross-vendor-agent-credential-theft.md` that documents the codexui-android → `OpenClaw Codex Claude AI Agent` → `sentry.anyclaw.store` chain end-to-end, the BrutalStrike 50,000+ Play Store download exposure, and the per-credential-type NEXUS defense (vault-brokered login + watchdog + revocation-lag detector), and cross-reference the 2026-06-05-12 #2 Mitiga `~/.claude.json` watchdog, the 2026-06-05-00 #2 Crossmint 7.1% baseline, and the 2026-06-06-12 #3 `KaijuTaskCredential` primitive so a security reviewer can audit the entire cross-vendor credential-leak defense surface in a single read. Publish the per-deployment "0% credential exposure across all 6 types" metric as a NEXUS dashboard panel (third distinctive NEXUS marketing surface after the OWASP L3 maturity score from 2026-06-05-12 #1 and the v2.0 failure-mode coverage score from 2026-06-05-18 #3) so a buyer comparing NEXUS against the Microsoft Scout + Agent 365 SKU can see the cross-vendor credential-leak defense NEXUS ships out of the box.

### 2. Microsoft MXC (Microsoft Execution Containers) at Build 2026 — First Production Reference for an OS-Level Composable Sandbox That Uses *OpenClaw on Windows* as the Canonical Day-One Benchmark, the First Time Microsoft Has Shipped a Sandbox Backed by Both *gVisor-Style* and *Micro-VM* Isolation Modes, and a Direct Path to Add a `nexus.sandbox.runtime=mxc` Label to NEXUS's Loki Schema So a NEXUS-Equipped Fleet Can Co-Run with the Microsoft Agent 365 + MXC Stack on Windows Without Double-Wrapping
- Source: VentureBeat https://venturebeat.com/security/microsoft-launches-mxc-an-os-level-sandbox-for-ai-agents-with-openai-and-nvidia-already-on-board; The Verge https://www.theverge.com/ai-artificial-intelligence/941870/microsoft-makes-it-more-secure-to-run-openclaw-on-windows; WinCentral https://thewincentral.com/openclaw-native-windows-microsoft-execution-containers/; Petri https://petri.com/microsoft-execution-containers-boundaries-ai-agents/; CSO Online https://www.csoonline.com/article/4180467/microsoft-wants-to-put-ai-agents-on-a-short-leash.html; NVIDIA Developer Blog https://developer.nvidia.com/blog/build-personal-ai-agents-on-windows-pcs-with-new-tools-from-microsoft-and-nvidia/ (MXC + OpenShell, NemoClaw for Windows via WSL); Cloud Wars https://cloudwars.com/ai/with-autopilots-microsoft-delivers-long-running-enterprise-grade-ai-agents-tapping-openclaw/ (Autopilots, Scout, MXC); Directions on Microsoft https://www.directionsonmicrosoft.com/build-2026-microsoft-pushes-the-agent-envelope-with-android-devices-new-windows-pcs/ (Scout + MXC preview July 2026); DEV Community https://dev.to/om_shree_0709/microsoft-just-made-windows-the-os-level-security-layer-for-ai-agents-heres-what-mxc-actually-30nn; The Register https://www.theregister.com/os-platforms/2026/06/03/microsoft-build-surface-rtx-spark-dev-box-coreutils-for-windows-air-gapped-github-and-more/5250501 (MXC + OpenClaw + WSLC + air-gapped GitHub)
- Why NEXUS cares: MXC is the first OS-level, policy-driven execution layer integrated into Windows and WSL that *ships with an OpenClaw day-one benchmark* — i.e. Microsoft's enterprise runtime validated against the *same* agent framework NEXUS's swarm controller on port 18789 already runs, and ships as a TypeScript SDK + per-process isolation modes (process, session, micro-VM, Linux container) that an enterprise IT admin can push via Agent 365 / Entra / Intune / Purview. Three concrete NEXUS maps. **(a) OpenClaw is the canonical MXC benchmark ↔ NEXUS's `src/nexus_os/swarm/openclaw_spawner.py`** — Microsoft's choice to use the *OpenClaw Windows Companion App* as the first MXC benchmark validates the *exact* runtime NEXUS operates (`src/nexus_os/swarm/openclaw_spawner.py` is the spawner for OpenClaw agents on the NEXUS swarm mesh). The runtime surface is identical: a long-running headless agent loop that calls MCP tools, reads files, writes to disk, and calls out to a model relay. NEXUS's `src/nexus_os/security/sanitizer.py` Python-layer containment decorators (per the 2026-06-05-09 #2 follow-up) are functionally equivalent to MXC's process-isolation mode, but the *policy push* is different — MXC pushes the policy from Agent 365 / Intune, NEXUS pushes it from `src/nexus_os/governor/kaiju_auth.py` at request time. The two are complementary: NEXUS's Python-layer checks should run *inside* the MXC process-isolation mode, and NEXUS's `ConfigFileWatchdog` should run *outside* (in the host) so a sandboxed agent that tampers with its own `.claude/settings.json` cannot disable the watchdog. **(b) Composable isolation modes (process / session / micro-VM / Linux container) ↔ NEXUS's `src/nexus_os/execution_paths.py`** — MXC exposes four isolation levels (process, session, micro-VM, Linux container) with a unified TypeScript SDK; NEXUS's `execution_paths.py` is the canonical "where does this tool call run" surface but today has no per-tool-isolation-mode policy. The 2026-06-05-09 #2 follow-up added a `nexus.sandbox.composable` flag to `sanitizer.py`; the MXC announcement is the production reference for what that flag should *do* — when a NEXUS MCP tool call is flagged for high-impact (e.g., `kaiju_auth.py` hold-queue entry), the NEXUS gateway should signal to the MXC runtime to *promote* the call to a higher isolation mode (e.g., process → micro-VM) so that a malicious MCP tool cannot exfiltrate via the host filesystem even if the Python containment layer is bypassed. **(c) Agent 365 + Entra + Intune + Purview integration preview July 2026 ↔ NEXUS's `src/nexus_os/api/server.py` governance endpoints** — Microsoft previewed the Agent 365 / MXC integration for July 2026; NEXUS's `src/nexus_os/api/server.py` already exposes `/skills/propose`, `/skills/status/{id}`, `/dashboard/stats`, `/governance/proposals`, `/governance/approve` (per the 2026-05-18 `01_PROJECT_STATE.md` P0 sequence item #5), which are the *exact* endpoints a Microsoft-stack IT admin would need to bridge a NEXUS-equipped OpenClaw fleet into an Agent 365 / Intune-driven MXC deployment. The integration shape is: a NEXUS-equipped OpenClaw agent registers with the local MXC runtime via a `nexus_mxc_attestation` JWT (mirroring the Microsoft Entra Agent OBO actor facet from 2026-06-06-12 #2), the MXC runtime pushes the per-agent policy (file system access, network egress, credential access) from Intune to MXC at startup, and the NEXUS `kaiju_auth.py` hold-queue decisions are emitted to Purview as audit events via the existing Microsoft Purview Compliance API bridge pattern from 2026-06-06-12 #1. The Scout-OpenClaw-MXC triad (Microsoft Scout built on OpenClaw, running on Windows in MXC) is the strongest possible production reference for the NEXUS mesh, and the 2026-06-06-12 #3 Microsoft-Scout-parity doc can be extended with a "MXC runtime parity" section.
- Suggested follow-up: Add a `nexus.sandbox.runtime` Loki label to `src/nexus_os/observability/audit_log.py` with values `python|none|mxc|gvisor|kata|micro_vm|linux_container` so the dashboard can show the distribution of NEXUS-equipped agents by runtime isolation mode. Extend the `nexus.sandbox.composable` flag in `src/nexus_os/security/sanitizer.py` (per the 2026-06-05-09 #2 follow-up) to detect the MXC runtime at process startup — inspect `MXC_SANDBOX_ID`, `MXC_ISOLATION_MODE`, `MXC_POLICY_VERSION` env vars, and short-circuit the in-Python containment decorators when `MXC_ISOLATION_MODE` is `process|session|micro_vm|linux_container` (no double-wrapping). Add a `nexus.mxc.promote_isolation` call in `src/nexus_os/governor/kaiju_auth.py` that the hold-queue decision emits to the MXC runtime via the existing `nexus_mxc_attestation` JWT pattern: when a tool call is escalated to human review or when a Contain-stage risk score exceeds a threshold, the NEXUS gateway signals MXC to promote the agent's isolation mode (process → micro-VM, etc.) for the duration of the call. Add a `nexus.mxc.policy_diff` job that polls the Intune-pushed MXC policy version every 60 seconds and emits a `nexus.mxc.policy_drift` event when the live `(policy_version, file_access_list, network_egress_list, credential_access_list)` tuple diverges from the NEXUS-pinned snapshot, mirroring the drift detector pattern from 2026-06-06-18 #1 SEP-2575. Add `docs/architecture/openclaw-mxc-parity.md` extending the existing `openclaw-microsoft-parity.md` from 2026-06-06-12 #3 with a "MXC runtime parity" section that documents the line-by-line architectural mapping between NEXUS's `src/nexus_os/security/sanitizer.py` Python containment layer and the MXC OS-level containment layer, and the NEXUS-specific deltas (the CDR-stage trust engine, the FunctionGemma router intent-classifier, the Lethal Trifecta pre-flight, the new codex_auth_watchdog from finding #1) that MXC does not ship. Track the Microsoft Build 2026 / MXC / Scout / Agent 365 follow-up coverage for the July 2026 Agent 365 + MXC integration GA, the OpenShell / NemoClaw parity, and the surface-as-tablet Windows Surface Laptop Ultra + RTX Spark Dev Box launch, all of which are NEXUS-relevant because they define the production reference for "OpenClaw on Windows in an MXC sandbox" that the NEXUS mesh can co-deploy with.

### 3. Crossmint / Visa Intelligent Commerce / Basis Theory + the 7.1% OpenClaw-Skill Audit — The First *Production-Reference* PCI-Level-1 Tokenization Layer for Agent Card Payments, the First Time a *Measured Baseline* (7.1%) Has Been Published for Credential Exposure in Agent Skills, and a Direct Path to Extend NEXUS's `src/nexus_os/security/sanitizer.py` `nexus.credential.exposed` Counter from Card-PAN to a Multi-Credential-Type Family (OAuth refresh_token, AWS access key, GCP service account, K8s kubeconfig, SSH private key) with a Per-Type Loki Gauge Benchmarked Against the Crossmint Baseline
- Source: FinTech Magazine https://fintechmagazine.com/news/crossmint-launches-new-secure-api-for-ai-agent-card-payments (June 7, 2026); Forbes https://www.forbes.com/sites/digital-assets/2026/06/07/visa-mastercard-and-coinbase-are-fighting-over-how-ai-agents-pay/ (June 7, 2026 — Visa Intelligent Commerce, Mastercard Agent Pay, Coinbase, OpenAI Instant Checkout on Shared Payment Token); Crossmint Lobster.cash https://crossmint.com/ (open payment standard for AI agents, plans to integrate Mastercard Agent Pay + Verifiable Intent); Basis Theory https://basistheory.com/ (PCI Level 1, SOC 2 Type 2, ISO 27001 tokenization vault); aikido.dev https://www.aikido.dev/blog/codex-remote-ui-steals-ai-tokens (the 7.1% OpenClaw-skill stat is the Aikido audit, surfaced by Crossmint in their June 7 announcement)
- Why NEXUS cares: The Crossmint / Visa Intelligent Commerce / Basis Theory announcement is the first *production-grade* PCI-Level-1 tokenization layer for agent card payments, with three NEXUS-relevant primitives in one release. **(a) The 7.1% OpenClaw-skill exposure stat is the first measured baseline for credential exposure in agent skills** — the Aikido audit (cited in the Crossmint / FinTech Magazine disclosure) found that **7.1% of published OpenClaw skills expose raw card credentials in their source**, which is the same architectural category as NEXUS's `src/nexus_os/mcp/functiongemma_router.py` registered tool catalog (NEXUS OpenClaw skills are the canonical publication surface) and the same 7.1% baseline the 2026-06-05-00 #2 follow-up set up a `nexus.credential.exposed` counter to track. NEXUS should extend the counter from card-PAN/CVC to a multi-credential-type family (the 5 listed above + the OAuth refresh_token from finding #1's codexui-android disclosure) and publish a per-deployment "0% exposure across all 6 credential types" metric as a NEXUS dashboard panel — this is the *third* distinctive NEXUS marketing surface (after the OWASP L3 maturity score from 2026-06-05-12 #1 and the v2.0 failure-mode coverage score from 2026-06-05-18 #3) because it directly answers the buyer question "how exposed are my agent skills to credential leakage?" and the NEXUS answer is "0%, benchmarked against the 7.1% OpenClaw-skill baseline." **(b) Visa Intelligent Commerce AI-Ready Cards + Mastercard Agent Pay Agentic Tokens + OpenAI Shared Payment Token is the first production reference for the *per-task* agent-credential primitive** — i.e. a tokenized card credential scoped to a *single agent + single merchant + single transaction window* that the agent never sees as raw PAN. The same primitive is what NEXUS's `KaijuTaskCredential` (2026-06-06-12 #3), the new `KaijuCodexTaskCredential` (finding #1), and the OWASP L3 "ephemeral credentials" (2026-06-05-12 #1) require. NEXUS should adopt the Shared Payment Token wire format for the in-NEXUS per-task credential envelope (RFC 8785 JCS canonicalization, ed25519 signature, JWKS-published key, `merchant + agent + task + window` scope), so a NEXUS-equipped agent that needs to make a payment (or sign a Codex OAuth refresh) does so through a NEXUS-brokered token, never through a raw credential. **(c) Crossmint Lobster.cash integration with OpenClaw / Hermes / Zo Computer** is the first time a PCI-Level-1 payment layer has *natively integrated* with the OpenClaw agent framework, which is the same integration surface NEXUS's `src/nexus_os/mcp/functiongemma_router.py` registered tool catalog exposes. NEXUS should add a `nexus_payment_skill` reference implementation that uses the Crossmint / Visa Intelligent Commerce / Basis Theory Shared Payment Token pattern end-to-end (zero raw PAN in the skill code, zero raw PAN in the skill logs, per-skill / per-task / per-merchant scope, eGPU PII obfuscation per the NVIDIA OpenShell pattern from 2026-06-05-09 #2) so a NEXUS-equipped fleet that ships a payment-style skill inherits the 0% exposure baseline by default.
- Suggested follow-up: Extend the `nexus.credential.exposed` event-type family in `src/nexus_os/observability/audit_log.py` (per the 2026-06-05-00 #2 follow-up) with the five additional credential types (`oauth_refresh_token`, `aws_access_key`, `gcp_service_account`, `k8s_kubeconfig`, `ssh_private_key`) and a per-type regex set; the per-type regex set should match the Aikido Google-API-key 23-minute revocation-window finding (2026-06-01) so a deleted-but-cached credential is still flagged. Add a per-type Loki gauge `nexus.credential.exposed_rate_per_1k_calls{type=card_credential|oauth_refresh_token|aws_access_key|gcp_service_account|k8s_kubeconfig|ssh_private_key}` so the dashboard can publish a per-deployment exposure rate per credential type, benchmarked against Crossmint's 7.1% card-credential baseline. Add a `nexus_payment_skill` reference implementation in `src/nexus_os/mcp/` (or in a new `src/nexus_os/payments/` subpackage) that uses the Crossmint / Visa Intelligent Commerce / Basis Theory Shared Payment Token pattern end-to-end (zero raw PAN in skill code, zero raw PAN in skill logs, per-skill / per-task / per-merchant scope, JCS canonicalization, ed25519 signature) and a `nexus_payment_skill.validate()` pre-deploy check that fails the skill registration if any of the per-credential-type regexes matches a string in the skill source. Add a `docs/marketing/credential-exposure-parity.md` page that publishes the NEXUS per-deployment 0% credential-exposure metric per credential type, benchmarked against Crossmint's 7.1% OpenClaw-skill card-credential baseline, and that documents the line-by-line NEXUS defense per credential type (vault-brokered login + watchdog + revocation-lag detector + per-skill CI scan), so a buyer comparing NEXUS against the Microsoft Scout + Agent 365 SKU or against a Crossmint / Visa Intelligent Commerce deployment can see the multi-credential-type exposure defense NEXUS ships out of the box. Track the Forbes / CoinDesk / Crossmint / Basis Theory follow-up coverage for the Mastercard Agent Pay + Verifiable Intent integration (planned by Crossmint), the JPMorgan / Bank of America / Citigroup tokenized-deposit network (The Clearing House, H1 2027), and the OpenAI Instant Checkout Shared Payment Token Etsy / Shopify rollout, all of which are NEXUS-relevant because they define the production reference for the per-task agent-credential primitive that the new `KaijuCodexTaskCredential` (finding #1) and the existing `KaijuTaskCredential` (2026-06-06-12 #3) implement.

## Out-of-scope (saw, did not pursue)
- Quantinuum $1.68B IPO (2026-06-03) + Schrödinger's color-geometry paper at Los Alamos (2026-06-06) — quantum computing IPO + physics; not a NEXUS code path
- Anthropic "Mythos-class models" / "six to 12 months before Beijing catches up" (Politico 2026-06-07) — frontier model geopolitics, not NEXUS code
- OpenAI ChatGPT super-app revamp (FT 2026-06-07) — OpenAI product marketing, not NEXUS code
- OpenAI Lockdown Mode (2026-06-06) — already covered in 2026-06-06-18 and earlier; same ChatGPT-only containment primitive, no new NEXUS code path
- AGIBOT World Challenge 2026 (Shanghai, 526 teams, 27 countries, June 2026) — embodied AI benchmark, not NEXUS code
- McDonald's ArchIQ / Walmart Code Puppy / Nike World Cup / Cannes Lions — marketing, not NEXUS code
- Visa / Mastercard / Coinbase agentic commerce fight (Forbes 2026-06-07) — covered in finding #3; the agentic-payments angle is the production reference for the per-task credential primitive, no new NEXUS code path beyond the existing follow-ups
- Silent Ransom Group law-firm attacks (Mandiant + FBI FLASH, June 2026) — same social-engineering / in-person data-theft category as the 2026-06-06-12 SilentRansom and AspyrMedia items, no new NEXUS code path
- Detroit Free Press / Michigan $100 OpenAI Codex credits (2026-06-06) — education policy, not NEXUS code
- DOJ / DOGE Social Security data breach (TechCrunch 2026-06-07) — government data breach, same policy category as the 2026-06-06-12 / 2026-06-06-18 DOGE items, no NEXUS code path
- Surface Laptop Ultra + RTX Spark + RTX 5070 (Computex 2026) + Microsoft / NVIDIA / NemoClaw for Windows via WSL — same Build 2026 enterprise agent stack as finding #2 and 2026-06-06-18 #3; the MXC angle is the more NEXUS-relevant piece
- Cosmos-Construct/openclaw-codex-sdk + Cosmic-Construct OAuth backchannel — OpenClaw Codex SDK integration; the `codex-sdk` MCP backchannel already runs NEXUS, no new NEXUS code path
- friuns2/codex-mobile Issue #198 — same codexui-android disclosure as finding #1; the GitHub-side disclosure is the same data point, the npm-side exfiltration chain is the canonical NEXUS-relevant reference
- PixelML/claw-openai-codex-auth + entif-ai/codex-multi-auth + Mohit-Patil/clawdex-mobile + pique2233/Codex-CIL + attmous/codlink — Codex auth / multi-account / mobile bridge; same category as the codexui-android finding #1; no new NEXUS code path
- openclaw/openclaw#69511 secrets audit PLAINTEXT_FOUND for synthetic codex-app-server apiKey — same false-positive category, no new NEXUS code path
- minns.ai MinnsDB / Agent Forge / JS-TS SDK (2026-02-16 / 2026-03-27) — same agent-memory infrastructure category as Weaviate Engram; same NEXUS Mem0Adapter shape, no new NEXUS code path
- OctaMem Memory-as-a-Service (2026) — same agent-memory category as Weaviate Engram and minns.ai, same NEXUS Mem0Adapter shape, no new NEXUS code path
- engramhq.com / engramme.com / Pattern Agentic / TensorMem / Airweave YC X25 / Prava / r80 LLC — agent-memory infrastructure; same NEXUS Mem0Adapter competitive data point
- Mali / FTX / Coinbase / Stripe / Mastercard / Visa agent-pay partner / chatbot UIs / financial-services agent rollout — same enterprise agentic-AI rollout category as the 2026-06-06-12 / 2026-06-06-18 finance items, no new NEXUS code path
- Trump AI executive order (June 3, 2026) — government policy, same policy category as the 2026-06-06-12 / 2026-06-06-18 Trump AI items, no NEXUS code path
- Forbes "Rule-Followers Will Lose To AI" / Politico frontier AI hurricane warning / Axios "100B tokens/month" / Business Insider "ChatGPT no longer OpenAI's most important product" / TIME magazine "AI agents transformation" / Mashable "AI vibe shift" / Qualys / Forbes prompt engineering / Workera AI skills benchmark — opinion / marketing / vendor benchmark, not NEXUS code
- SportsLine / CBS Sports / Fox Sports / SportsLine NFL/NHL/MLB/NASCAR / 2026 French Open / 2026 Belmont Stakes / 2026 FireKeepers Casino 400 / 2026 FIFA World Cup odds / Mitch Marner hat-trick / Alexander Zverev French Open / Mirra Andreeva French Open / Golden Knights vs Hurricanes Stanley Cup Game 3 / NBA Finals Knicks vs Spurs / Peru runoff / Armenia election / Kosovo election / Trump attendance at MSG Game 3 / Toledo Ohio shooting / David Beckham / Sabrina MacLean / Rodney Petterson / Audrey Youngman / John O'Neill / William Wolfe / Carol Grieshaber / Ernie Clement diving play / Trade Compliance Records / Ses AI Corp — sports, business, finance, court, watch news, geopolitics, not NEXUS code
- Crypto / Bitcoin miners → AI data centers (IREN $9.7B Microsoft deal) / Broadcom sell-off (Forbes 2026-06-07) / Stock market / Best Stables / Wall Street / Intel Lip-Bu Tan Computex / Sam Altman token leader / Anthropic Claude Mythos — finance / chip industry / vendor marketing, not NEXUS code
- Bash / AIsa / Encoded Ltd / r80 / WasabiCard / Cardnetix / Prava — agent payment API / PSP / card-issuing platforms; same Crossmint / Basis Theory production-reference category as finding #3, no new NEXUS code path
- AI worm (Canadian researchers, Foro3D, 2026-06) — same Papernot / U Toronto / Vector / ServiceNow / Cambridge AI worm quantification as 2026-06-04-06 #2, 2026-06-04-12, 2026-06-05-09, 2026-06-05-12; no new NEXUS code path
- Microsoft Build 2026 / Microsoft Defender for Cloud / Microsoft Defender for AI Workloads / Microsoft Surface RTX Spark Dev Box / MAI-Thinking-1 / MAI-Code-1 / Microsoft Foundry Local / Microsoft Q / Microsoft Copilot Studio / Microsoft Fabric + OneLake + Foundry / Windows 365 for Agents / Microsoft Purview + Claude Compliance API — same Microsoft Build 2026 enterprise stack as finding #2 and 2026-06-06-12 / 2026-06-06-18; the MXC + Agent 365 + Scout angle is the more NEXUS-relevant piece
- Microsoft DART JustAskJacky (already covered in 2026-06-06-12) — same Microsoft DART AI assistant malware category
- Salesforce Agentforce 360 / HubSpot Breeze Agents / Workday Illuminate / Atlassian Rovo / Stripe agentic payments / Notion AI 3.0 / Slack Workflow Builder + Agent Builder / Microsoft Q / Microsoft Foundry / Copilot Studio one-click publishing — vendor agent stack, same category as the 2026-06-06-12 / 2026-06-06-18 enterprise agent rollout items, no new NEXUS code path
- AWS Bedrock AgentCore / Amazon Q Developer / Google Vertex AI Agent Engine / Azure AI Foundry Agent Service / IBM watsonx Orchestrate / Salesforce Agentforce / ServiceNow Now Assist / Oracle AI Agent Studio / MuleSoft Agent Fabric — vendor agent platform coverage, no NEXUS code path
- Weaviate Engram (lokmattimes 2026-06-06) — same agent-memory infrastructure category as NEXUS Mem0Adapter; the Engram launch is a competitive data point but does not introduce a new NEXUS code path
- Weaviate 1.37 (April 2026) + Weaviate MCP server at /v1/mcp — same vector-DB + MCP category as the 2026-06-06-12 Pinecone Nexus / serverless 2.0 items; the Weaviate MCP server is a reference implementation, no new NEXUS code path
- OBO actor facet / Entra Agent ID / Scout / Agent 365 / Work IQ / Microsoft Purview / Microsoft Threat Intelligence Claude Code OIDC theft — already covered in 2026-06-06-12 / 2026-06-06-18, the MXC + Scout + OpenClaw angle in finding #2 is the more NEXUS-relevant new piece
- Horizon Media Agentic Commerce Trust Report (Chain Store Age 2026-06) / InternetRetailing agentic commerce / Gopuff + Grok agentic commerce (CNBC 2026-06-04) / Practical Ecommerce New Tools (2026-06-03) — same agentic commerce category as finding #3, no new NEXUS code path
- EU AI Act / NIST AI RMF / ISO/IEC 42001 / Singapore Model AI Governance Framework v2 / Japan AI Promotion Act / Korea AI Basic Act / Brazil AI Bill / India DPDP Act / China Generative AI Measures / UK AI Bill / Canada AIDA / COSAiS SP 800-53 overlays — government policy, no NEXUS code path
- Trump Sanders 50% public ownership / On public ownership in AI — government policy, not NEXUS code
- Microsoft Surface Ultra / ZDNET / Nvidia RTX Spark — same Surface Laptop Ultra / Computex 2026 category as finding #2 and 2026-06-06-12, no new NEXUS code path
- Ryan Kavanaugh / Lights Out — entertainment / business, not NEXUS code
- Aikido Security deleted Google API key 23-minute revocation window (June 2026) + AWS 4-second IAM eventual consistency window (offensai.com) — captured in finding #1's "revocation_lag" follow-up, no separate NEXUS code path
