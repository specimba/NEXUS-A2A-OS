# NEXUS Research Scout — 2026-06-11 00:00 UTC

## Source quick-scan
- arxiv: 0 new thermodynamic/BEC/EPR hallucination-detection papers this window (the SkilLab AI Research Watch edition 2 (June 9) confirms the SWE-bench Verified → SWE-bench Pro migration is the canonical eval shift; CEDAR at UIUC, PAS at Los Alamos, and the Generative AI pub "Uncertainty Architecture" are uncertainty/hallucination-governance pieces but not thermodynamic/BEC/EPR)
- News: 6 query sweep (thermo/BEC/EPR hallucination, MCP/A2A best practices, SLM function-calling routing, adaptive circuit-breaker LLM gateway, agent gateway A2A MCP release, model-routing overhead) + 6 web_research passes on (1) Databricks/Linux Foundation OpenSharing launch (June 10, 2026) — first open vendor-neutral protocol for cross-org AI-asset + agent-skill + unstructured-data sharing, hosted by the Linux Foundation, (2) Liquibase Secure 5.2 "Agent Safe Governance" (June 10, 2026) — first database-change-management vendor to ship an MCP server that hooks AI-generated DDL/changelog/rollback into the same pre-deploy policy + drift-detection + audit + VEX controls as human-written changes, (3) JFrog + Anthropic JFrog Platform plugin for Claude Code (June 10, 2026) — first artifact-supply-chain vendor to ship an MCP + Agent Skills Registry for a major coding agent, with provenance + license-compliance + secret-scan gates at write time, (4) lakeFS for Agentic AI (June 10, 2026) — first data-versioning vendor to ship per-agent zero-copy data branches with pre-merge policy + agent-identity-bound audit, (5) Topaz "Explainable Model Routing" paper (already covered in 2026-06-10-06 #2 as the canonical gateway-layer explainable-routing reference; nothing new this window), (6) Lynkr v1.7-A P6 routing table + DeLM shared-context SWE-bench +10.5pp (June 9-10, 2026 Ruflo issue #2343) — micro-routing + multi-agent context-shared reference architectures
- GitHub trending / project activity (relevant repos): **mcp-odoo v0.4.0** (tuanle96, 2026-06-10) shipped multi-instance support with per-instance approval tokens + per-instance schema caches + 412 unit tests, the first Odoo MCP server to formally separate instance credentials from instance data; **mcp-registry v0.2.0 + @incultnitollc/mcpr `install` command** (June 10, 2026) shipped a cross-client MCP config installer with JSON deep-merge + atomic writes + npm-only publishing, the first registry client to surface `mcp-probe` validation + `mcp-vouch` OWASP MCP Top 10 trust grades; **tuanle96/mcp-odoo, mcp-odoo v0.4.0** also reports 25 default MCP tools; **OriginTrail DKG v10.0.0-rc.17** (2026-06-10 pre-release) shipped per-KA Chorus memory + unified `/api/knowledge-assets` + OpenClaw-aware agent-tooling via `SKILL.md`; **ruflo issue #2343** documents the **DeLM** (arXiv:2606.10662, Mao & Mirhoseini, Jun 9, 2026) shared-context architecture hitting +10.5pp SWE-bench at −50% cost, with a 23-line `SwarmContextSnapshot` feature flag (`snapshot_context`) at swarm-spawn; **Cequence Security / Boomi Agentstudio + Snowflake Cortex** (June 10, 2026) — first vendor to ship a Tower-style cross-vendor agent governance UI, maps onto the existing NEXUS `NexusObservability` substrate; **Neura Agentic Development for AWS Trainium** (June 10, 2026) — first kernel-development skill-pack to ship Neuron NKI agent skills for write/debug/profile/analyze

## Findings (NEXUS-relevant only)

### 1. Databricks + Linux Foundation OpenSharing — First Open, Vendor-Neutral, Linux-Foundation-Hosted Protocol for Cross-Organizational Sharing of Agent Skills + AI Models + Unstructured Data, Launched 2026-06-10 as the First Protocol to Extend Delta-Sharing-Style Serverless Peer-to-Peer Asset Exchange to the Agentic Era With Native Iceberg-IRC + Multi-Cloud Interop, the First Open Standard to Define Discovery + Authorization + Access in a Single API Without Data Movement, and a Direct Path to Add a `NexusOpenSharingAdapter` to `src/nexus_os/vault/knowledge_sharing.py` (or new `src/nexus_os/exchange/`) So a NEXUS-Equipped Agent Fleet Can Publish and Consume Agent Skills, Models, and Structured Datasets Across Organizational Boundaries Without Re-Encoding the Authorization Layer
- Source: **Linux Foundation announcement** ("The Linux Foundation Announces OpenSharing Project to Standardize AI Asset and Data Exchange", 2026-06-10, PRNewswire) https://www.prnewswire.com/news-releases/linux-foundation-announces-opensharing-project-to-standardize-ai-asset-and-data-exchange-302796802.html ; **Databricks announcement** ("Databricks Announces OpenSharing, a New Open Standard for Sharing of Data and AI Assets Across Platforms and Organizations", 2026-06-10) https://www.databricks.com/company/newsroom/press-releases/databricks-announces-opensharing ; **Third News coverage** ("The Linux Foundation's OpenSharing: A Game-Changer for AI Asset and Data Exchange", 2026-06-10) https://third-news.com/article/ee89ea8e-64de-11f1-9ed2-9ca3ba08e13f ; **Forgenex analysis** ("Databricks OpenSharing: The End of 'Email Me a File' for AI Agents", 2026-06-10) https://www.forgenex.com/en/blog/databricks-opensharing-el-fin-del-email-me-a-file-para-agentes-de-ia ; **Databricks Storage Ecosystem blog** ("Announcing the Databricks storage ecosystem: Governing the enterprise data estate, wherever it lives", 2026-06-10) https://www.databricks.com/blog/announcing-databricks-storage-ecosystem-governing-enterprise-data-estate-wherever-it-lives (covers MinIO AIStor as the first on-prem OpenSharing endpoint + VAST Data extension + Iceberg IRC interop)
- Why NEXUS cares: OpenSharing is the *first* open, vendor-neutral, Linux-Foundation-hosted protocol for cross-organizational AI-asset + agent-skill + unstructured-data exchange, and the launch closes the canonical 2026 gap that has been implicit in every prior finding about MCP / A2A / ACS / PortAuth: *all of those protocols assume the agent and the asset are inside the same trust boundary*. OpenSharing is the *cross-boundary* primitive, and the design choices map line-for-line onto the NEXUS substrate. Three concrete maps. **(a) Agent-skill + AI-model + unstructured-data exchange ↔ NEXUS's `NexusAgentSkillsRegistry` and the `NexusKnowledgeBase` (per `NEXUS-OS/knowledge/nexus_kb.py`)** — OpenSharing's "publish and consume" model is a peer-to-peer extension of Delta Sharing, and the canonical 2026 use case is "I want my agent to consume a colleague team's curated skill bundle without copy-pasting the JSON." The recommended NEXUS follow-up is to add a `NexusOpenSharingAdapter` to `src/nexus_os/vault/knowledge_sharing.py` (or a new `src/nexus_os/exchange/opensharing_adapter.py`) that exposes a `nexus.opensharing.publish(skill_id, version, manifest, signature)` and `nexus.opensharing.consume(organization, skill_id, version, verify=True)` Python API, where `verify=True` checks the asset's signature against the publisher's JWKS-pinned ed25519 key (the same `NEXUS_PUBLISHER_KEY` pattern recommended in 2026-06-06-18 #1 for MCP server identity). **(b) Authorization without data movement ↔ NEXUS's existing `KaijuTaskCredential` (per the 2026-05-18 safety merge + 2026-06-07-18 #3) and the `NexusPortAuthConstraintAdapter` follow-up (per 2026-06-10-12 #2)** — OpenSharing's authorization model is "the publisher signs the asset and pins the access policy in the manifest; the consumer receives a short-lived bearer token bound to (publisher, consumer, asset, time_window); no data copy is ever made." This is *structurally* the same constraint-attenuation model that `KaijuTaskCredential` + `NexusPortAuthConstraintAdapter` already implement; the recommended follow-up is to wire the OpenSharing adapter to NEXUS's existing `KaijuTaskCredential` engine so a NEXUS-issued credential can be used as the OpenSharing bearer token (no re-encoding) and emit a `nexus.opensharing.credential_eval{organization, skill_id, version, verdict=allow|deny, latency_ms, constraint_set}` Loki event on every consume attempt. **(c) Multi-cloud + on-prem interop via Iceberg IRC ↔ NEXUS's existing on-prem + cloud hybrid design (per AGENTS.md, the 3-vCPU / 4GB-RAM Debian 12 + 80GB-disk runtime)** — OpenSharing's first on-prem implementation is MinIO AIStor (per the Databricks Storage Ecosystem blog, 2026-06-10), and the design choice is "Iceberg IRC clients + Unity Catalog + OpenSharing endpoints, no data movement". The recommended NEXUS follow-up is to add a `nexus.opensharing.boundary_exchange{from_org, to_org, asset_type, bytes_exchanged, mechanism=opensharing|sftp|none}` Loki event so a downstream audit can verify the canonical 2026 default ("OpenSharing, not email-me-a-file") is being honored, and a `nexus.opensharing.fallback_used{from_org, to_org, reason=network|auth|schema|version}` Loki event whenever the OpenSharing path fails and a fallback is used, so a downstream dashboard can graph the cross-org asset-flow health.
- Suggested follow-up: Add `src/nexus_os/exchange/opensharing_adapter.py` exposing `nexus.opensharing.publish(skill_id, version, manifest, signature)` and `nexus.opensharing.consume(organization, skill_id, version, verify=True)` Python APIs; pin publisher identity via the existing `NEXUS_PUBLISHER_KEY` JWKS pattern (per 2026-06-06-18 #1) so every published asset carries a signed `publisher=ed25519:fingerprint:...` field in its manifest. Wire the OpenSharing adapter to the existing `KaijuTaskCredential` engine (per 2026-05-18 safety merge) so a NEXUS-issued credential *is* the OpenSharing bearer token (no re-encoding); emit `nexus.opensharing.credential_eval{organization, skill_id, version, verdict=allow|deny, latency_ms, constraint_set}` Loki events on every consume attempt and a `nexus.opensharing.constraint_attenuation_chain{credential_id, parent_credential, child_credential, attenuation_reason}` Loki event on every constraint-attenuation operation. Add `nexus.opensharing.boundary_exchange{from_org, to_org, asset_type=skill|model|unstructured|structured, bytes_exchanged, mechanism=opensharing|sftp|none, signature_verified}` Loki events on every cross-org asset exchange, and `nexus.opensharing.fallback_used{from_org, to_org, reason=network|auth|schema|version|policy}` Loki events on every fallback to a non-OpenSharing path; the canonical Loki alert is "fallback rate > 5% over 1h" so a downstream audit can verify the OpenSharing default is being honored. Add `tests/test_opensharing_adapter.py` that uses a fixture publisher (ed25519 keypair) and a fixture consumer (different ed25519 keypair); assert a published `skill_id=v1` round-trips through `consume(verify=True)` with the signature check passing, assert a tampered manifest fails `consume(verify=True)` with a `credential_eval{verdict=deny}` event, and assert the cross-org exchange emits the expected Loki events with the right `signature_verified` flag. Add `docs/architecture/opensharing_interop.md` documenting the 1:1 mapping between OpenSharing primitives (publisher / consumer / asset manifest / bearer credential) and NEXUS modules (`NexusAgentSkillsRegistry` / `KaijuTaskCredential` / `NexusPortAuthConstraintAdapter` / `NexusProofChain`); include a worked example showing how a NEXUS-issued `KaijuTaskCredential` for `agent_id=builder-bot, scope=skill:read+skill:invoke, constraint_set={skill_id in {v1,v2}, time_window in weekdays 8am-6pm}` can be presented as an OpenSharing bearer token to consume a skill from a partner organization without re-encoding.

### 2. Liquibase Secure 5.2 "Agent Safe Governance" — First Database-Change-Management Vendor to Ship an MCP Server That Hooks AI-Generated DDL + Changelog + Rollback Logic Into the Same Pre-Deploy Policy + Drift-Detection + Audit + VEX Controls as Human-Written Changes, the First Vendor to Define a Four-Capability "Agent Safe" Substrate (Governed Authoring + Pre-Deployment Policy Checks + Drift Detection + VEX Integration) That Treats AI and Human Database Changes as Identical First-Class Citizens, and a Direct Path to Add a `NexusDatabaseChangeMCPAdapter` to `src/nexus_os/mcp/server.py` (or New `src/nexus_os/db/change_management.py`) So a NEXUS-Equipped Agent Fleet Cannot Bypass the Existing `NexusCompliance` Policy Engine When Generating Database Migrations
- Source: **Liquibase announcement** ("Liquibase Secures Your Database Changes. Human or AI." 2026-06-10) https://www.liquibase.com/blog/liquibase-secures-your-database-changes-human-or-ai?goaltrackinglabel=5.2-Secure-Announcement ; **SecurityBrief coverage** ("Liquibase Secure adds AI governance for database changes", 2026-06-10) https://securitybrief.news/story/liquibase-secure-adds-ai-governance-for-database-changes ; **Security Boulevard coverage** ("Liquibase Secures Your Database Changes. Human or AI. | Liquibase Secure", 2026-06-10) https://securityboulevard.com/2026/06/liquibase-secures-your-database-changes-human-or-ai-liquibase-secure/ ; **ADVFN coverage** ("Liquibase Introduces Agent Safe Governance for AI-Generated Database Change", 2026-06-10) https://mx.advfn.com/bolsa-de-valores/noticias/98710999/liquibase-introduces-agent-safe-governance-for-ai
- Why NEXUS cares: Liquibase Secure 5.2's "Agent Safe Governance" is the *first* database-change-management vendor to ship an MCP server that treats AI-generated DDL/changelog/rollback as a *first-class citizen* subject to the same pre-deployment policy checks, drift detection, audit trail, and recovery controls as human-written changes, and the design closes a *specific* gap that every prior agent-governance finding (ACS, PortAuth, MCPGuard, Aegis-Layer) has implicitly relied on but never addressed: *agents that write to a database need their changes gated by the same change-management system that humans use, and the gate must be invoked via the same MCP substrate the agent already speaks.* Three concrete NEXUS maps. **(a) AI-generated DDL ↔ NEXUS's existing `src/nexus_os/db/encryption.py` (per 2026-05-18 safety merge, DB encryption policy hard-fails by default) and the `NexusCompliance` policy engine at `src/nexus_os/governor/compliance.py`** — Liquibase's "AI can produce a Liquibase-formatted changelog, but the changelog must pass the same pre-deployment policy checks as a human's" is the canonical "AI is not a separate trust class" architecture, and the NEXUS equivalent is to require every AI-generated DDL to flow through the existing `NexusCompliance` policy engine *before* the DDL is allowed to touch the production database. The recommended follow-up is to add a `NexusDatabaseChangeMCPAdapter` to `src/nexus_os/mcp/server.py` (or a new `src/nexus_os/db/change_management.py`) that exposes an MCP tool `nexus.db.propose_change(changelog_yaml, target_database, agent_id, task_credential_id)` which: (i) parses the changelog, (ii) submits it to the existing `NexusCompliance` engine for evaluation (the same engine that enforces the canonical 2026-05-18 hard-fail DB encryption policy), (iii) on `verdict=allow` returns a `proposal_id`, on `verdict=deny` returns the policy violations, on `verdict=hold` adds it to the existing `KaijuAuth` hold queue (per 2026-05-18). **(b) Drift detection ↔ NEXUS's existing drift detection surface (per 2026-05-18 safety merge, the `NexusCompliance` policy engine has a "drift detection" rule) and the `NexusProofChain` audit trail at `src/nexus_os/governor/proof_chain.py`** — Liquibase's "drift detection ensures that any state divergence between the live database and the expected state is caught and recorded" is the canonical 2026 reference for the substrate NEXUS has already committed to; the recommended NEXUS follow-up is to extend the existing drift-detection rule in `NexusCompliance` to emit a `nexus.db.drift_detected{change_id, expected_state, actual_state, drift_class=schema|data|permission, severity=info|warn|critical, agent_id, task_credential_id}` Loki event and append a `db.drift_detected` event to the existing `NexusProofChain` so a downstream audit can replay the drift history. **(c) VEX integration (Vulnerability Exploitability eXchange) ↔ NEXUS's existing `NexusSecuritySanitizer` (per 2026-05-18 safety merge) and the recommended `NexusTokenRotationGapDetector` (per 2026-06-09-12 #3)** — Liquibase's VEX support means "this database change introduces a known-vulnerable library version, but the vulnerability is *not exploitable* in our deployment because of X" is now a first-class artifact in the change-management pipeline. The recommended NEXUS follow-up is to add a `NexusVEXAdapter` to `src/nexus_os/security/sanitizer.py` that, on every database change proposal, checks the proposed change's library-version delta against a VEX feed (loaded from `src/nexus_os/security/vex_feed.json`, seeded with the OpenSSL CVE-2026-45447 family and other 2026 VEX records), and emits a `nexus.vex.vulnerability_assessed{change_id, cve_id, library, old_version, new_version, exploitable=true|false, justification}` Loki event; the `justification` field is the human-readable VEX rationale.
- Suggested follow-up: Add `src/nexus_os/mcp/tools/nexus_db_propose_change.ts` (or a Python equivalent) that exposes the `nexus.db.propose_change(changelog_yaml, target_database, agent_id, task_credential_id)` MCP tool, parses the changelog, submits it to the existing `NexusCompliance` engine (per 2026-05-18 safety merge), and returns `{proposal_id, verdict, policy_violations, hold_queue_position}` JSON; wire the tool to the existing `KaijuAuth` hold queue so a `verdict=hold` proposal is queued behind the existing hold-queue dashboard. Extend the existing drift-detection rule in `NexusCompliance` to emit `nexus.db.drift_detected{change_id, expected_state, actual_state, drift_class=schema|data|permission, severity=info|warn|critical, agent_id, task_credential_id}` Loki events and append a `db.drift_detected` event to the existing `NexusProofChain` (per 2026-05-18 safety merge); the canonical NEXUS alert is "drift severity=critical in the last 5 minutes" so a downstream dashboard can flag production-affecting drift in real time. Add `NexusVEXAdapter` to `src/nexus_os/security/sanitizer.py` that loads `src/nexus_os/security/vex_feed.json` (seeded with the OpenSSL CVE-2026-45447 family + LiteLLM CVE-2026-42271 / CVE-2026-48710 from 2026-06-09-12 #1 + Microsoft 206-CVE Patch Tuesday 2026-06-10 + Windows BitLocker CVE-2026-50507) and, on every database change proposal, emits a `nexus.vex.vulnerability_assessed{change_id, cve_id, library, old_version, new_version, exploitable=true|false, justification}` Loki event. Add `tests/test_database_change_mcp_adapter.py` that exercises four scenarios: (i) a benign changelog (add column) returns `verdict=allow` with a `proposal_id`, (ii) a DDL change that violates the canonical 2026-05-18 hard-fail DB encryption policy returns `verdict=deny` with the policy violations, (iii) a high-impact schema change returns `verdict=hold` with the `KaijuAuth` hold-queue position, (iv) a changelog that introduces a known-vulnerable library version with a VEX-justified non-exploitability returns `verdict=allow` with the VEX justification in the audit trail. Add `docs/architecture/database_change_governance.md` documenting the 1:1 mapping between Liquibase Secure 5.2's four Agent-Safe capabilities (Governed Authoring, Pre-Deployment Policy Checks, Drift Detection, VEX Integration) and NEXUS modules (`NexusDatabaseChangeMCPAdapter`, `NexusCompliance`, `NexusProofChain`, `NexusVEXAdapter`); include a worked example showing how a NEXUS agent's `propose_change` call flows through the canonical 2026-05-18 safety substrate end-to-end.

### 3. JFrog Platform Plugin for Claude Code + lakeFS for Agentic AI — First Artifact-Supply-Chain Vendor (JFrog) to Ship an MCP + Agent Skills Registry That Gates a Major Coding Agent's Dependency Use at Write Time, and First Data-Versioning Vendor (lakeFS) to Ship Per-Agent Zero-Copy Data Branches With Pre-Merge Policy + Agent-Identity-Bound Audit, the Two Together Closing the Canonical 2026 "Agent Sees the Right Dependency + the Right Data Branch + the Right Audit Trail" Substrate, and a Direct Path to Add a `NexusArtifactSupplyChainMCPAdapter` to `src/nexus_os/mcp/server.py` and a `NexusAgentDataBranch` Module to `src/nexus_os/vault/` So a NEXUS-Equipped Agent Fleet Cannot Pull Unverified Artifacts and Cannot Mutate Production Data Outside Its Per-Agent Branch
- Source: **JFrog + Anthropic joint announcement** ("JFrog and Anthropic Bring Enterprise-Grade Software Supply Chain Governance and Security to Claude Code", 2026-06-10) https://www.businesswire.com/news/home/20260610800676/en/JFrog-and-Anthropic-Bring-Enterprise-Grade-Software-Supply-Chain-Governance-and-Security-to-Claude-Code ; **GuruFocus coverage** (2026-06-10) https://www.gurufocus.com/news/8910026/jfrog-and-anthropic-bring-enterprisegrade-software-supply-chain-governance-and-security-to-claude-code ; **Las Vegas Sun coverage** (2026-06-10) https://lasvegassun.com/news/2026/jun/10/jfrog-and-anthropic-bring-enterprise-grade-softwar/ ; **ADVFN coverage** (2026-06-10) https://mx.advfn.com/bolsa-de-valores/NASDAQ/FROG/noticias/98713142/jfrog-and-anthropic-bring-enterprisegrade-software-supply-chain-governance-and-security-to-claude-code ; **lakeFS for Agentic AI announcement** ("lakeFS for Agentic AI: Isolated, Reproducible Enterprise Data for Every Agent", 2026-06-10) https://www.businesswire.com/news/home/20260610833771/en/lakeFS-for-Agentic-AI-Isolated-Reproducible-Enterprise-Data-for-Every-Agent ; **Yahoo Finance / BusinessWire coverage** (2026-06-10) https://finance.yahoo.com/sectors/technology/articles/lakefs-agentic-ai-isolated-reproducible-120000429.html
- Why NEXUS cares: Two complementary 2026-06-10 product launches ship the *first* open-vendor primitives for "an AI agent should not pull unverified artifacts (JFrog) and should not mutate production data outside its own branch (lakeFS)", and together they close the canonical 2026 attack surface that the existing NEXUS substrate (per AGENTS.md: `NexusTokenRotationGapDetector` + `NexusDeveloperConfigAuditor` + `NexusC2FamilyDetector` per 2026-06-09-12) has been defending against but not *preventing*. The two launches are direct empirical validation of two NEXUS substrate choices and a direct gap. **(a) JFrog's MCP + Agent Skills Registry ↔ NEXUS's existing `NexusMCPServerManifestDriftGate` (recommended 2026-06-10-18 #3) and the new `NexusDeveloperConfigAuditor` (recommended 2026-06-09-12 #2)** — JFrog's plugin ships a verified-only MCP server registry (an agent can only `tools/call` an MCP server that is on the verified list) and a verified-only Agent Skills Registry (an agent can only invoke a skill that is on the verified list); the registry gates *every* dependency the agent pulls (npm, PyPI, Maven, Docker, JFrog itself). The NEXUS equivalent is the recommended `NexusMCPServerManifestDriftGate` + a new `NexusArtifactProvenanceGate` that, on every artifact-resolution request from any registered agent, checks the artifact's provenance + signature + license + CVE-status against a NEXUS-pinned registry (loaded from `src/nexus_os/security/artifact_provenance.json`, seeded with the 18B+ artifacts JFrog manages) and emits a `nexus.artifact.provenance_check{artifact, source, signature_verified, license, cve_count, severity=info|warn|critical}` Loki event; a `severity=critical` artifact is blocked from resolution. **(b) lakeFS for Agentic AI's per-agent zero-copy data branches ↔ NEXUS's existing `src/nexus_os/vault/memory_tracks.py` (5-track vault: episodic / semantic / procedural / reflective / working, per 2026-05-18 safety merge) and the new `NexusAdversarialMemoryMirror` (recommended 2026-06-10-18 #2)** — lakeFS's design is "every agent gets its own zero-copy data branch with immutable version pinning + pre-merge policy validation + agent-identity-bound audit + recovery in seconds", which is *exactly* the substrate NEXUS's vault needs at the *data* layer (the existing 5-track vault is at the *memory* layer). The recommended follow-up is to add a `NexusAgentDataBranch` module to `src/nexus_os/vault/` that, on every NEXUS agent spawn, creates a per-agent zero-copy data branch in the local lakeFS-compatible substrate (e.g., a DVC or `git-annex`-backed branch), pins the initial data version immutably, requires pre-merge policy validation on every data write (wired to the existing `NexusCompliance` engine per 2026-05-18 safety merge), and records the agent's `agent_id` + `run_id` + `task_credential_id` in the audit trail via `NexusProofChain`. **(c) Pre-merge policy validation ↔ NEXUS's existing `NexusCompliance` (per 2026-05-18 safety merge) and the new `NexusDatabaseChangeMCPAdapter` from finding #2 above** — lakeFS's "merges to production require pre-merge validations and carry agent identity and context, creating a unified audit trail" is the canonical *data* substrate complement to the *code* substrate that finding #2's Liquibase Secure 5.2 ships; the recommended NEXUS follow-up is to require that every `NexusAgentDataBranch.merge_to_production(branch_id, agent_id, task_credential_id)` call is gated by the *same* `NexusCompliance` engine that gates database changes, so an agent cannot merge data to production without a policy-clean pre-merge evaluation; emit `nexus.data.branch_merge{branch_id, agent_id, task_credential_id, verdict=allow|deny|hold, policy_violations, latency_ms}` Loki events and append `data.branch_merge` events to `NexusProofChain`.
- Suggested follow-up: Add `src/nexus_os/mcp/tools/nexus_artifact_provenance.ts` (or a Python equivalent) that exposes the `nexus.artifact.resolve(coordinates, agent_id, task_credential_id)` MCP tool, checks the artifact's provenance + signature + license + CVE-status against `src/nexus_os/security/artifact_provenance.json` (seeded with the JFrog 18B+ artifact universe's policy digest), and returns `{verdict, signature_verified, license, cve_count, severity, blocked_reason}` JSON; a `verdict=block` artifact is refused and a `nexus.artifact.provenance_check{artifact, source, signature_verified, license, cve_count, severity=info|warn|critical}` Loki event is emitted. Add `src/nexus_os/vault/agent_data_branch.py` exposing `nexus.data.branch_create(agent_id, task_credential_id, base_version)` and `nexus.data.branch_merge_to_production(branch_id, agent_id, task_credential_id, policy_eval_id)` Python APIs; the create API pins the initial data version immutably and records `(agent_id, run_id, task_credential_id, branch_id, base_version_hash)` in `NexusProofChain`; the merge API gates on the existing `NexusCompliance` engine (per 2026-05-18 safety merge) and emits a `nexus.data.branch_merge{branch_id, agent_id, task_credential_id, verdict=allow|deny|hold, policy_violations, latency_ms}` Loki event. Wire the artifact-provenance adapter to the existing `NexusMCPServerManifestDriftGate` (recommended 2026-06-10-18 #3) so a `verdict=block` MCP-server artifact cannot be registered; wire the agent-data-branch module to the existing `NexusAdversarialMemoryMirror` (recommended 2026-06-10-18 #2) so a known-malicious agent's data-branch writes are quarantined for forensic replay. Add `tests/test_artifact_provenance_and_data_branch.py` that exercises four scenarios: (i) a known-good artifact returns `verdict=allow` with `signature_verified=true`, (ii) an artifact with an unknown signature returns `verdict=block` with `severity=critical`, (iii) an artifact with a known-vulnerable library version (VEX-not-exploitable) returns `verdict=allow` with the VEX justification, (iv) a data-branch merge to production without a policy-clean pre-merge evaluation returns `verdict=hold` with the policy violations. Add `docs/architecture/agent_data_branch_and_artifact_provenance.md` documenting the 1:1 mapping between JFrog's MCP+Skills Registry and lakeFS's per-agent branches and the NEXUS substrate (`NexusMCPServerManifestDriftGate` + `NexusArtifactProvenanceGate` + `NexusAgentDataBranch` + `NexusCompliance` + `NexusProofChain`); include a worked example showing a NEXUS agent's `artifact.resolve(npm:lodash@4.17.21)` + `data.branch_merge_to_production(branch_id=run-42, agent_id=builder-bot)` calls flowing through the canonical substrate end-to-end and emitting the expected Loki events.

## Out-of-scope (saw, did not pursue)
- Anthropic Claude Fable 5 / Mythos 5 (Infosecurity Magazine / The Hacker News / Futurum / Tiatra / aidevme / Blockchain.News, 2026-06-09 to 2026-06-10) — same Mythos-class model as 2026-06-09-12 / 2026-06-09-18 / 2026-06-10-06 / 2026-06-10-12 / 2026-06-10-18 out-of-scope; the June 10 coverage adds the Stripe 50M-line Ruby migration demo (claude5.ai) + the Mythos 5 crypto-supply-chain concern (Blockchain.News) but these are use-case stories, no new NEXUS code path
- Claude Managed Agents Dreaming + Outcomes + Multiagent Orchestration (byteiota, 2026-06-10) — same Claude Managed Agents roadmap story as the 2026-06-10 Blockchain.News piece on Scheduling + Secure CLI; the Dreaming/Outcomes/Multiagent additions are product features, no new NEXUS code path beyond the already-captured Akeyless 2026 report in 2026-06-10-12 #3
- Ory Agent DX for Claude Code / OpenAI Codex / Gemini CLI (einpresswire, 2026-06-10) — same identity-as-plugin category as 2026-06-10-12 out-of-scope; the Ory plugin is a developer-experience addition, the underlying OAuth2 / OIDC substrate is already captured in the Akeyless + Gravitee + Kyndryl PortAuth + Microsoft ACS findings
- Microsoft Patch Tuesday June 2026 (The Hacker News / Dark Reading / CyberSecurityNews, 2026-06-10) + Windows BitLocker 0-day CVE-2026-50507 + Windows Kernel CVE-2026-45657 (use-after-free, 9.8) + HTTP.sys CVE-2026-47291 (integer overflow, 9.8) + DHCP Client CVE-2026-44815 (stack overflow, 9.8) — same monthly-patch category, no LLM/agent code path
- Juniper / Cisco / OpenSSL / SAP / Apache / FFmpeg / OP-512 / UniFi / Gogs / cPanel / Magento CVEs — already covered in 2026-06-09-12 out-of-scope
- Amazon v. Perplexity Comet AI browser / Computer Fraud and Abuse Act (Bloomberg Law News, 2026-06-10) — legal test case for agent disclosure; not a NEXUS code path
- PTC Orbit + PTC Jetstream AI platform (Stock Titan, 2026-06-10) — enterprise product lifecycle / CAD / PLM platform with 12 AI agents; not NEXUS code
- Bitget Wallet tokenized RWA / DEX Aggregator API (markets.businessinsider, 2026-06-10) — same tokenized-RWA category as 2026-06-10-06 / 2026-06-09-12 out-of-scope
- Decart Oasis 3 world model (TechCrunch, 2026-06-10) — same world-model / video-generation category as 2026-06-10-18 out-of-scope
- MyLex.AI / Aurelian Callie / Close Chloe / Atomicwork Governed AI / Taskade / agnt8x / Sierra + Amadeus / Woodstock MCP / Weaviate Engram / Boomi Agentstudio + Snowflake Cortex — same vendor-agentic-workforce category as 2026-06-09-12 / 2026-06-10-06 / 2026-06-10-12 out-of-scope
- Apple WWDC 2026 + iOS 27 on-device AI (MacRumors / Verge, 2026-06-09) + Gemma 4 12B benchmarks (TECHSY / LavX, 2026-06) + DiffusionGemma 26B-A4B-it (NVIDIA / Google Developers Blog, 2026-06) + Qwen Code v0.17.x (Qwen Code Docs, 2026-06-04) + xda-developers local LLM tool-calling piece (xda-developers, 2026-06) + ToolingGemma 270M (Level Up Coding, 2026-06) + Needle 26M attention network (GitHub 306026185, 2026-06) + Switchcraft DistilBERT classifier (already covered in 2026-06-10-18 #1) — same on-device / FunctionGemma-class category, no new NEXUS code path
- Topaz "Explainable Model Routing for Agentic Workflows" (arXiv:2604.03527, already covered in 2026-06-10-06 #2) — Lynkr dev.to post re-shared the framework this week, but the paper is the same as 2026-06-10-06 #2 and the follow-up is identical
- Lynkr v1.7-A P6 + Prism task-type routing + 4×3 grid (dev.to/rikuq, 2026-06-10) — same model-routing category as the Switchcraft + Topaz + PADISO + BizTechWeekly pieces; the Lynkr Prism v1.7-A P6 calibration is a vendor-product update, no new NEXUS code path beyond the already-captured Topaz + Switchcraft substrate
- DeLM (arXiv:2606.10662, Mao & Mirhoseini, Jun 9, 2026) shared-context +10.5pp SWE-bench (ruflo issue #2343, 2026-06-10) — same multi-agent context-shared category as the already-captured digital-twin / context-engineering pieces; the DeLM architecture is a research result, no new NEXUS code path
- CEPAR / PAS Los Alamos / Uncertainty Architecture neuro-symbolic verification / Hallucination Reduction layered framework / Retrieval Architecture for Agents / Building an LLM Evaluation Harness — same hallucination-detection / RAG-quality category, no thermodynamic/BEC/EPR paper this window
- mcp-odoo v0.4.0 multi-instance support (GitHub, 2026-06-10) — same MCP-server rollout category as the Woodstock MCP / Google GCS MCP / Tencent A2A / LangChain ADK / Arcade MCP runtime / Microsoft Agent Framework items in 2026-06-09-12 / 2026-06-10-06 / 2026-06-10-12 out-of-scope; the mcp-odoo v0.4.0 multi-instance work is a domain-vertical MCP server
- mcp-registry v0.2.0 + @incultnitollc/mcpr install + mcp-probe + mcp-vouch (dev.to/incultnitollc, 2026-06-10) — same MCP-registry / install-tool category; the mcpr install command is a developer-experience addition, the underlying `mcp-vouch` OWASP MCP Top 10 scoring is a useful reference but already captured in 2026-06-10-06 #1/#3 (CASCADE) and 2026-06-10-18 #3 (MCPGuard)
- OriginTrail DKG v10.0.0-rc.17 (2026-06-10) — same decentralised-knowledge-graph category as the Weaviate Engram / OSF-x402 / lakeFS pieces; the DKG per-KA Chorus memory is a vendor product addition
- AWS Neuron Agentic Development for Trainium (AWS Machine Learning Blog, 2026-06-10) — same AI-coding-tools / kernel-development category as the JFrog + Anthropic Claude Code plugin; the Neuron NKI agent skills are AWS-specific
- PTC / Sierra + Amadeus / Aurelian Callie / Close Chloe / Atomicwork Governed AI / Boomi Agentstudio + Snowflake Cortex / agnt8x / Woodstock MCP / Weaviate Engram / Taskade / Macroscope — same vendor agentic-AI-workforce category, no NEXUS code path
- ION Storage / Quantinuum / Microsoft Majorana 1 / IBM Heron R2 / Google Willow — quantum computing hardware, no LLM-detection or NEXUS code path
- Okta CIC + Auth0 / Microsoft Entra ID for Agents / CyberArk + AWS Bedrock / HashiCorp Vault + Agent Identity — already covered in 2026-06-09-12 / 2026-06-10-12 out-of-scope
- LiteLLM CVE-2026-42271 + CVE-2026-48710 — already covered in 2026-06-09-12 #1
- Microsoft re-compromise / Miasma worm / Hades Campaign / Proofpoint UNK_DeadDrop — already covered in 2026-06-08-18 / 2026-06-09-12 out-of-scope
- Iran-Israel / Trump administration / NBA Finals / NHL Stanley Cup / sports / politics / pharma / agriculture / energy / automotive / Iran missile strike / Trump executive order / sports — out of scope per standard scout filter
- Quantum error correction 11-800x improvement on ion-trap QCCD (Nature 2026-06-10) — physics, not LLM-detection
- Active inference driving collision avoidance (Nature Communications, 2026-06-10) — computational neuroscience, not LLM
- False data injection attacks in multi-area power systems (Nature Communications, 2026-06-10) — power systems, not LLM
- Carbon nanotube edge computing circuits (Nature Communications, 2026-06-10) — hardware, not NEXUS code
- JUNO neutrino detector initial findings (AP News / Phys.org, 2026-06-10) — physics, not NEXUS code
- Anomaly detection on multimodal LLM for tensegrity (Nature Microsystems & Nanoengineering, 2026-06-10) — engineering / structural-health-monitoring, not NEXUS code
- Soliton solutions of stochastic nonlinear Schrödinger equation (Scientific Reports, 2026-06-10) — physics, not LLM
- Spectral entropy EMI suppression in inverters (MDPI Entropy, 2026-06-10) — power electronics, not LLM
- Quantum-inspired differential equation solvers (MDPI Mathematics, 2026-06-10) — numerical PDEs, not LLM
- Cryo-EM structures of Drosophila pheromone receptors (Nature, 2026-06-10) — biology, not LLM
- AI digital twins for bilingual aphasia rehabilitation (News-Medical, 2026-06-09) — clinical, not NEXUS code
- Sage Energy Partners Uinta Basin PLC hardening (oilandgas360, 2026-06-10) — industrial, not NEXUS code
- GENISOM AI / ICRA 2026 / embodied intelligence (markets.businessinsider, 2026-06-10) — robotics, not NEXUS code
- Verizon Outage Bakersfield / Fitness trackers / Trump fund / Bill Gates Epstein interview / sports / politics / Iran-Israel / Trump administration / Reuters/Ipsos AI-job-loss poll / etc. — out of scope per standard scout filter
- Trump Section 702 FISA extension (Politico, 2026-06-10) — US surveillance law, not NEXUS code
- Trade Compliance Records / Trump executive order on customs enforcement (markets.businessinsider, 2026-06-05) — trade compliance, not NEXUS code
- Chase Sapphire Preferred refresh (Chase media, 2026-06-10) — credit card, not NEXUS code
- 700Credit $17.5M data breach settlement (Bloomberg Law, 2026-06-04) — credit reporting data breach, not NEXUS code
- OpenAI confidential IPO (CNBC, 2026-06-09) + Anthropic confidential IPO (Forbes Africa, 2026-06-10) — finance, not NEXUS code
- SecurityWeek CISO Forum Mid-Year Review (2026-06-10) — security industry event, no new NEXUS code path
- EPA / Zeldin / data center regulation (Politico, 2026-06-10) — policy, not NEXUS code
- China ghost particle JUNO detector — physics, not NEXUS code
- AI digital twins for bilingual aphasia rehabilitation — clinical, not NEXUS code
- Bay Area sourdough / Woodstock / yacht rock / Bitcoin / NBA / NHL — out of scope
