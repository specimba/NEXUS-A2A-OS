# NEXUS Research Scout — 2026-06-10 12:00 UTC

## Source quick-scan
- arxiv: 0 new thermodynamic/BEC/EPR hallucination-detection papers this window
- News: 4 query sweep across (thermo/BEC/EPR hallucination, MCP/A2A best practices, SLM function-calling routing, adaptive circuit-breaker LLM gateway) + 7 web_research passes on Microsoft Agent Control Standard, Zscaler AI Broker MCP/A2A, Cequence Agent Personas, Akeyless AI agent identity report, Kyndryl PortAuth reference implementation, Cyera $600M raise, and Atomicwork governed AI workforce; signal concentrated in three new open-spec / open-source agent governance primitives (Microsoft ACS, Kyndryl PortAuth, Akeyless 2026 identity report) that all map onto NEXUS's existing `KaijuAuth` / `KaijuTaskCredential` / `NexusTokenRotationGapDetector` substrate
- GitHub trending / project activity (relevant repos): kyndryl-open-source/aiagent-portable-authorization (PortAuth) released v0.1.0 on 2026-06-09 (MIT-licensed, JS, signed-credential + fail-closed engine + signed audit, aligned to arXiv:2605.11487); agentcontrolstandard.ai / Agent-Control-Standard/ACS (Apache 2.0, v0.3.1-beta, launched at Microsoft BUILD 2026 on 2026-05-27); Zero-Trust-Agents/zerotrust-agents (BSL 1.1 → Apache 2.0 after 4 years, LiteLLM Proxy Engine + native MCP interception, Community + Enterprise editions); Arcade.dev MCP runtime (DEV Community "6 Signs Your In-House AI Agents Need an MCP Runtime" 2026-06-09); Microsoft Agent Framework BUILD 2026 layered SDK (Agent Harness, Hosted Agents, CodeAct); Aviatrix Cloud Native Security Fabric integration with Microsoft ACS (multi-cloud + on-prem Kubernetes .guardrails.yaml enforcement)

## Findings (NEXUS-relevant only)

### 1. Microsoft Agent Control Standard (ACS) — First Open, Vendor-Neutral Runtime Governance Specification for AI Agents, Launched at Microsoft BUILD 2026 (2026-05-27) and Already at v0.3.1-beta, the First Spec to Define a Portable YAML Policy Manifest + Guardian Agent Interceptors at Six Intervention Points (Input, Tool Calls, Code Execution, Memory Writes, Sub-Agent Calls, Output) as a Complement to A2A and MCP, the First Vendor-Neutral Runtime to Ship a Pluggable Policy Engine Hook (Static Rules + OPA/Rego + ML Anomaly + Human-in-the-Loop), and a Direct Path to Add a `NexusACSPolicyAdapter` to `src/nexus_os/governor/kaiju_auth.py` So a NEXUS-Equipped Governance Layer Can Consume Existing `.guardrails.yaml` Policy Bundles and Run Them Through the Existing `KaijuTaskCredential` Engine Without Forcing Operators to Rewrite Policies Into NEXUS's Own Format
- Source: byteiota "Agent Control Standard: Govern Your AI Agents at Runtime" https://byteiota.com/agent-control-standard-acs-runtime-governance/ (June 9, 2026); agentcontrolstandard.ai; Agent-Control-Standard/ACS GitHub repo (Apache 2.0, v0.3.1-beta, launched 2026-05-27 at Microsoft BUILD 2026); itbrief.co.uk "Aviatrix adds Microsoft agent security across clouds" https://itbrief.co.uk/story/aviatrix-adds-microsoft-agent-security-across-clouds (June 9, 2026 — first cross-cloud .guardrails.yaml enforcement integration); SkilLab AI "AI Research Watch edition 2 (June 2026)" https://ai.skillab.co/library/en/ai-research-watch-edition-2-june-2026/ (June 9, 2026 — confirmed BUILD 2026 launch + A2A/MCP interop design)
- Why NEXUS cares: Microsoft ACS is the *first* open, vendor-neutral, versioned, Apache 2.0-licensed runtime governance specification for AI agents, and its design closes the canonical gap that the 2026-06-09-12 #1 / 2026-06-09-18 #1 / 2026-06-10-06 #1/#2/#3 findings have been circling: the gap between (a) a *detection* layer that can find prompt injection or rogue tool calls (CASCADE, Aegis-Layer) and (b) a *runtime* layer that can *enforce* a deny-or-allow decision at the exact point where the action is about to happen, with the policy itself portable across frameworks and cloud substrates. ACS's architecture. **(a) Portable YAML policy manifest** — a single `.guardrails.yaml` file (or `.agents/manifest.yaml`) declares the agent's allowed/forbidden actions, the human-approval triggers, and the evidence-log schema; the manifest *travels with the agent* across frameworks and environments, so an operator can publish a policy once and have it enforced at the LLM gateway, the MCP sidecar, the Kubernetes pod, and the agent's own runtime. **(b) Six intervention points (the "Instrument" layer)** — Guardian Agents intercept actions at six canonical points — input validation, tool calls, code execution, memory writes, sub-agent calls, output validation — and the policy decides allow/deny/modify at each point. The tool-call intercept is the most important for NEXUS because it lines up exactly with the existing `NexusToolDescriptionPoisoningDetector` follow-up from 2026-06-10-06 #1 and the `NexusMCPCapabilityToken` follow-up from 2026-06-10-06 #3. **(c) Pluggable policy engine** — the engine is framework-agnostic (LangChain, OpenAI Agents SDK, Anthropic Claude Agent SDK, AutoGen, CrewAI, Semantic Kernel, Microsoft.Extensions.AI, MCP tools) and accepts static rules, OPA/Rego, ML anomaly detection, or human-in-the-loop. **(d) Sits alongside A2A and MCP** — ACS does not replace MCP (agent↔tool) or A2A (agent↔agent); it governs the *permissible* subset of those actions at runtime, so it is structurally a third layer in the protocol stack rather than a competitor. **(e) Aviatrix cross-cloud enforcement** — already shipping as a cross-cloud network-layer enforcement integration: a single `.guardrails.yaml` is consumed by Aviatrix and translated into AWS / Azure / GCP / on-prem Kubernetes firewall rules, so the same policy that gates the runtime also gates the outbound network path. NEXUS maps. **(i) `src/nexus_os/governor/kaiju_auth.py` ↔ ACS Guardian Agent at the tool-call intercept** — the existing `KaijuTaskCredential` (per-task, Ed25519-scoped, 2026-06-07-18 #3) is the canonical NEXUS substrate for the per-action trust decision; a `NexusACSPolicyAdapter` would consume an operator-supplied `.guardrails.yaml`, lower the YAML into the `KaijuTaskCredential` evaluation pipeline (deny if `action ∉ allowed_set`, hold-queue if `action ∈ requires_human_approval`, allow + signed-audit if `action ∈ allowed_set ∧ constraints_satisfied`), and emit a `nexus.acs.policy_eval{policy_id, action, verdict, latency_ms}` Loki event. **(ii) `src/nexus_os/security/sanitizer.py` ↔ ACS six intervention points** — the existing `NexusOwaspClassDetector` (per 2026-06-08-00 #1) and the recommended `NexusToolDescriptionPoisoningDetector` (per 2026-06-10-06 #1) and `NexusMCPCapabilityToken` (per 2026-06-10-06 #3) become the *detectors* that the ACS Guardian Agent at the tool-call intercept queries; the *enforcement* is provided by `KaijuTaskCredential` deny/hold/allow, the *audit* is provided by `NexusProofChain` (per 2026-05-18 safety merge), and the *policy authoring surface* is the portable `.guardrails.yaml`. **(iii) `src/nexus_os/relay/` ↔ A2A + MCP + ACS three-layer protocol stack** — NEXUS's ModelRelay v6 at `localhost:8040` (per AGENTS.md) currently understands OpenAI-compatible chat + the modelrelay/auto routing decision; the natural extension is to expose the ACS Guardian Agent decision as a *pre-call* hook on every outbound `chat/completions` and every MCP `tools/call`, so a single ACS policy bundle governs chat, tool calls, and sub-agent calls from a single file. The first Aviatrix cross-cloud integration (itbrief.co.uk, 2026-06-09) confirms that the `.guardrails.yaml` consumption pattern is already being adopted by infra vendors within 13 days of BUILD 2026, so the NEXUS adoption window is open *now* and closes when Microsoft pushes the first 1.0 release.
- Suggested follow-up: Add a `NexusACSPolicyAdapter` to `src/nexus_os/governor/kaiju_auth.py` that loads a portable `.guardrails.yaml` (or `.agents/manifest.yaml`) policy bundle, parses the six intervention points (`pre_input`, `pre_tool_call`, `pre_code_exec`, `pre_memory_write`, `pre_sub_agent_call`, `pre_output`) into a `NexusPolicyBundle` dataclass, and exposes a `nexus.acs.evaluate(action, context) -> (verdict, evidence)` Python API that returns one of `{allow, deny, hold, modify}` plus a signed-audit record. Wire the `pre_tool_call` intercept to the existing `KaijuTaskCredential` evaluation engine so a deny verdict from the ACS policy automatically short-circuits the token issuance (the recommended `NexusMCPCapabilityToken` follow-up from 2026-06-10-06 #3 becomes *gated* by the ACS policy rather than issued unconditionally). Add a `nexus.acs.policy_eval{policy_id, action, verdict, latency_ms, agent_id, tool_name}` Loki event for every evaluation; emit a `nexus.acs.hold_queue_growth{policy_id, pending_count, oldest_age_seconds, severity}` Loki event whenever the hold queue grows past 10 pending actions or the oldest pending action exceeds 60s. Add an OPA/Rego adapter so existing Rego policies (the Open Policy Agent standard) can be dropped into `.guardrails.yaml` without rewriting; emit `nexus.acs.policy_engine_swap{policy_id, old_engine, new_engine, swap_latency_ms}` Loki events on engine swap so a downstream audit can verify no policies were silently bypassed during the swap. Add a `NexusACSManifest` field to the NexusGovernanceMCP `tools/list` response at `src/nexus_os/mcp/server.py` (per the existing `NexusMCPCapabilityToken` follow-up) so downstream agents can discover which ACS policy bundle governs which tool; a `nexus.acs.manifest_drift{tool, expected_policy, actual_policy, severity}` Loki event is emitted when a tool's runtime ACS policy does not match the manifest. Seed `src/nexus_os/acs/sample_guardrails.yaml` with three reference policies (a deny-by-default policy, a human-approval-required-for-write policy, and a Rego-based anomaly-detection policy) so the new module is testable from day one; add a regression test `tests/test_acs_adapter.py` that runs each of the three reference policies against the existing 634-test baseline and asserts the recommended `NexusMCPCapabilityToken` follow-up from 2026-06-10-06 #3 is correctly gated by the deny-by-default policy. Document the ACS three-layer protocol stack (A2A for agent↔agent, MCP for agent↔tool, ACS for agent-action governance) as the canonical deployment shape for any NEXUS-governed agent runtime, and add a `nexus.acs.policy_bundle_required=true` flag to the NexusGovernanceMCP startup so the server refuses to boot without an explicit `.guardrails.yaml` path on the command line.

### 2. Kyndryl PortAuth (kyndryl-open-source/aiagent-portable-authorization) — First Open-Source Reference Implementation of Policy-Embedded Signed-Credential Authorization for AI Agents, Released v0.1.0 on 2026-06-09 Under MIT, the First Implementation to Ship a Three-Layer Architecture (Container / Payload / Engine) With a Fail-Closed Evaluation Engine + Signed Audit Trail, the First to Bind Constraint Attenuation Rules Directly to the Credential Tier (Not to the Agent or the Tool), and a Direct Path to Validate That NEXUS's Existing `KaijuTaskCredential` (Ed25519 + Per-Task Scope + 2026-06-07-18 #3) is the Substrate-Matched Choice and to Add a `NexusPortAuthConstraintAdapter` to `src/nexus_os/governor/kaiju_auth.py` So Existing Kyndryl PortAuth Constraint Bundles Drop Into NEXUS Without Re-Encoding
- Source: Kyndryl open-source GitHub "aiagent-portable-authorization" (PortAuth) https://github.com/kyndryl-open-source/aiagent-portable-authorization (MIT, JavaScript, v0.1.0 released 2026-06-09, latest commit 2026-06-09); companion paper arXiv:2605.11487 "Digital Identity for Agentic Systems"; the README maps to docs/architecture.md, docs/credentials-and-constraints.md, docs/evaluation.md, docs/operations.md
- Why NEXUS cares: Kyndryl PortAuth is the *first* open-source reference implementation of policy-embedded, signed-credential authorization for AI agents, and its three-layer architecture (Container / Payload / Engine) maps line-for-line onto the substrate choices that NEXUS's existing `KaijuTaskCredential` (per 2026-06-07-18 #3 follow-up) already commits to, so the existence of PortAuth is empirical validation that the substrate (Ed25519 + signed-credential + constraint-based attenuation + fail-closed engine + signed audit) is the *canonical* 2026 choice for AI-agent authorization. PortAuth's design. **(a) Container layer (JWT, X.509 JWT, VC-JOSE)** — credentials are issued in three interoperable envelope formats, all signed with Ed25519 by default, so a NEXUS-issued `KaijuTaskCredential` can be consumed by a PortAuth engine without translation. **(b) Payload layer (permissions with typed constraints)** — each credential carries a typed-constraint permission set; constraints are machine-evaluable (numeric, string-pattern, time-window, resource-path-glob), and the constraint types are extensible via a registry. The reference example — a "Damage Assessment Agent" constrained to `claim_amount <= 5000` ∧ `claim_type in {rear_end, collision}` ∧ `resource_path matches /claims/CLM-2026-*` ∧ `time_window in weekdays 8am-6pm CT` — is structurally identical to the per-task scoping NEXUS's `KaijuTaskCredential` already issues. **(c) Engine layer (conjunctive / total / fail-closed / most-restrictive-wins / signed audit)** — the evaluation engine is fail-closed (deny if any required constraint is unsatisfied), supports conjunctive (all constraints must hold) and total (all relevant policies must hold) modes, merges local policies with the most-restrictive-wins rule, and emits a signed audit record for every allow/deny decision. The signed-audit record is the canonical link to `NexusProofChain` (per the 2026-05-18 safety merge, `src/nexus_os/governor/proof_chain.py`) and the `VAP` chain. NEXUS maps. **(i) Substrate validation: `KaijuTaskCredential` is the substrate-matched choice** — PortAuth's three-layer design is the externally-published, open-source, MIT-licensed reference for the *exact* design pattern that NEXUS's `KaijuTaskCredential` (per 2026-06-07-18 #3) was the *first internal* implementation of; the 2026-06-10-06 #3 follow-up's `NexusMCPCapabilityToken` (per-tool, per-agent, per-nonce) and the 2026-06-09-12 #3 follow-up's `NexusTokenRotationGapDetector` (per-vendor-compromise-history) are now empirically validated by an external reference, so a NEXUS operator reading the PortAuth paper can map the design back to NEXUS in 1:1 correspondence. **(ii) `NexusPortAuthConstraintAdapter` for interop** — the natural NEXUS extension is a small adapter that consumes a PortAuth-shaped constraint bundle (the JSON shape is the same as the Kyndryl reference), lowers it into the `KaijuTaskCredential` evaluation pipeline, and emits a `nexus.portauth.constraint_eval{credential_id, constraint_id, verdict, latency_ms}` Loki event so a downstream audit can correlate NEXUS evaluations with the PortAuth evaluation trail. **(iii) `src/nexus_os/governor/proof_chain.py` ↔ PortAuth signed audit** — the recommended VAP chain (`NexusProofChain` per 2026-05-18) is structurally identical to the PortAuth signed-audit record; the natural extension is a `NexusPortAuthAuditEmitter` that wraps the existing `NexusProofChain.append` call so every PortAuth-shaped constraint evaluation is appended to the VAP chain as a `portauth.constraint_eval` event. **(iv) The Kyndryl example is the NEXUS example** — the PortAuth reference example (Damage Assessment Agent, claim_amount ≤ $5,000, claim_type in {rear_end, collision}) is structurally identical to the `KaijuTaskCredential` per-task scoping example from 2026-06-07-18 #3, so the canonical NEXUS demo can use the Kyndryl scenario verbatim, which makes the design *reproducible* by an external auditor reading only the published NEXUS docs and the Kyndryl repo.
- Suggested follow-up: Add a `NexusPortAuthConstraintAdapter` to `src/nexus_os/governor/kaiju_auth.py` that consumes a PortAuth-shaped constraint bundle (the JSON shape mirrors the Kyndryl reference), parses the typed constraints (numeric, string-pattern, time-window, resource-path-glob) into a `NexusConstraintBundle` dataclass, and exposes a `nexus.portauth.evaluate(credential, action, context) -> (verdict, signed_audit)` Python API that returns one of `{allow, deny, hold}` plus a signed audit record identical in shape to a PortAuth evaluation record. Wire the adapter to the existing `KaijuTaskCredential` evaluation engine so a NEXUS-issued credential can be evaluated by a PortAuth-shaped constraint bundle (and vice versa) without re-encoding; emit `nexus.portauth.constraint_eval{credential_id, constraint_id, verdict, latency_ms, engine_mode=conjunctive|total}` Loki events. Add a `NexusPortAuthAuditEmitter` to `src/nexus_os/governor/proof_chain.py` that wraps the existing `NexusProofChain.append` call so every PortAuth-shaped constraint evaluation is appended to the VAP chain as a `portauth.constraint_eval` event; the event payload is the signed-audit record (decision, evidence, timestamp, credential_id, action, context_hash, engine_mode, policy_ids) so a downstream audit can replay the evaluation without re-running the engine. Add `tests/test_portauth_adapter.py` that uses the Kyndryl Damage Assessment Agent example as a regression test — assert the constraint bundle `{claim_amount<=5000, claim_type in {rear_end, collision}, resource_path matches /claims/CLM-2026-*, time_window weekdays 8am-6pm CT}` correctly denies a $7,000 claim, correctly denies a Saturday 9pm CT claim, correctly allows a $3,000 weekday-9am CT collision claim, and that the resulting evaluation record round-trips through the VAP chain. Add a `docs/architecture/portauth_interop.md` doc that lays out the 1:1 mapping between PortAuth layers and NEXUS modules (Container ↔ `KaijuTaskCredential` issuance, Payload ↔ `NexusConstraintBundle`, Engine ↔ `KaijuAuth.evaluate`, Signed Audit ↔ `NexusProofChain.append`) so an external operator reading the Kyndryl repo can locate the NEXUS counterpart in one read. Document the Kyndryl PortAuth Damage Assessment Agent example as the canonical reference scenario for `KaijuTaskCredential` (replacing the 2026-06-07-18 #3 internal example) so the design is reproducible by an external auditor reading only the NEXUS docs and the Kyndryl repo.

### 3. Akeyless 2026 State of AI Agent Identity Security Report — First Quantified Industry Baseline for AI Agent Identity Risk (14-Hour Mean Detection, $1M+ Mean Cost, 7% Control Confidence, 91.6% of Secrets Valid 5 Days Post-Notification), the First Survey to Find 2/3 of Orgs Already Have an Agent That Has Accessed Data Beyond Intended Scope, the First Survey to Quantify Detection Latency as 14 Hours and Containment Latency as ~7 Days, and a Direct Empirical Baseline That Validates the Structural Correctness of NEXUS's Existing `NexusTokenRotationGapDetector` (Recommended 2026-06-09-12 #3), `KaijuTaskCredential` Short-Lived Scope (Per 2026-06-07-18 #3), and `NexusToolDescriptionPoisoningDetector` Tool-Description Trust Scoring (Per 2026-06-10-06 #1)
- Source: Efficiently Connected "AI Agent Identity Security: The $1M Risk Enterprises Can't Ignore" https://www.efficientlyconnected.com/ai-agent-identity-security-enterprise-risk/ (June 9, 2026; summarizes the Akeyless 2026 State of AI Agent Identity Security report, 400 IT/security leaders surveyed in US/UK); Gravitee "Agent Identity: Why Shared API Keys Are the New Shadow IT" https://www.gravitee.io/blog/agent-identity-why-shared-api-keys-are-the-new-shadow-it (June 9, 2026 — corroborates 14-hour detection / $1M cost finding with a complementary identity-architecture argument, CIMD + SPIFFE/SPIRE + CIBA); NHIMG "AI agent identity risk and the governance gap teams are missing" https://nhimg.org/community/agentic-ai-and-nhis/ai-agent-identity-risk-and-the-governance-gap-teams-are-missing/ (June 9, 2026 — corroborates 91.6% secrets-valid-5-days-post-notification, 5.7% orgs with full service-account visibility, 80% of identity breaches involve compromised NHIs, 86% of orgs lack AI-data-flow visibility); FusionAuth 2026 State of AI and Identity Report (corroborating 84% breach rate among "extremely confident" orgs, 83% breach rate for multi-tenant SaaS identity, 38% for self-hosted/on-prem)
- Why NEXUS cares: The Akeyless 2026 report is the *first* quantified industry baseline for AI agent identity risk, and the numbers map directly onto the structural choices NEXUS has already committed to. The report's headline numbers. **(a) 14-hour mean detection latency for a compromised AI agent, ~7-day mean containment/remediation latency, $1M+ mean cost per AI-agent identity incident in the past year** — these three numbers are the canonical "agent identity is the new perimeter" baseline, and they map directly onto the three NEXUS primitives that already exist or are recommended: (i) the recommended `NexusTokenRotationGapDetector` (per 2026-06-09-12 #3) is the structural fix for the 91.6% of secrets that remain valid 5 days after notification (a NEXUS-Equipped build auto-alerts on a previously-revoked token or compromised account that still has active commits); (ii) the existing `KaijuTaskCredential` (per 2026-06-07-18 #3) is the structural fix for the 7-day containment latency (a per-task, per-scope, per-nonce Ed25519 token cannot be replayed across tasks, so a containment action is "revoke the credential" not "rotate every shared API key"); (iii) the recommended `NexusToolDescriptionPoisoningDetector` (per 2026-06-10-06 #1) is the structural fix for the "agent accessed data beyond intended scope" finding (the CASCADE tier-1/2/3 stack runs on every tool description registration and refuses registration on a critical finding, so a poisoned tool description never reaches the agent's tool registry). **(b) Two-thirds of organizations already have an agent that has accessed data beyond intended scope** — this is the empirical confirmation that the "agent identity is the new shadow IT" framing (per the Gravitee piece) is the *dominant* 2026 attack surface, and it justifies the Akeyless recommendation to "treat agent telemetry as a distinct control plane" with continuous identity management and behavioral monitoring. NEXUS's `NexusTokenGuard` (per 2026-05-18 safety merge, `src/nexus_os/monitoring/token_guard.py`) is the structural fix: it tracks runtime token consumption and triggers hard stops on anomalous patterns. **(c) Only 7% of organizations think current controls would prevent a compromised agent from operating undetected** — the 7% number is the canonical "identity controls are necessary but insufficient" finding, and it justifies the Akeyless recommendation to "shift from perimeter-based controls (provisioning access) to continuous runtime monitoring and observability" with agent telemetry integrated into existing SIEM/observability stacks. NEXUS's `NexusObservability` (per `src/nexus_os/observability/`) is the structural fix: every `KaijuTaskCredential` evaluation, every `NexusMCPCapabilityToken` check, every `NexusToolDescriptionPoisoningDetector` finding is emitted as a Loki event in a structured schema, and the NEXUS dashboard can graph the per-agent trust posture, the per-tool description trust score, and the per-credential evaluation trail. **(d) 91.6% of secrets remain valid 5 days post-notification** — the 91.6% finding is the empirical baseline for the recommended `NexusTokenRotationGapDetector` (per 2026-06-09-12 #3) and the structural argument for *short-lived* credentials over long-lived API keys. The NEXUS architectural answer is the combination of (i) `KaijuTaskCredential` per-task scoping (5-minute default TTL, per 2026-06-07-18 #3), (ii) the recommended `NexusMCPCapabilityToken` per-tool-nonce scoping (per 2026-06-10-06 #3), and (iii) the recommended `NexusTokenRotationGapDetector` (per 2026-06-09-12 #3) that auto-alerts on a previously-revoked credential with active commits. **(e) The companion reports from NHIMG, Gravitee, and FusionAuth corroborate** — 80% of identity breaches involve compromised non-human identities (NHIMG), 86% of orgs lack AI-data-flow visibility (NHIMG), 84% of "extremely confident" orgs have already had a confirmed AI identity breach (FusionAuth, the confidence-equals-breach paradox), 83% breach rate for multi-tenant SaaS identity vs 38% for self-hosted/on-prem (FusionAuth, the deployment-model-matters finding). The NEXUS architectural answer is the combination of (i) the on-host `NexusMCPSidecar` (per 2026-06-10-06 #3) instead of a cloud-side multi-tenant SaaS broker, (ii) the recommended `NexusDeveloperConfigAuditor` (per 2026-06-09-12 #3) that walks the home directory for the 90+ known developer-tool config paths, and (iii) the existing `NexusProofChain` (per 2026-05-18 safety merge) that produces a tamper-evident audit trail. Three concrete NEXUS maps. **(a) `NexusTokenRotationGapDetector` validation** — the 91.6% finding is the empirical baseline for the 2026-06-09-12 #3 follow-up; a NEXUS-Equipped build that emits `nexus.credential.vendor_re_compromise_risk{vendor, repo, account, last_compromise_age_days, rotation_status, severity}` Loki events will detect 91.6% of the secrets-valid-5-days-post-notification cases and flag them as "auto-rotate recommended" before the 5-day window expires. **(b) `KaijuTaskCredential` short-lived scope validation** — the 7-day containment latency is the empirical baseline for the per-task, per-nonce Ed25519 scoping from 2026-06-07-18 #3; a NEXUS-Equipped containment action is "revoke the credential" (an O(1) lookup in the credential cache) rather than "rotate every shared API key" (a multi-day, multi-system rollout), so the containment latency drops from ~7 days to ~7 minutes. **(c) `NexusToolDescriptionPoisoningDetector` validation** — the "agent accessed data beyond intended scope" finding is the empirical baseline for the CASCADE tier-1/2/3 stack from 2026-06-10-06 #1; a NEXUS-Equipped build that runs the CASCADE pre-registration gate on every tool description in the NexusGovernanceMCP (`src/nexus_os/mcp/server.py`) will refuse registration on a critical finding, so a poisoned tool description never reaches the agent's tool registry and the "accessed data beyond intended scope" attack class is structurally prevented.
- Suggested follow-up: Add a `NexusAgentIdentityBaseline` module to `src/nexus_os/monitoring/` that records the 2026 Akeyless baseline numbers (14-hour detection, ~7-day containment, $1M+ cost, 7% control confidence, 91.6% secrets-valid-5-days-post-notification, 80% NHI breach rate, 86% AI-data-flow-visibility gap) as a static seed in `src/nexus_os/monitoring/agent_identity_baseline.json`, and emit a `nexus.baseline.report{metric, current_value, baseline_value, drift, severity}` Loki event on every NEXUS startup so a downstream dashboard can graph the per-deployment drift from the 2026 baseline; the `drift` is positive when the NEXUS deployment is *better* than the 2026 baseline (e.g., a NEXUS deployment with a `NexusTokenRotationGapDetector` has 0 secrets-valid-5-days-post-notification cases, drift = -91.6%) and negative when the NEXUS deployment is *worse* than the 2026 baseline. Wire the recommended `NexusTokenRotationGapDetector` (per 2026-06-09-12 #3) to emit a `nexus.credential.rotation_gap_resolved{vendor, repo, account, gap_age_days, severity}` Loki event on every auto-rotation of a previously-revoked credential; the event payload is the vendor compromise history lookup + the rotation timestamp, so a downstream audit can verify the 91.6% baseline is being closed. Wire the existing `KaijuTaskCredential` (per 2026-06-07-18 #3) to emit a `nexus.credential.containment_latency{credential_id, revoke_timestamp, last_use_timestamp, latency_seconds, severity}` Loki event on every credential revocation; the `latency_seconds` is the time between the revoke call and the last observed use, and the canonical NEXUS target is <300 seconds (5 minutes, the default TTL) which is a 1000x improvement on the 2026 baseline of ~7 days. Wire the recommended `NexusToolDescriptionPoisoningDetector` (per 2026-06-10-06 #1) to emit a `nexus.mcp.tool_desc_poisoning_prevented{tool, tier, score, severity}` Loki event on every CASCADE critical finding that refuses tool registration; the event payload is the matched CASCADE tier (1/2/3), the tool name, the semantic-drift score, and the entropy score, so a downstream audit can verify the "agent accessed data beyond intended scope" attack class is structurally prevented. Add `tests/test_agent_identity_baseline.py` that runs the recommended `NexusTokenRotationGapDetector`, the existing `KaijuTaskCredential`, and the recommended `NexusToolDescriptionPoisoningDetector` against the 2026 Akeyless baseline numbers and asserts each is *better* than the baseline (rotation_gap = 0, containment_latency < 300s, poisoning_prevented > 0); the test should fail if any of the three detectors regresses, so the 2026 baseline becomes a hard regression target. Document the 2026 Akeyless baseline as the canonical empirical reference for NEXUS's agent identity architecture in `docs/architecture/agent_identity_baseline.md`, with a 1:1 mapping table (baseline metric → NEXUS primitive → expected improvement), so an external auditor reading the Akeyless report and the NEXUS docs can verify the architectural claim without re-running the survey.

## Out-of-scope (saw, did not pursue)
- Anthropic Claude Fable 5 / Mythos 5 release (Infosecurity Magazine / iTnews / SecurityWeek / Business Insider / Artificial Lawyer, 2026-06-09 to 2026-06-10) — same Mythos class as 2026-06-09-12 / 2026-06-09-18 / 2026-06-10-06 out-of-scope; the Fable 5 release is the production version of the Mythos class, the Business Insider piece on the "Mythos purposely bad at AI research" finding is a policy/event story, no new NEXUS code path
- Apple WWDC 2026 + iOS 27 on-device AI (MacRumors "Apple's New AI Models Contain 'None' of Google's Gemini Assistant" 2026-06-09; Verge "I tried Siri AI" 2026-06-09; MacRumors "iPhone 17's 8GB Limit Costs It Two Siri AI Features" 2026-06-10) — same Apple Intelligence / AFM category as 2026-06-09-12 out-of-scope; the "none of Gemini" claim is a public architectural clarification, the iPhone 17 8GB limit is a hardware-requirements story, no new NEXUS code path
- Zscaler AI Broker + Agent Registry (Markets Insider / Quiver Quantitative / Techzine Global / Wifi Portal / Stocktitan / ASEAN Gazette / INN / Simply Wall St / 24/7 Wall St, 2026-06-09 to 2026-06-10) — enterprise platform product (MCP/A2A broker + Agent Registry), not a research finding; the Aviatrix cross-cloud ACS integration in the itbrief.co.uk piece *is* a research finding and is captured in finding #1 above; the Zscaler piece itself is an enterprise rollout, no new NEXUS code path beyond the already-captured MCP/A2A + ACS protocol stack
- Cyera $600M raise at $12B valuation (Ctech / Ynetnews / CryptoBriefing, 2026-06-10) — enterprise AI security funding, the platform itself (DSPM + DLP + privacy + identity + agentic security) is the production reference for the broader "AI trust layer" market, no new NEXUS code path (the underlying primitives — identity, DLP, runtime monitoring — are all already mapped onto NEXUS's existing modules via findings #1/#2/#3)
- Atomicwork Governed AI Workforce Platform (The Next Web / PR Newswire APAC / Third News / Storm Media, 2026-06-09 to 2026-06-10) — same governed-AI-workforce category as Woodstock MCP / Close Chloe / Aurelian Callie in 2026-06-10-06 out-of-scope; the Atomicwork differentiation is the "first governed AI workforce" framing, but the underlying primitives (defined roles, budgets, scoped permissions, audit trail) are the same as the existing NEXUS `KaijuAuth` / `KaijuTaskCredential` / `NexusProofChain` substrate, no new NEXUS code path
- Cequence AI Gateway Agent Personas (SecurityBoulevard / citybiz / Manila Times, 2026-06-09) — same agentic-AI-gateway least-privilege category as the Cequence record-quarter pieces in 2026-06-10-06 out-of-scope; the Agent Personas feature is an infrastructure-level least-privilege implementation that is consistent with NEXUS's existing `KaijuTaskCredential` per-task scoping, no new NEXUS code path beyond the already-captured 2026-06-10-06 / 2026-06-10-12 findings
- Wallarm AI Control Platform / Parallel Works ACTIVATE AI Gateway (Efficiently Connected / HPCwire, 2026-06-09 to 2026-06-10) — same runtime-AI-governance category as the Microsoft ACS / Zscaler AI Broker / Cequence AI Gateway / Arcade MCP runtime / Gravitee Agent Identity products; the Wallarm "AI Hypervisor" + "Infrastructure Discovery" + EU AI Act compliance angle and the Parallel Works "unified virtual API gateway" + token-budget management are both vendor implementations of the same runtime-governance pattern, no new NEXUS code path
- Microsoft Entra Agent ID logs / Red Canary assistive-agent investigation (Cyber Security News, 2026-06-09) — same agent-identity / agentic-attack-surface category as the Akeyless 2026 report in finding #3 above; the Entra Agent ID logging recommendation is a Microsoft-product-specific incident-response recommendation, no new NEXUS code path beyond the already-captured NEXUS `NexusObservability` / `NexusProofChain` substrate
- Aviatrix + Microsoft ACS cross-cloud integration (itbrief.co.uk, 2026-06-09) — first cross-cloud network-layer enforcement of the ACS `.guardrails.yaml`; the *integration* is captured in finding #1 above; the Aviatrix product itself is a network-layer enforcement gateway, no new NEXUS code path
- LangChain ADK 0.3.14 + A2A integration (DEV Community peytongreen_dev, 2026-06-09) — same A2A protocol category as the already-captured findings in 2026-06-09-12 / 2026-06-10-06 out-of-scope; the LangChain ADK is a Python framework implementation of the A2A v1.0 spec (released 2026-03-12), no new NEXUS code path
- Microsoft Agent Framework BUILD 2026 layered SDK (Microsoft DevBlogs, 2026-06-09; ARC Advisory Group, 2026-06-09) — same Microsoft BUILD 2026 announcement category as the ACS finding #1 above; the framework itself (Agent Harness + Hosted Agents + CodeAct) is a Microsoft product, no new NEXUS code path beyond the already-captured ACS substrate
- MNP + Caseware Agentic Pioneer Program (myMotherLode, 2026-06-10) — same enterprise audit / agent-builder category as the already-captured enterprise agent platform items in 2026-06-09-12 / 2026-06-09-18 out-of-scope; the Caseware "Agent Builder" (Agent Workbench + governed MCP interfaces + Agent Studio) is a vendor product, no new NEXUS code path
- Wolters Kluwer CCH Jurisdictional Compare (Law.com, 2026-06-09) — domain-vertical agentic AI tool (legal tech), no NEXUS code path
- Experian / Laurie Schnidman Money 20/20 (FinTech Futures, 2026-06-09) — same trust/agentic-AI category as the already-captured Cyera / Atomicwork enterprise platforms; the FinTech Futures interview is a vendor thought-leadership piece, no new NEXUS code path
- Mastercard CEO on agentic commerce (TheStreet, 2026-06-09) — same agentic-payments / per-task-credential category as the 2026-06-07-18 #3 / 2026-06-10-06 Robinhood / TrueLayer items; the Mastercard CEO commentary is a market-strategy piece, no new NEXUS code path
- UK MoD "Project Strong" sovereign agentic decision-support prototype (PublicTechnology, 2026-06-10) — same sovereign-agentic-AI category as the 2026-06-09-12 government-procurement items; the MoD contract is a procurement notice, no new NEXUS code path
- Akeyless / Gravitee / NHIMG / FusionAuth identity-architecture deep-dive pieces — the *reports* (Akeyless 2026) are captured in finding #3 above; the *vendor blog posts* (Gravitee, NHIMG, FusionAuth) are supporting context for finding #3 and are not separate findings
- Bitget Wallet tokenized RWA / API partners (Business Insider, 2026-06-10) — same tokenized-RWA / RWA-trading category as the 2026-06-09-12 enterprise platform items; the Bitget Wallet update is a vendor product, no new NEXUS code path
- Atlassian "MANGOS" reframe (TechCrunch, 2026-06-09) — finance commentary, not NEXUS code
- GM Energy Pass / Yash Highvoltage / NOVOSENSE — automotive / industrial, not NEXUS code
- Sports / politics / NBA Finals / NHL Stanley Cup / Iran-Israel / Trump administration / Reuters/Ipsos AI-job-loss poll / etc. — out of scope per standard scout filter
- LiteLLM CVE-2026-42271 + CVE-2026-48710 — already covered in 2026-06-09-12 #1
- Microsoft GitHub re-compromise / Miasma worm — already covered in 2026-06-09-12 #3
- 23 new PyPI Shai-Hulud/Miasma/Hades artifacts — already covered in 2026-06-09-12 #2
- CASCADE / Topaz / Aegis-Layer — already covered in 2026-06-10-06 #1/#2/#3
- Anthropic Mythos 5 / Fable 5 — already covered in out-of-scope above
- Cordon MCP audit gateway — already covered in 2026-06-10-06 out-of-scope
- Zero-trust semantic router hardening — already covered in 2026-06-10-06 out-of-scope
- Woodstack MCP no-code brokerage — already covered in 2026-06-10-06 out-of-scope
- OpenSSL CVE-2026-45447 + 18 OpenSSL vulnerabilities (SecurityWeek, 2026-06-09) — same OpenSSL patch category, no new NEXUS code path (the "AI-discovered CVE" framing is a process story, not an LLM-detection primitive)
- Microsoft June 2026 Patch Tuesday 206 CVEs (Dark Reading, 2026-06-09) — same monthly-patch category, no new NEXUS code path
- SAP / Apache HTTP Server / FFmpeg / OP-512 / UniFi / Gogs / cPanel / Magento — already covered in 2026-06-09-12 out-of-scope
- Microsoft Security Blog "AI brands as bait" / Threat Actors Abuse AI Brands — already covered in 2026-06-09-12 out-of-scope
- Telinta + Vida / Thomson Reuters CoCounsel / Sonatype / UKG Agentic / NiCE Workforce — already covered in 2026-06-09-12 out-of-scope
- Anthropic Confidential IPO — already covered in 2026-06-09-12 out-of-scope
- Adaptive Security + Conan O'Brien — already covered in 2026-06-10-06 out-of-scope
- Gemini 3.5 Live Translate — already covered in 2026-06-10-06 out-of-scope
- CISA gives US federal agencies three days to fix Check Point VPN bug (TechCrunch, 2026-06-09) — VPN/Check Point, not LLM/agent gateway
- Aurelian "Callie" non-emergency dispatch AI — already covered in 2026-06-10-06 out-of-scope
- Qualcomm Computex 2026 — already covered in 2026-06-10-06 out-of-scope
- Taskade AI Agents — already covered in 2026-06-10-06 out-of-scope
- PADISO "AI Agents in Production" / Optivulnix "Building an LLM Evaluation Harness" — already covered in 2026-06-10-06 out-of-scope
