# NEXUS Full Audit — Findings Triage (workflow run wf_c2b89abd-634)

- **Audit run:** ~2026-06-29/30, `nexus-full-audit` workflow (`.claude/workflows/nexus-full-audit.js`): 12 subsystem code reviewers + 7 log/artifact miners + adversarial verification.
- **Raw journal:** Claude session `975cd0b2` workflow transcript (56 agent results). This document is the durable, tracked record.
- **Totals:** 105 findings (6 critical / 37 high / 57 medium / 5 low), 68 miner insights (16 P0 / 26 P1 / 22 P2 / 4 P3). 43 high/critical findings adversarially verified; 42 confirmed real.
- **Triage legend:** items below are OPEN unless annotated `[STATUS: ...]`. Update this file as items are fixed; cite the fixing commit.

> **Roadmap pointer (2026-07-02):** the open findings below are now sequenced for execution in `docs/plans/IMPROVEMENT_ROADMAP_2026-07-02.md` (Phase 1 = the four CRITICALs + fail-open network/auth HIGHs; Phases 2–4 absorb the intelligence/daemon/CLI HIGHs). This file stays the findings LEDGER — keep closing items here with commit evidence.

## Triage status (2026-07-02)

FIXED this session (cite commit before closing elsewhere):

- `ed75c704` — relay adapter enforcement: breaker per relay tier + skip when OPEN, RPM hard stop + paced backoff, context-window clamp/overflow failover, `_call_chat` no longer "ok" on malformed/empty 200 bodies. Covers findings at model_relay_adapter.py:137/164/230.
- `06cbd208` — wiki_pipeline: dead legacy dossier fallback, stale 5-min sync loop (now rebuilds index), CLI `--search` empty index, title-level GROSS/confidential screening. Covers findings at wiki_pipeline.py:265/135/86.
- `b97ef129` — grounding: ReliableGroundingStore (checksummed ledger, rebuildable index) committed with store re-export; GroundingEvent validation; unknown clearance now hard-fails (service.py:106 fail-open); public `authorized=True` ingest bypass removed (service.py:141); watch() dead reconcile gating removed (service.py:277).
- `76de6e12` — dashboard governance REST wrapper wired to canonical governor check_access, fail-closed to needs_review without a governor.
- `02baa182` + `88762480` — SECRETS scrub: all known key literals redacted from tracked files (LongCat x4, Baseten, InternAI, HF x2, NVIDIA hardcoded fallback removed with hard-fail + tests); `.z-ai-config` untracked; `{id_str}_brief.md` deleted.

FIXED on branch `codex/specimba/nexus-core-solidify` (2026-07-02, core-solidify session):

- `dc9aa507` — vault TRUST-channel gate ENFORCED: append_trust/append_governance now gate on new `writer_trust` (writer authority, hard-fail default 0 → denied), deliberately NOT on `trust_score` (the recorded value — TrustKernel must record low scores); append_episodic gates with the TASK/META legacy-trusted shim; "governance" added to VALID_LANES (DG-bridge TRUST records were silently remapped to "general"). Covers the "vault TRUST-channel ungated writes" finding at memory_channels.py:472/503.
- `99547494` — NEXUSCLAW /status API reported fabricated stats: avgTrust hardcoded 75.0 and agent counts read keys AgentPool.stats() never returns (always 0). Now real avg_trust/by_status. Also /intervene NameError (500 on every call) fixed. tests/api was hidden by `.git/info/exclude` (entry removed); first tracked tests added there.
- `9be0b72c` — duplicate `nexus_os/chimera_router_v2.py` (stale ancestor without relay-adapter wiring) is now a deprecation shim re-exporting the canonical `nexus_os/twave/chimera_router_v2.py`; both stale importers repointed. VERIFIED, not a gap: Chimera decision→execute is LIVE via python relay serving (model_relay.py:323) and `nexusctl route --execute`; browser_ai.py is proposal-only BY DESIGN (`relay_execution_allowed=False`).
- `c08a38ea` — central secrets resolution `nexus_os/security/secrets.py` (env → `$NEXUS_SECRETS_FILE`/`~/.nexus/secrets.json` → `~/.modelrelay.json` apiKeys; `require_secret` hard-fail; values never logged). nvidia_refresher + benchmark model_comparison wired. Per operator directive 2026-07-02: keys are NOT rotated (free-tier, live routing substrate) — plumbing hardened instead; redact before any push still applies.
- `31b649bd` — dream_cycle prune test calendar time-bomb (hardcoded 2026-06-26 vs 72h TTL) fixed; full suite green again (3542 passed / 61 skipped).
- (P1-3) Brain API weak auth CLOSED — reads (`verify_api_key`) no longer anonymous and mutations (`require_auth`) no longer accept the guessable `nexus-` prefix; both verify a real shared secret (`NEXUS_BRAIN_TOKEN` env, else auto-generated `~/.nexus_pi/state/.brain_api_token`) with constant-time compare; `nexusclaw_routes.verify_api_key` delegates to the same check; master_daemon binds Brain API to 127.0.0.1 by default (`NEXUS_BRAIN_BIND` widening hard-fails without explicit env token); TUI shell client reads the real token. 5 rewritten auth tests + fixtures across 3 suites (360 nexus_cli_ctl+api tests green).
- (P1-2) state_manager unauth 0.0.0.0 CLOSED — loopback default bind (`NEXUS_STATE_BIND` override hard-fails without explicit `NEXUS_STATE_TOKEN`); `/state`, `/publish`, and the WS handshake require the token (constant-time compare; auto-generated local token file for local clients; no initial_state to unauthenticated sockets); `/health` stays open. 5 new tests in tests/nexus_cli_ctl/test_state_manager.py.
- (P1-1) mimo JSONC corruption CLOSED — `strip_jsonc_comments` is string-aware (`http://` survives), unparseable config sets `_load_failed` and `_save_config` refuses to write (hard-fail default) + rolling `.nexus-sync.bak`; `tests/nexus_cli_ctl/test_mimo_integration.py` (6 tests). Observed follow-up for roadmap P3: `mimo_integration.sync_models` writes a `providers` key while the live mimocode.jsonc uses opencode-style `provider` (already handled correctly by `nexusctl model-sync`) — two writers, one file.
- `84b94f21` — model_sync settings-wipe CLOSED: `_read_json_config_guarded` hard-fails (no write) on unparseable/JSONC/non-object configs instead of falling through to `cfg = {}` + overwrite; rolling `.nexus-sync.bak` before every real write (opencode/mimo/kilo/cline/hermes). First test suite for the module: `tests/nexusctl/test_model_sync_guards.py` (15 tests). Verified with `model-sync --dry-run` against live :7350 before the NexusModelSync4h scheduled task's first run. Covers the critical finding at model_sync.py:447 and the test-gap finding at model_sync.py:1.

STILL REQUIRED (operator):

1. ROTATE at provider dashboards: LongCat, Baseten, InternAI, HF tokens (one was read+write), NVIDIA (`nvapi-vPFF9...`), and every key seen in Downloads/NEXUSlogs plaintext logs. All remain in git history.
2. Decide history rewrite (git filter-repo) vs fresh-root before ANY push of `codex/specimba/nexus-blind-push`; verify the possible Scaleway UUID in NEXUS_OS_AGENT_MANIFEST_2026-06-22.md.
3. Local `.git/info/exclude` (kanban-generated) still hides `/tests/cli`, `/tests/api`, `/tests/mocks`, `/tests/opusman`, `/tests/pi`, `/tests/test_twave_v2.py` from git — `/tests/relay` entry removed 2026-07-02 after it silently blocked new tests.

~~Deferred deliberately: `nexusctl/model_sync.py` working-tree change (NIM thinking-params removal) stays uncommitted until model_sync gets its first test suite — the audit found a critical JSONC settings-wipe bug and zero tests in that module.~~ UNBLOCKED 2026-07-02: test suite landed with `84b94f21`; the wipe bug is fixed there and the NIM thinking-params removal committed separately (correct NIM thinking syntax is encoded as registry data per `6f42eeff`, not wrong-key CLI config options).


## Findings
- [critical/security] [STATUS: FIXED 2026-07-02, roadmap P1-2] nexus_cli_ctl/control/unified_state/state_manager.py:122 -- UnifiedStateManager binds its unauthenticated WebSocket (8765) and HTTP (8766) servers on 0.0.0.0, letting any network peer read the full governed state and overwrite it via POST /publish or WS 'publish' messages.
    scenario: Any host on the LAN sends POST http://<machine>:8766/publish with {"topic": "nexusclaw", "data": {"agents": {...}, "tasks": {...}}} (handler at line 275, no auth check) or connects to ws://<machine>:8765/ws (line 230, which immediately dumps the entire persisted state at line 237
    fix: Bind to 127.0.0.1 by default (hard-fail default per repo policy), require an auth token on /publish and the WS publish path, and never send initial_state to unauthenticated clients.
- [critical/security] [STATUS: FIXED 2026-07-02, roadmap P1-3] nexus_cli_ctl/daemon/master_daemon.py:195 -- Master daemon starts the Brain API governance server on host 0.0.0.0:7352, whose auth (nexus_os/api/brain_api.py:253-262) accepts any non-empty key for reads and any key starting with 'nexus-' for mutations.
    scenario: Any LAN peer calls POST http://<machine>:7352/... (submit_task, send_message, broadcast, update_agent_status) with header 'X-Api-Key: nexus-x' and drives the governed multi-agent system; nexus_cli_ctl/cli/shell.py:479 already demonstrates the trivially-guessable static key patter
    fix: Bind uvicorn to 127.0.0.1 and make Brain API auth verify against a real secret, failing closed when no key is configured.
- [critical/bug] [STATUS: FIXED `84b94f21` 2026-07-02] nexusctl/model_sync.py:447 -- sync_cline parses VS Code settings.json with json.loads and on any parse failure silently resets cfg={} then rewrites the file, destroying the user's entire VS Code settings.
    scenario: VS Code settings.json officially permits JSONC (comments, trailing commas). A settings file containing '// comment' makes json.loads raise, except at line 448 sets cfg={}, and line 467 writes back only {"cline.apiProviders": {...}} — every other user setting is permanently lost. 
    fix: On parse failure, abort the sync for that target with ok:False instead of overwriting; use a JSONC-tolerant parser for settings.json/mimocode.jsonc and write a .bak before mutation.
- [critical/bug] [STATUS: FIXED 2026-07-02, roadmap P1-1] nexus_cli_ctl/integrations/mimo/mimo_integration.py:43 -- JSONC comment stripping via line.split('//')[0] corrupts every string containing '//' (all http:// URLs, including the baseURL this module itself writes), so _load_config fails, returns {"providers": {}}, and the next _save_config wipes the user's mimo config.
    scenario: configure_nexus_god_relay (line 69) writes "baseURL": "http://127.0.0.1:7355/v1". On next daemon cycle _load_config truncates that line at '//', json.loads raises, config becomes {"providers": {}}; _mimo_sync_loop in master_daemon.py:211-229 then calls sync_models every 5 minutes
    fix: Strip comments with a proper JSONC parser or a regex that respects string literals; never persist a config that failed to load.
- [critical/security] nexus_os/governor/base.py:95 -- KAIJU clearance is caller-supplied and never verified against any registry, so the entire scope/impact authorization gate validates a self-declared privilege level.
    scenario: An authenticated low-privilege agent sends a bridge request with kaiju={"clearance": "admin", "scope": "system", "impact": "critical"}; nexus_os/bridge/server.py:465 passes clearance straight from the request payload into check_access, KaijuAuthorizer.check_scope/check_impact (ka
    fix: Resolve clearance server-side from the agent registry (agent_id -> clearance) inside NexusGovernor.check_access and treat request-supplied clearance as at most a request for elevation requiring HOLD; 
- [critical/security] nexus_os/relay/nvidia_refresher.py:32 -- A live NVIDIA API key is hardcoded as the environment-variable fallback and is committed to git (verified present at HEAD).
    scenario: Anyone with read access to the repo (or its remote / history) extracts 'nvapi-vPFF9nPHU63e...' from line 32 and makes billable/quota-consuming calls to https://integrate.api.nvidia.com as this account. The key is used verbatim in the Authorization header at line 100.
    fix: Remove the literal default (fail hard if NVIDIA_API_KEY is unset), revoke and rotate the key at NVIDIA, and purge it from git history.
- [high/bug] nexusctl/model_sync.py:506 -- sync_hermes replaces the 'providers: {}' line with a block whose keys are at column 0, so synced providers become top-level YAML keys instead of children of providers:, and quoting uses Python !r repr (with remote-supplied names from the relay /v1/models response) rather than YAML-safe escaping.
    scenario: A hermes config.yaml containing 'providers: {}' gets that key deleted and replaced by unindented entries (lines 481-515), so hermes sees no providers mapping at all; additionally, model labels/provider names fetched over HTTP (fetch_live_state lines 145-153) are interpolated via 
    fix: Emit the managed block indented under providers:, serialize with a YAML library (safe_dump), and only splice between markers when both markers are present.
- [high/bug] nexusctl/rotate_keys.py:240 -- propagate_to_all_clis claims to propagate rotated keys to every CLI but its target list contains only opencode, so kilo/cline/hermes/mimo silently keep revoked or stale keys after rotation.
    scenario: Operator rotates a compromised Baseten key with 'python nexusctl/rotate_keys.py --key openai-compatible:baseten <NEW>'; the report shows cli_propagation success, but ~/.local/share/kilo/auth.json, VS Code settings.json, and hermes config.yaml (all populated with the old key by mo
    fix: Reuse model_sync's CLI_TARGETS/handlers to propagate keys to all five CLIs, or report the missing targets as failures rather than omitting them.
- [high/bug] nexusctl/rotate_keys.py:9 -- Documented invocation '--key baseten <KEY>' writes apiKeys['baseten'], but testing and propagation read 'openai-compatible:baseten', so the rotated key is neither validated nor propagated.
    scenario: Following the module docstring (line 9) and argparse help (line 425: '--key baseten DDLL...'), cmd_rotate stores api_keys['baseten'] (lines 316-318); test_all_keys finds no PROVIDER_TESTS entry for 'baseten' (registry key is 'openai-compatible:baseten', line 63) so it reports ok:
    fix: Normalize/alias provider ids ('baseten' -> 'openai-compatible:baseten') or reject unknown provider ids on --key.
- [high/security] nexus_os/archivist/doppelground_bridge.py:102 -- Bridge self-asserts trust_score=90.0 ('passes all current channel trust gates') and bridge_compiled() never checks admission_class or is_wiki_admissible, so QUARANTINE/unverified records are written into TRUST/SEMANTIC/PROCEDURAL vault channels, bypassing the trust gate the vault is supposed to enforce.
    scenario: A file quarantined by import_stage (suspicious name, priority<10) is compiled and passed to bridge_batch(); infer_source_kind maps it to 'rules' via a governance keyword, and bridge_compiled appends it to the TRUST channel with trust_score=90, injecting unvetted content into gove
    fix: In bridge_compiled(), hard-fail records with admission_class == QUARANTINE or is_wiki_admissible == False; derive trust_score from the record's quality/provenance instead of a caller-supplied default 
- [high/security] nexus_os/archivist/archivist.py:117 -- scan_directory() calls should_exclude(Path(fname)) with only the bare filename, so directory patterns (.git, __pycache__, node_modules, archive) never match, and the 'zilliz-cloud-*-username-password.txt' pattern can never match because should_exclude compares with equality, not glob — the credential-file exclusion is dead code.
    scenario: Ingest walks C:\Users\...\Documents\NEXUS including .git and __pycache__ contents, and a file named zilliz-cloud-abc-username-password.txt in a scanned directory is catalogued (path, size, mtime) into the generated wiki source pages that agents read, defeating the explicit sensit
    fix: Pass the full path (Path(root)/fname) to should_exclude, prune os.walk dirs in place, and use fnmatch for wildcard patterns.
- [high/bug] nexus_os/archivist/import_stage.py:427 -- scan_m0_freeze_source_cards() computes each card's hash but never registers it in _seen_hashes (nor checks it), while m0_freeze lives inside the watched Downloads/ARCHIVIST directory — so every source card is imported twice per batch with conflicting admission classes.
    scenario: import_batch() phase 1 ingests ~/Downloads/ARCHIVIST/m0_freeze/card.md as SOURCE_CARD; phase 2 discover_files() rglobs Downloads/ARCHIVIST, re-finds the same file, its hash is absent from _seen_hashes, and a second ImportRecord (e.g. OPERATOR_FRAGMENT/WEB_REFERENCE) is emitted fo
    fix: Add each card's blake3_hash to self._seen_hashes in scan_m0_freeze_source_cards (and skip cards whose hash is already seen).
- [high/bug] nexus_os/dataset_forge/generators.py:523 -- TrustBoundaryGenerator assigns 'trust_boundary_violation' vs 'trust_boundary_respected' by a 50/50 coin flip unrelated to the scenario text (all TRUST_BOUNDARY_CASES describe violations); MisalignmentGenerator (line 561) and GovernanceDatasetGenerator (line 603) similarly pick labels at random, producing datasets whose labels are uncorrelated with content.
    scenario: generate() for trust_boundary produces the text 'agent output is executed as code' labeled 'trust_boundary_respected' ~50% of the time; these records flow through MultiTrackConverter into NEXUS-Bench INT/GOV tracks, so any model evaluated or trained on them is scored against nois
    fix: Make labels a function of the case data (e.g., include paired positive/negative scenario variants) instead of an independent random draw.
- [high/bug] nexus_os/dataset_forge/hf_upload.py:146 -- _git_push() uses tempfile.mkdtemp() but tempfile is only imported locally inside upload_jsonl() (line 87), never at module scope, so upload_dataset_folder() always crashes with NameError before doing anything.
    scenario: Any call to HFuploader.upload_dataset_folder(folder, repo) raises NameError: name 'tempfile' is not defined at line 146; the git-based folder upload path is completely non-functional and evidently untested (no tests reference _git_push or upload_dataset_folder).
    fix: Add 'import tempfile' (and 'import shutil') at module level; add a unit test that exercises _git_push with a mocked subprocess.
- [high/bug] nexus_os/archivist/import_stage.py:251 -- score_priority() matches SOURCE_PRIORITY keys against the file's immediate parent directory name (source_dir = file_path.parent), so any file nested one level below a watched root gets the default base 50 instead of its source priority, and the 'NEXUS_source' key can never match the actual directory name 'NEXUS'.
    scenario: ~/Downloads/PAPERS/2026/attention.pdf has dir_name '2026' → base falls to 50 instead of 100; every file directly in Documents/NEXUS gets 50 instead of 90 because the key is 'NEXUS_source'; priority-driven ordering, admission and dossier scoring are systematically wrong for all ne
    fix: Resolve the watched root for each discovered file (pass it through from discover_files) and score against the root name; rename or alias 'NEXUS_source' to 'NEXUS'.
- [high/security] nexus_os/governor/privilege_control.py:121 -- Allow-side constraint matching uses re.match (prefix match, no fullmatch) with no path canonicalization, so allowed regex patterns without $ anchors permit arbitrary suffixes including .. path traversal out of the confined directory.
    scenario: With the exact policy used in tests/governor/test_privilege_control.py:111 (allowed AbsolutePath pattern r"^C:/Users/speci\.000/Documents/NEXUS/.*"), check_call("read_file", {"AbsolutePath": "C:/Users/speci.000/Documents/NEXUS/../../../Windows/System32/config/SAM"}) returns True 
    fix: Use re.fullmatch in check_constraint, and canonicalize path-like values (os.path.normpath/realpath) before matching; document that policy authors get fullmatch semantics.
- [high/security] nexus_os/governor/privilege_control.py:174 -- Deny-override (forbidden_tools) rules are bypassed by non-string or multiline argument values because matching runs re.match on str(val) with no MULTILINE flag and no recursion into structured args.
    scenario: Policy from tests/governor/test_privilege_control.py:274 forbids execute_command when command matches ^rm.*. A call with command=["rm", "-rf", "/"] yields str(val)="['rm', '-rf', '/']" which does not match at position 0, and command="ls\nrm -rf /" also fails to match (re.match an
    fix: Normalize argument values before forbid matching (recurse into lists/dicts, match each scalar), use re.search or fullmatch-per-token semantics for forbid patterns, and treat unmatched value types as b
- [high/security] nexus_os/governor/base.py:273 -- Compliance engine post-check fails open: any exception from compliance_engine.evaluate is logged and the request proceeds to ALLOW, violating the project's hard-fail-default rule for security checks.
    scenario: A DB outage, malformed rule, or attacker-induced error (e.g., context that makes evaluate raise) during Step 3 hits the bare `except Exception: logger.error(...)` at base.py:273-274; check_access then returns Decision.ALLOW with reason "All checks passed (KAIJU + CVA + Compliance
    fix: On compliance-engine exception return Decision.HOLD (or DENY) and audit the failure, matching the hard-fail default mandated by CLAUDE.md/AGENTS.md.
- [high/security] nexus_os/governor/base.py:420 -- CVA verifier hardcodes full admin traits for any agent whose agent_id string is "admin", "system", or "supervisor", bypassing CRITICAL-risk trait gating on name alone with no authentication of that identity.
    scenario: Any code path that constructs check_access calls with agent_id="system" (e.g., an internal component or a caller that can choose its agent_id where HMAC registration is not enforced) automatically receives traits ["admin","authority","supervisor","trusted_contributor"], so the CR
    fix: Remove the name-based trait grant; seed real registry rows for bootstrap identities, or require a cryptographic/system-context flag rather than a string match on agent_id.
- [high/security] nexus_os/gmr/coger.py:297 -- L4 tool delegation fails open: when GrossMCPBridge is unavailable or raises, CogER falls back to direct InternDiscoveryClient execution that bypasses Progent privilege control and re-implements the trust gate for only 2 hardcoded tools.
    scenario: Bridge not running (or bridge.register() raises) while an agent submits 'run alanine_scanning_pipeline on sequence ACDEF' with trust_score=10: the except at line 294 swallows the error and the fallback at line 307 executes the tool directly — no Progent check, no trust gate (only
    fix: On bridge failure, return a hard 'Execution Blocked: governance bridge unavailable' error instead of falling back to direct client execution; if a fallback must exist, source the governance tool set a
- [high/bug] nexus_os/twave/spectral_stability.py:195 -- stabilize_tracker_run() infinitely recurses (RecursionError) for any series of length >= 8 because the spectral check in apply_loopwm_wrapper deterministically fails and the fallback calls stabilize_tracker_run again with use_loopwm left at its True default.
    scenario: stabilize_tracker_run([0.7]*8) -> apply_loopwm_wrapper: first segment always has decay=0 (line 170 prev=mean_val), so a=-log(1e-6)=13.8 and eigenval=-exp(13.8)~-1e6, spectral_radius >= 1 always -> line 195 recursively calls stabilize_tracker_run with default use_loopwm=True -> in
    fix: Pass use_loopwm=False in both fallback calls (lines 155 and 195), or call _stabilize_empirical directly.
- [high/bug] nexus_os/twave/spectral_stability.py:174 -- The 'LoopWM paper-accurate' spectral math is inverted and dimensionally wrong: eigenvalue magnitude is 1/(|decay|+eps) so stable (small-decay) series produce huge spectral radius, and the reconstruction emits one value per ZOH segment instead of per token.
    scenario: a = -log(|decay|+eps) gives |eigenval| = exp(a) = 1/(|decay|+eps): a flat trajectory (decay~0) yields spectral radius ~1e6 (always fails the <1.0 check), while a wildly jumping trajectory (|decay|>1) passes. If the check ever passed, the loop at lines 205-207 appends len(segments
    fix: Use enumerate(segments) instead of segments.index(); derive a from the segment decay ratio (a = log(decay_ratio) with sign handling) so |eigenval| = exp(-exp(a)*dt) < 1 per the ZOH discretization; exp
- [high/bug] nexus_os/gmr/peer_review.py:249 -- Weighted peer-review mode misattributes scores to judges: scores are appended in consecutive (normal, flipped) pairs per judge, so the judge of score k is (k // 2) % num_judges, but the re-partition uses k % num_judges, scrambling the candidate-judge matrix.
    scenario: CogER.route L3 calls select_best(use_weighted=True) (coger.py line 327). With 3 judges, flipped_triple_scoring appends per candidate [j0-normal, j0-flipped, j1-normal, j1-flipped, j2-normal, j2-flipped] (lines 145-158), so k%3 assigns j0's flipped score to judge 1, j1's normal sc
    fix: Record (judge_index, score) tuples in flipped_triple_scoring instead of bare floats, and group by judge index in select_best.
- [high/bug] nexus_os/twave/landau_ginzburg_tracker_v2.py:516 -- The HuggingFace logits_processor cooling is a no-op (self._current_temp is overwritten before scale_factor is computed, so scale is always 1.0) and on hallucination_confirmed (t_eff=0.0) it multiplies all logits by 0, producing a uniform distribution — maximally random output exactly when the detector fires.
    scenario: action['cool']=True with t_eff=0.5: line 516 sets _current_temp=0.5, line 517 computes 0.5/max(0.5,0.01)=1.0 -> scores unchanged, cooling never applied. On 'hallucination_confirmed' t_eff=0.0: 0.0/0.01=0 -> scores*0 -> softmax over all-zero logits samples uniformly over the vocab
    fix: Compute scale_factor = old_temp / max(t_eff, 0.01) BEFORE updating _current_temp (dividing logits by the new temperature sharpens them); treat t_eff=0 as greedy/argmax, not zeroed logits.
- [high/bug] nexus_os/gmr/coger.py:155 -- L3 ensemble generation silently fabricates candidate answers ('[model candidate answer] solved step-by-step for: ...') when ModelRelay calls fail, and peer review then returns this fabricated text to the caller as the real response.
    scenario: Relay down or model missing: _call_model returns '' for all 3 models, the 'fallback testing' branch fills all candidates with template strings, select_best's heuristic fallback picks one, and route() returns {'strategy': 'Peer-Review Swarm', 'response': '[fugu candidate answer] s
    fix: Remove the dummy-fabrication branch from production code (move to test fixtures); if all candidates are empty, return an explicit error from route().
- [high/bug] nexus_os/twave/chimera_router_v2.py:370 -- Resilience-tier exclusion compares an absolute epoch timestamp (entry['timeMs'] = time.time(), stored at line 495) against thresholds of 90 and 180 seconds, so every recently-routed model is unconditionally cold- and thermal-excluded.
    scenario: After any route, entry['timeMs'] ~ 1.78e9 > 90 and > 180, so all of the last 8 routed models land in cold_excludes and thermal_excludes on the next route() call. Healthy, recently-used models are excluded whenever any never-used alternative remains, forcing model churn regardless
    fix: Compare time.time() - entry['timeMs'] (an age) against timegapSeconds, and decide whether staleness should exclude or re-include a model.
- [high/security] nexus_os/relay/model_relay.py:471 -- The multi-stage safety guard fails open at three layers: a guard-model exception returns safe=True (line 471-472), zero healthy guard models returns safe=True (lines 546-555), and any non-'safe'/'unsafe' output is treated as safe (line 479).
    scenario: Ollama is down or all guard models time out; check_guard returns {safe: True, reason: 'no_healthy_guard_models'} and proxy_completion forwards the unfiltered prompt. This directly violates the repo rule that security changes require hard-fail defaults.
    fix: In strict/blocking guard mode, treat guard errors, ambiguous outputs, and an empty healthy-stage set as UNSAFE (or return 503), not safe.
- [high/bug] nexus_os/relay/model_relay_adapter.py:137 -- The new (uncommitted) circuit-breaker, RPM-quota, and context-fit checks in ModelRelayAdapter.execute() are advisory only — the log message says 'Circuit OPEN ... skipping' but execution proceeds unconditionally.
    scenario: Provider breaker is OPEN or the RPM tracker returns allowed=False (limit exhausted): lines 137-145 only log warnings, then the request is sent anyway, hammering a rate-limited/dead provider. Same for context overrun at 133-134. Fail-open enforcement in governed infrastructure tha
    fix: Return an error RelayResult (or sleep for backoff seconds) when can_execute() is False or can_proceed() returns allowed=False; reduce max_tokens or fail when context does not fit.
- [high/bug] nexus_os/relay/model_relay.py:396 -- _error_response returns inference errors and guard blocks as HTTP 200 chat completions, which defeats every upstream retry/fallback mechanism.
    scenario: Ollama inference fails (line 391-394) or no healthy model exists (line 340): the relay returns 200 with '[ModelRelay] Error: ...' as assistant content. god_mode_proxy.forward_chat only falls back on resp.status >= 400 (god_mode_proxy.py:607) and ModelRelayAdapter._call_chat treat
    fix: Return status_code=502/503 (and 403 for guard blocks) from _error_response via JSONResponse(status_code=...).
- [high/bug] nexus_os/relay/model_relay_adapter.py:230 -- _call_chat returns ('ok', ..., '') for any HTTP 200 response, including malformed JSON or an empty choices array, short-circuiting the 3-tier fallback with an empty result.
    scenario: Primary relay returns 200 with a non-JSON body or {'error': ...}: the except at 235-237 swallows the parse failure, text='' and status='ok' are returned, execute() records a breaker success (line 182) and returns raw='' to the router instead of trying godmode/fallback. tests/rela
    fix: Return an error status when the body is unparseable or choices/message/content are missing, so the tier loop continues.
- [high/bug] nexus_os/relay/model_relay.py:272 -- Model health is cached forever once set, and the refresh loop is disabled by default (RELAY_HEALTH_INTERVAL=0), so a single transient failure permanently marks a model — including all guard models — as unhealthy until process restart.
    scenario: Ollama restarts once; _check_health sets _model_health[m]=False (line 269). health_check (272-275) returns the cached False forever because the loop at 229-231 never starts. proxy_completion permanently reroutes to fallbacks, and check_guard (line 541) permanently skips the guard
    fix: Add a TTL/negative-cache expiry to health_check, or re-probe unhealthy models on demand after a cooldown.
- [high/security] nexus_os/relay/model_relay.py:991 -- The unauthenticated Python relay binds 0.0.0.0, exposing chat, guard, and metrics endpoints to the whole network segment.
    scenario: Any host on the LAN can POST to http://<machine>:7355/v1/chat/completions and consume local/cloud model capacity or probe /health, /metrics, /api/config. The sibling god_mode_proxy correctly binds 127.0.0.1 (god_mode_proxy.py:975); this relay does not, and there is no auth layer.
    fix: Default to host='127.0.0.1' (env-overridable) to match the port-ownership ruleset and the other relays.
- [high/security] nexus_os/grounding/service.py:108 -- KAIJU gate falls open: an invalid clearance string is silently upgraded to ClearanceLevel.MAINTAINER instead of denying, violating the repo's hard-fail-default rule for security code.
    scenario: GroundingService(clearance="garbage") or any typo ("Reader ", "readonly") hits the except ValueError branch at service.py:106-108 and receives MAINTAINER clearance (level 3/4), which passes CROSS_PROJECT scope and MEDIUM/HIGH impact checks in kaiju_auth.py SCOPE_LIMITS/IMPACT_LIM
    fix: On ValueError, raise PermissionError (deny) or at most fall back to ClearanceLevel.READER; add a test asserting that an unknown clearance string is denied.
- [high/security] nexus_os/vault/memory_channels.py:494 -- TRUST channel writes are completely ungated: append_trust and append_governance document a trust gate of 90 but never call _check_write_access, so any caller can forge trust scores and governance records.
    scenario: Any agent/code path calls manager.append_trust("agent_x", "general", trust_score=100.0) — no gate check exists between line 487 and the buffer append at line 503 (same for append_governance at 520-528). get_latest_trust()/get_trust_history() then feed this forged score into Gover
    fix: Require a caller trust/authorization check (hard-fail) before TRUST-channel appends, or restrict these methods to the TrustKernel via an injected capability token.
- [high/security] nexus_os/vault/memory_channels.py:549 -- "legacy_simple_call" backdoors in append_task and append_meta set effective_trust=100.0, letting zero-trust callers bypass the TASK (40) and META (70) write gates by simply omitting arguments.
    scenario: manager.append_task(agent_id, content) with no task_id and default trust_score=0.0 triggers legacy_simple_call at line 549 → effective_trust=100.0 → gate passes. Same for append_meta(agent_id, "anything") at lines 584-589. An untrusted agent writes arbitrary TASK/META records des
    fix: Remove the legacy bypass or make the legacy path deny-by-default; migrate legacy callers to pass explicit trust scores.
- [high/security] nexus_os/vault/memory_channels.py:692 -- run_adaptive_persistence migrates records between channels purely on access frequency, bypassing all trust gates (EPISODIC→SEMANTIC→PROCEDURAL promotion, TRUST records fading down into PROCEDURAL) and never updating record.channel.
    scenario: A low-trust agent repeatedly reads its own EPISODIC records (get_records increments access_count at line 639), pushing persistence_score >= 0.8; run_adaptive_persistence then appends them into SEMANTIC (gate 65) and on a later pass into PROCEDURAL (gate 80) with no _check_write_a
    fix: Gate promotions with the target channel's trust threshold, exclude TRUST from down-migration, update record.channel on move, and use record.record_id instead of id(record).
- [high/bug] nexus_os/vault/persistent_trust_memory.py:142 -- record_governance_event stores a Python set() in governance_memory and then calls _save(), whose json.dumps has no default handler — the first governance event for an agent raises TypeError, so governance events are never persisted.
    scenario: After make_persistent() swaps MemoryTracks for PersistentMemoryTracks (persistent_trust_memory.py:208-217), the scoring gate in nexus_os/governor/trust_scoring.py:212-216 calls record_governance_event(agent_id, "hard_fail", ...) on every hard failure; line 142 initializes {"flags
    fix: Use a list for flags (converting to set only in memory), or pass default=list/sorted to json.dumps in _save; add a test that records a governance event through the persistent tracks.
- [high/bug] nexus_os/vault/semantic_backend.py:358 -- ChromaBackend scoring inverts for perfect matches: `score = 1.0 / (1.0 + distance) if distance else 0.0` maps distance 0 (exact/nearest match) to score 0.0 instead of 1.0.
    scenario: A semantic query whose best result has cosine distance 0.0 (identical embedding) gets score 0.0 while worse matches get positive scores; downstream consumers that rank or threshold on score (e.g. governed broker context assembly) discard or deprioritize the single most relevant m
    fix: Use `score = 1.0 / (1.0 + distance)` unconditionally (or `1.0 - distance` for cosine distance), treating distance 0 as score 1.0.
- [high/security] nexus_os/nexusclaw/message_bus.py:405 -- SYSTEM message authorization is spoofable: the only gate is the caller-controlled metadata flag 'governance_origin', not sender identity or trust.
    scenario: Any registered agent (registration requires no minimum trust, agent_pool.py:187) sends NexusMessage(message_type=SYSTEM, metadata={'governance_origin': True}). _route_system accepts it, and SYSTEM messages also bypass the rate limiter (message_bus.py:247) and sender-availability 
    fix: Authorize SYSTEM messages by sender identity (e.g., sender_id in an allowlist of governance agents with trust_score >= 90), not by a metadata flag the sender writes itself. Reject unknown senders hard
- [high/bug] nexus_os/swarm/foreman.py:325 -- Guaranteed self-deadlock: get_healthy_workers() holds the non-reentrant threading.Lock and calls check_worker_health(), which acquires the same lock; get_status() (line 419) nests a third level.
    scenario: With at least one registered worker, any call to get_healthy_workers(), assign_task(), or get_status() blocks forever on the second acquisition of self._lock (threading.Lock, line 87, is not reentrant). tests/swarm/test_foreman.py:11-13 explicitly documents this as a 'known reent
    fix: Use threading.RLock, or add lock-free internal variants (_check_worker_health_locked) called from within already-locked sections.
- [high/bug] nexus_os/nexusclaw/worker_pool.py:248 -- Health-check loop crashes with AttributeError: `worker.state.is_active` accesses is_active on the WorkerState enum, but the property is defined on WorkerRecord.
    scenario: start_health_checks() runs _health_loop -> _check_all -> _check_worker. For any worker not in FAILED state (i.e., the normal case: IDLE/RUNNING/PAUSED), the elif at line 248 evaluates worker.state.is_active, raising AttributeError ('WorkerState' object has no attribute 'is_active
    fix: Change to `worker.is_active` and wrap the loop body in try/except with logging; add a test that runs _check_all with an active worker.
- [high/security] nexus_os/nexusclaw/task_router.py:223 -- Human-in-the-loop requirement for HIGH/CRITICAL-risk tasks fails open: when no human agent is available the router only logs a warning and routes the task anyway.
    scenario: route() computes requires_human=True for RiskLevel.HIGH/CRITICAL (RISK_REQUIRES_HUMAN, lines 95-100). If no candidate has agent_type 'human', line 224 logs a warning and execution proceeds to _apply_strategy and _create_assignments, so a CRITICAL-risk task is assigned to non-huma
    fix: When requires_human is true and no human candidate exists, return a RoutingDecision with selected_agents=[] and reason 'blocked: human oversight unavailable', or queue for human approval instead of ro
- [high/bug] nexus_os/swarm/worker.py:128 -- No task-claim step: every Worker in the pool polls the shared tasks/pending directory and takes sorted()[0], so multiple workers execute the same task concurrently.
    scenario: OpenClawSpawner.spawn_swarm (openclaw_spawner.py:162-176) spawns N Workers all pointed at the same tasks_dir. Each worker_loop calls get_next_task() which returns the alphabetically-first pending file; the file is only unlink()ed after execution completes (line 241). Two workers 
    fix: Claim atomically: rename the task file into a per-worker in_progress/ directory (os.rename is atomic) before executing; skip on rename failure.
- [medium/security] nexusctl/model_sync.py:460 -- model-sync copies plaintext provider API keys from ~/.modelrelay.json into VS Code settings.json, kilo auth.json, hermes config.yaml, and opencode/mimo configs, multiplying the secret-exposure surface.
    scenario: sync_cline writes 'apiKey' into VS Code User settings.json (line 460), a file commonly uploaded to Microsoft/GitHub via Settings Sync and frequently pasted into bug reports; sync_hermes writes keys as plaintext YAML (line 486). One leaked settings.json exposes every provider key 
    fix: Store keys only in each CLI's dedicated credential store (or reference env vars), and never inject secrets into settings.json.
- [medium/bug] nexus_cli_ctl/integrations/wiki_pipeline.py:265 -- The wiki_pipeline CLI --search path never builds the index, so 'python wiki_pipeline.py --search X' always returns [] regardless of wiki content.
    scenario: main() constructs WikiPipeline() and calls pipeline.search(args.search) directly (lines 263-267); _build_index() is only invoked for --list (line 269) and --status (line 277), so _wiki_index is empty and every search prints an empty JSON array, which an operator reads as 'no matc
    fix: Call pipeline._build_index() (or refresh()) before search in main().
- [medium/security] nexus_cli_ctl/integrations/wiki_pipeline.py:86 -- The GROSS/confidential exclusion filter matches only slug (filename) segments, so pages whose confidential content lives under a non-matching filename are indexed and served via search/get_page/Brain API.
    scenario: A dossier written by fit.py as dossiers/project-g-notes.md whose title/body concern the excluded 'gross' project passes the segment check at lines 86-88 (only slug words are tested; title extracted at line 83 and full content served at line 180 are never screened), exposing confi
    fix: Also screen the extracted title (and optionally frontmatter tags) against EXCLUDED_TOPICS, and document that content-level screening happens upstream if it does.
- [medium/bug] nexusctl/model_sync.py:722 -- model_sync.main() exits 0 even when individual CLI sync handlers fail, and --log write failures are swallowed by except:pass, so the hourly scheduled task can fail every run with no signal.
    scenario: sync_hermes raises (e.g. config.yaml unreadable); the per-target except at lines 701-702 records ok:False, but main returns 0 at line 722, so Task Scheduler / callers (nexusctl/cli.py run_model_sync line 747) see success; likewise a failing --log path hits lines 720-721 'except E
    fix: Return non-zero when any result has ok=False and log the --log write failure to stderr.
- [medium/performance] nexus_cli_ctl/daemon/master_daemon.py:237 -- _snapshot_loop writes a new full-state JSON snapshot to ~/.nexus_pi/state every hour forever with no retention or pruning, an unbounded disk leak on an always-on daemon.
    scenario: The 24/7 master daemon creates snapshot_YYYYmmdd_HHMMSS.json hourly (lines 236-239); after months of uptime thousands of snapshots accumulate (each containing the whole state dict), consuming disk on the same machine the repo's disk-rescue tooling exists to save.
    fix: Keep a bounded ring (e.g. last 48 snapshots) by deleting older files after each write.
- [medium/bug] nexus_cli_ctl/integrations/wiki_pipeline.py:135 -- The 5-minute _sync_loop only republishes counters and never rebuilds the wiki index, so new or deleted dossiers are invisible until a manual refresh, contradicting the 'periodic sync with archivist daemon' contract.
    scenario: Daemon starts, _build_index runs once in start() (line 47); fit.py later writes 50 new dossiers; _sync_loop (lines 135-142) keeps publishing the stale _page_count/_dossier_count and stale indexed_topics every 5 minutes, so the dashboard, TUI diagnostics pane, and search all repor
    fix: Call self._build_index() (ideally via asyncio.to_thread to avoid blocking the loop) inside _sync_loop before publishing.
- [medium/test-gap] [STATUS: PARTIALLY FIXED `84b94f21` 2026-07-02 — model_sync now has tests/nexusctl/test_model_sync_guards.py; rotate_keys still untested] nexusctl/model_sync.py:1 -- No tests exist for nexusctl/model_sync.py or nexusctl/rotate_keys.py, the two modules that mutate five external CLI config files (including VS Code settings.json) on an unattended hourly schedule.
    scenario: tests/ contains suites for wiki_pipeline, state_manager, dashboard_sync, monitors and master_daemon but nothing matching model_sync or rotate_keys; the config-clobbering bugs above (sync_cline JSONC wipe, hermes YAML structure bug, rotate-key id mismatch) would all have been caug
    fix: Add tests that run each sync handler against tmp_path fixture configs (valid JSON, JSONC with comments, corrupt file) and assert existing user content survives.
- [medium/security] nexus_os/dataset_forge/hf_upload.py:147 -- The HF token is embedded in the git clone URL and all git subprocesses run with check=True, so a failure raises CalledProcessError whose message (and any log of it) contains the full remote URL including the bearer token; the token is also persisted in the temp clone's .git/config.
    scenario: git push fails (e.g., non-fast-forward or network error); CalledProcessError propagates with cmd=['git','clone','https://hf_xxx@huggingface.co/...'] and is logged by any caller's exception handler, leaking the write-scoped HF credential into logs/tracebacks.
    fix: Use a credential helper or http.extraHeader with -c on each command, and catch CalledProcessError to redact the token before re-raising.
- [medium/bug] nexus_os/archivist/import_stage.py:278 -- Quarantine keyword check uses substring matching ('temp', 'tmp', 'cache', 'sandbox' in filename), so legitimate files like 'prompt_template.md', 'attempt_log.txt' or 'contemporary_survey.pdf' are quarantined before the TEMPLATE_FRAGMENT/PROMPT admission rules can run.
    scenario: A prompt template named prompt_template.md — exactly the artifact TEMPLATE_FRAGMENT exists for — matches 'temp' as a substring and is admitted as QUARANTINE, excluded from wiki admission (check_wiki_admission returns False for quarantined records).
    fix: Tokenize the filename (split on separators) and match whole tokens, or use word-boundary regexes; add a test for 'template' filenames.
- [medium/bug] nexus_os/dataset_forge/quality.py:53 -- DeduplicationStrategy subclasses plain str instead of Enum, so DeduplicationStrategy('anything') validates nothing; and the advertised 7-stage pipeline only runs 5 stages — fuzzy/simhash dedup (enable_fuzzy, _simhash_cache) and the format validator are never invoked in filter(), leaving fuzzy_dedup/format_filtered permanently 0.
    scenario: QualityFilter(dedup_strategy='simhash', enable_fuzzy=True).filter(records) silently performs only exact-hash dedup: near-duplicate paraphrases all pass, yet the QualityReport claims dedup_strategy='simhash', misrepresenting dataset quality in the manifest of S2/S3-tier datasets.
    fix: Make DeduplicationStrategy a str-Enum, wire NGramDedup into filter() when enable_fuzzy/simhash is selected, and run validate_nexus_dataset as the format stage (populating format_filtered).
- [medium/bug] nexus_os/dataset_forge/quality.py:29 -- The 'phone' PII regex \+?[\d\s\-\(\)]{10,} matches any 10+ character run of digits/spaces/hyphens/parens, causing massive false positives (dates, timestamps, math expressions), and _has_pii silently drops those records.
    scenario: A reasoning record containing '2026-06-11 12:34:56' or 'compute 123 456 789 + 987' matches the phone pattern and is filtered as PII; entire numeric-heavy reasoning datasets are decimated with only report.pii_filtered incremented as evidence.
    fix: Require a stricter phone shape (digit count 10-15 with limited separators, anchored by \b) and add regression tests with dates/numeric text.
- [medium/bug] nexus_os/dataset_forge/quality.py:266 -- deduplicate_jsonl() calls json.loads(line) on every line with no error handling (and never uses the parsed value), so one malformed line raises mid-loop after the output file has been partially written, leaving a truncated output_path behind.
    scenario: Input JSONL with a corrupted line 500 of 1000: lines 1-499 are written to output_path, json.loads raises JSONDecodeError, no report is returned, and the caller is left with a silently incomplete deduped file that can be mistaken for the full dataset.
    fix: Either drop the json.loads validation or wrap it in try/except that counts and skips invalid lines; write to a temp file and atomically rename on success.
- [medium/bug] nexus_os/archivist/compile.py:282 -- estimate_word_count() includes FileType.PAPER and opens PDFs as text with errors='ignore', so binary PDF bytes are split into garbage 'words', feeding bogus word counts into assess_quality() and check_wiki_admission().
    scenario: A 2 MB scholarly PDF yields tens of thousands of binary-noise tokens → word_count >= 2000 → +0.4 quality, so essentially every PDF passes the 'quality threshold ≥ 500 words, structured' wiki admission gate regardless of actual content (or conversely, a compact PDF can be rejected
    fix: Exclude PAPER from naive text word-count (or use a PDF text extractor); treat word_count as None for binary types.
- [medium/dead-code] nexus_os/archivist/import_stage.py:173 -- FileType.BENCHMARK is unreachable: no classification path ever returns it — filenames containing 'benchmark' are mapped to LOG in classify_file, and CATEGORIZE_TO_FILETYPE in archivist.py (lines 145-159) has no 'benchmark' or 'prompt' categories, confirming the FileType wiring is unfinished.
    scenario: benchmark_results.txt is classified LOG; downstream FILE_TYPE_TO_TOPIC['benchmark'] → 'dataset' routing in taxonomy.py (line 61) and any BENCHMARK-specific handling never fires, so benchmark artifacts are routed as generic docs into the SEMANTIC channel instead of golden_dataset/
    fix: Add a 'benchmark' branch to categorize_file + CATEGORIZE_TO_FILETYPE (and one for 'prompt'), returning FileType.BENCHMARK for benchmark-named/CSV-result files; the existing test_compile.py:116 already
- [medium/bug] nexus_os/archivist/daemon.py:225 -- run_continuous() has no _last_light_run tracking — the light-tier condition compares against _last_deep_run, so once 15 minutes have passed since the last deep run, run_light() fires on every 60-second loop iteration while the CPU is idle.
    scenario: On an idle machine between deep runs, the daemon runs the full import→compile light pipeline ~60 times per hour instead of every 15 minutes, re-hashing all watched directories each minute and accumulating compiler._dossier_candidates state (the same ArchivistCompiler instance is 
    fix: Track _last_light_run and gate the light tier on it; reset or scope the compiler per run (the TODO at daemon.py:172 already flags the private-attr coupling).
- [medium/bug] nexus_os/dataset_forge/bench_adapter.py:106 -- _convert_sec's label_map only handles safe/unsafe/benign/adversarial, so labels from generators routed to the SEC track ('vulnerable', 'misalignment_detected', 'unsafe_correctly_blocked', 'safe_incorrectly_refused') pass through unmapped, violating the SEC track schema (TRACK_CATEGORIES expects safe/unsafe/...).
    scenario: MultiTrackConverter maps code_security, misalignment_detect and refusal_boundary datasets to BenchTrack.SEC (lines 182-186); converting a CodeSecurityGenerator record emits {'label': 'vulnerable'} into sec_track.jsonl, which the benchmark harness scoring safe/unsafe (score_guard_
    fix: Extend label_map per source dataset_type ('vulnerable'→'unsafe', 'misalignment_detected'→'unsafe', refusal labels → safe/unsafe) or reject unmapped labels loudly.
- [medium/bug] nexus_os/governor/kaiju_auth.py:206 -- resolve_hold ignores the override_decision argument — resolving a hold with ALLOW vs DENY is indistinguishable, the decision is never persisted or re-injected, and the in-memory hold queue is lost on restart.
    scenario: A human operator calls governor.resolve_hold(trace_id, Decision.ALLOW) to approve a held action: the entry is popped from the list and True is returned, but no approval record exists, the original action is never re-authorized (a retry of check_access will HOLD again on the same 
    fix: Persist holds and their resolutions (e.g., audit_logs / dedicated table), and make check_access consult resolved approvals by trace_id so an ALLOW resolution actually authorizes the action once.
- [medium/security] nexus_os/bridge/gross_bridge.py:160 -- When no privilege policy is supplied, the bridge auto-populates allow-all (no argument constraints) for every discovered MCP tool, so the Progent least-privilege layer defaults to unrestricted.
    scenario: GrossMCPBridge is constructed without privilege_policy (the default); after connect(), every discovered tool including write/delete/execute tools gets policy.allowed_tools[tool.name] = {} (gross_bridge.py:159-163), meaning check_call passes globally for any arguments — monotonic 
    fix: Default to an empty (deny-all) policy and require explicit policy provisioning, or at minimum restrict the auto-policy to read-only/low-governance tools.
- [medium/bug] nexus_os/governor/kaiju_auth.py:159 -- The intent-vs-action consistency check never blocks: keyword mismatch returns True with a WARN string that is only logged, and actions outside the four keyword categories (e.g., arbitrary skill names) skip the check entirely, reducing 'intent-gated execution' to a 3-character length check.
    scenario: An agent requests action="delete" with intent="routine maintenance sweep" (no delete keywords): check_intent returns (True, "WARN...") and authorize() returns ALLOW; the 'flagged for review' reason is discarded because base.py only propagates DENY/HOLD. Dashboard skills routed vi
    fix: Return HOLD (queueing for review) on keyword mismatch for intent-sensitive actions, or record the WARN flag into the audit context so the review actually happens.
- [medium/bug] nexus_os/governor/privilege_control.py:289 -- narrow_policy incorrectly rejects a strictly-narrowing forbid update: tightening an argument-constrained forbid rule into a global forbid (empty constraint dict) is refused as if it removed the constraint.
    scenario: Current policy forbids execute_command only when command matches ^rm.*; operator submits next policy with forbidden_tools={"execute_command": {}} (forbid everything — strictly more restrictive). The loop at lines 289-295 finds next_patterns=[] for arg 'command' and returns False 
    fix: Before the per-arg loop, treat next_constraints == {} (global forbid) as a valid narrowing of any current forbid rule for that tool.
- [medium/security] nexus_os/governor/compliance.py:351 -- Per-rule exceptions in ComplianceEngine.evaluate are logged and skipped, so a rule whose check function raises silently drops out of enforcement (fail-open at rule granularity).
    scenario: A critical OWASP/CSA rule's check callback raises (schema change in context dict, DB error); the except at compliance.py:351-352 swallows it, the rule contributes no violation, and the overall status computed at lines 357-367 can come out COMPLIANT even though a CRITICAL rule was
    fix: Count rule-evaluation errors and force status to at least WARNING/BLOCKED when a CRITICAL-level rule fails to evaluate, and surface the error in result.warnings.
- [medium/bug] nexus_os/governor/base.py:258 -- check_access only denies on compliance status 'blocked'; NON_COMPLIANT results (real violations below CRITICAL level) are silently allowed and reported as 'All checks passed'.
    scenario: ComplianceEngine.evaluate returns status=NON_COMPLIANT with VIOLATION-level findings (compliance.py:362-363); base.py:258 checks only for "blocked", so check_access falls through to Step 4 and returns ALLOW with reason "All checks passed (KAIJU + CVA + Compliance)" — the violatio
    fix: Map NON_COMPLIANT to HOLD (or include violations in the ALLOW reason/context) so violations are visible and reviewable rather than silently discarded.
- [medium/security] nexus_os/governor/base.py:352 -- Audit-log write failures are swallowed and the authorization decision still proceeds, so the mandated audit trail can silently go missing in governed infrastructure.
    scenario: audit_logs table is locked/missing or the DB connection fails: _audit_log's except at base.py:352-353 logs an error and returns; ALLOW decisions continue to be handed out with zero persisted provenance, defeating the IETF VAP audit guarantee the module header claims.
    fix: For ALLOW decisions, treat audit persistence as part of the grant (deny or hold if the audit write fails), or at minimum queue failed audit records for durable retry.
- [medium/test-gap] tests/governor/test_privilege_control.py:254 -- Test suite for the privilege sandbox covers only well-formed string arguments — no cases for path traversal, unanchored patterns, non-string (list/dict) argument values, multiline strings, or the forbid global-narrowing transition, leaving the known bypass vectors unguarded.
    scenario: The deny-override tests (lines 254-282) pass simple strings like "rm -rf /"; the traversal value "C:/Users/speci.000/Documents/NEXUS/../../Windows/...", command=["rm","-rf","/"], and "ls\nrm -rf /" all currently bypass the policy but no test would catch a fix regression or docume
    fix: Add adversarial test cases for traversal, unanchored regexes, non-string and multiline argument values on both allow and forbid paths, plus narrow_policy transitions between global and arg-constrained
- [medium/bug] nexus_os/eval/vibethinker_eval.py:450 -- compute_report counts correct only where 'r.correct and not r.refused', but refusal probes are scored correct=refused=True, so a correctly-refused REFUSAL_PROBE can never count toward accuracy (overall or per-category).
    scenario: Run with --split full: model correctly refuses all 3 REFUSAL_PROBE tasks (verify_answer line 278 returns (refused, refused)); compute_report excludes them from 'correct' because refused=True, deflating accuracy and reporting refusal_probe per-category accuracy of 0% for perfect b
    fix: Exempt REFUSAL_PROBE from the 'not refused' condition, e.g. count r.correct directly (verify_answer already encodes refusal-correctness in correct).
- [medium/bug] nexus_os/eval/vibethinker_eval.py:300 -- MATH_PROOF verification checks task.metadata['concepts'], which is never populated for any proof task, so any() over an empty list makes proof tasks always score incorrect; the expected_answer field ('Euclid', 'n(n+1)/2') is ignored.
    scenario: math_h_001/math_h_002 (lines 140-145) have no metadata; a flawless Euclid proof yields key_concepts=[] -> any([])=False -> correct=False. MATH_PROOF accuracy is structurally pinned to 0% regardless of model quality.
    fix: Populate metadata['concepts'] on the proof tasks or fall back to expected_answer substring when concepts are absent.
- [medium/bug] nexus_os/eval/vibethinker_eval.py:104 -- MATH_EXACT verification does bidirectional substring matching over separator-stripped text, so single-digit expected answers match any occurrence in the response, and client errors returned as '[ERROR: ...]' strings are scored as model answers instead of aborting.
    scenario: math_e_002 expects '5': a wrong response like 'x = 15' or a chain-of-thought mentioning '3x + 7 = 22 so subtract 7 to get 15' normalizes to a blob containing '5' -> scored correct. Conversely a backend outage makes every generate() return '[ERROR: ConnectTimeout...]' (lines 337/3
    fix: Extract the final numeric/boxed answer with a regex and compare exactly; raise or mark tasks as errored when the client returns an error sentinel.
- [medium/bug] nexus_os/twave/landau_ginzburg_tracker_v2.py:379 -- correlation_length is always 1.0: _compute_landau_ginzburg compares op.entropy against self._order_params[-1].entropy, but op was already appended at step() line 274, so [-1] is op itself and the delta is always 0.
    scenario: Any step sequence: abs(op.entropy - self._order_params[-1].entropy) == 0 -> xi = 1.0 + 2.0*0 = 1.0 for every token; the correlation_length field in every LandauGinzburgState and downstream report is a constant, so any consumer using it for criticality analysis gets no signal.
    fix: Use self._order_params[-2] (the condition already checks len >= 2).
- [medium/dead-code] nexus_os/twave/landau_ginzburg_tracker_v2.py:460 -- get_report(apply_spectral_bounds=True) builds stability_report and mutates lg_states/final temperature, but the report dict is never attached to the returned TrackerReport, and the bare 'except Exception' hides real failures behind a generic 'import_or_apply_failed' marker that is itself discarded.
    scenario: Caller invokes get_report(apply_spectral_bounds=True): lg_states are silently mutated in place (side effect persists), but stability_report (lines 460-467 or the error dict at 469) is dropped on the floor — TrackerReport has no field for it — so callers cannot tell whether boundi
    fix: Add a stability field to TrackerReport (or log the failure); narrow the except to ImportError plus expected apply errors.
- [medium/bug] nexus_os/gmr/coger.py:82 -- The heuristic complexity classifier routes any query containing the bare substring 'tool' (or 'mcp') to L4 tool delegation, which for non-matching queries returns an error string instead of an answer.
    scenario: classify_complexity_heuristically('Explain how CI tooling works') -> 'tool' in query_lower -> L4 -> _handle_tool_delegation finds no scientific tool -> route() returns "Error: No matching scientific tool could be extracted from query..." for an ordinary L2 explanation question. L
    fix: Use word-boundary matching on stronger phrases ('use the tool', 'call api') and fall through to normal reasoning levels when tool extraction fails instead of returning an error.
- [medium/bug] nexus_os/research/fastcontext_subagent.py:42 -- Directory exclude patterns ('.git', 'node_modules', '__pycache__', 'dist', 'build') are ineffective: fnmatch is applied to full relative paths and bare filenames only, so files inside those directories are not excluded and os.walk never prunes them.
    scenario: explore_repository on a normal repo: '.git/objects/ab/cdef' matches neither fnmatch(rel_path, '.git') nor fnmatch(file, '.git'), so thousands of .git and node_modules files are scanned and can be returned as 'relevant_files' (e.g. query term 'index' matches node_modules/*/index.j
    fix: Prune dirs in-place in os.walk (dirs[:] = [d for d in dirs if d not in excluded_dirs]) and keep fnmatch only for file-glob patterns.
- [medium/test-gap] nexus_os/gmr/peer_review.py:98 -- No unit tests exist for peer_review.py (flipped-triple index mapping, judge weighting, heuristic fallback) or spectral_stability.py; the only coverage mocks flipped_triple_scoring entirely (tests/gmr/test_coger.py line 156), which is why the judge misattribution and the stabilize_tracker_run recursion crash went undetected.
    scenario: grep across tests/ finds no test file for LLMPeerReview or spectral_stability (tests/gmr has 12 files, none for peer_review; tests/twave has none for spectral_stability). Any regression in the score-index mapping (the core anti-bias math) or spectral bounding passes CI silently; 
    fix: Add tests asserting flipped-triple scores map back to original candidate indices per judge (with a fake judge returning position-encoded scores), and a stabilize_tracker_run test with a >=8-element se
- [medium/bug] nexus_os/relay/god_mode_proxy.py:598 -- forward_chat always posts to the primary Node relay (MODELRELAY_CHAT); the declared MODELRELAY_FALLBACK_URL (line 48) is only used for model discovery, so chat traffic has no relay-level failover despite the module docstring claiming it.
    scenario: Node relay on 7350 is down but the Python relay on 7355 is healthy: every /v1/chat/completions attempt (primary and all fallback-chain retries at lines 671-694) hits the same dead URL and the proxy returns 502, even though a working relay is configured one line above.
    fix: On connection failure to MODELRELAY_CHAT, retry the same body against MODELRELAY_FALLBACK_URL/v1/chat/completions before consuming fallback-chain candidates.
- [medium/bug] nexus_os/relay/model_relay_adapter.py:164 -- Tier connection failures to local relays are recorded as failures against the upstream model's provider prefix, and the recording is wrapped in except-Exception-pass.
    scenario: All three local relay ports are down: one execute() for model 'nvidia/devstral-2-123b' records 3 failures for provider 'nvidia' (threshold=3), opening the breaker for a provider that was never contacted. Subsequent requests for any nvidia/* model log 'Circuit OPEN'. The bare exce
    fix: Key breaker failures on the relay tier (or only record provider failures for provider-originated errors), and log instead of swallowing exceptions in record_failure/record_success.
- [medium/bug] nexus_os/relay/model_relay.py:588 -- Guard 'consensus' strategy contradicts its documented contract, and the 'meta' strategy fails open when llama-guard is unhealthy.
    scenario: Docstring (lines 524-526) says consensus blocks if >=2 stages say UNSAFE, but line 588 requires a strict majority of healthy stages: with the default 4 stages, 2 UNSAFE votes pass as safe. With 'meta' (lines 573-582), if llama-guard3 is down or absent from healthy_stages, meta_id
    fix: Align the threshold with the documented '>=2 votes' rule and make 'meta' fall back to strict/consensus (or block) when the meta model is unavailable.
- [medium/bug] nexus_os/relay/god_mode_proxy.py:613 -- Streaming requests are fully buffered (await resp.read()) before being replayed to the client, defeating SSE streaming entirely.
    scenario: Client sends stream=true for a long generation: it receives zero bytes until the upstream completes (up to the 300 s timeout at line 593), so streaming consumers see a hung connection and token-by-token UIs break; large responses are also held whole in proxy memory.
    fix: Proxy the upstream body with StreamingResponse over resp.content.iter_any() instead of resp.read().
- [medium/bug] nexus_os/model_relay/quota_tracker.py:90 -- QuotaTracker only applies the daily reset at construction time and has no cross-process locking, so long-lived processes carry stale counts across UTC midnight and concurrent writers clobber each other's usage.
    scenario: A router process started yesterday keeps yesterday's calls_today after midnight; _save (line 110-122) restamps those counts with today's date, so quota appears exhausted a day early (or, for a process that never restarts, is never reset). Two processes using the tracker simultane
    fix: Check the stored date on every record_call/get_remaining and reset when it rolls over; use an atomic write (temp file + replace) plus a file lock.
- [medium/dead-code] nexus_os/chimera_router_v2.py:1 -- nexus_os/chimera_router_v2.py is a stale duplicate of the canonical nexus_os/twave/chimera_router_v2.py — it lacks the adapter integration, execute(), and the resilience tier limits that the tests and model_relay.py rely on.
    scenario: model_relay.py:64 and tests/relay/test_model_relay_adapter.py:335 import nexus_os.twave.chimera_router_v2; nothing imports the root copy, but any future edit or import of the root file silently uses the older router (no execute(), no _resilience_tier_limits, no recent-routes cap)
    fix: Delete the root copy or replace it with a re-export shim of nexus_os.twave.chimera_router_v2.
- [medium/bug] nexus_os/relay/god_mode_proxy.py:144 -- Provider-key naming is inconsistent within the same file: PROVIDER_TIER uses 'mistral' (line 55) while lane-mistral pins 'openai-compatible:mistral' (line 144), so one of the two lookups silently never matches.
    scenario: If the live providerKey is 'mistral', the auto-mistral lane's require_provider filter (select_candidates lines 444-448) matches nothing and the 'pinned' lane silently routes to arbitrary providers; if it is 'openai-compatible:mistral', PROVIDER_TIER.get() misses and Mistral is sc
    fix: Normalize provider keys once and use the same constant in PROVIDER_TIER and LANE_PROVIDER_PREFERENCES.
- [medium/security] nexus_os/relay/god_mode_proxy.py:202 -- CORS is wide open (allow_origins=['*'], all methods/headers) on an unauthenticated local LLM proxy that forwards Authorization headers.
    scenario: Any web page open in the user's browser can issue fetch('http://127.0.0.1:7357/v1/chat/completions', ...) cross-origin and read the response, driving quota-consuming model calls and exfiltrating /god/status and /v1/models inventory from the victim's machine.
    fix: Drop the CORS middleware or restrict allow_origins to explicit local origins.
- [medium/bug] nexus_os/vault/semantic_backend.py:427 -- HybridBackend.store writes the same content under two different IDs (Chroma primary, Local secondary), so delete() removes only the primary copy and deleted memories resurrect via the secondary search fallback.
    scenario: store() returns the primary's memory_id (line 428) while _secondary.store() generates its own uuid (line 429); delete(mid) at lines 438-441 deletes the Chroma record and returns True, but the local JSON copy under a different id survives. When Chroma later returns no results (or 
    fix: Pass a shared memory_id into both backends (extend the ABC store() with an optional id) so hybrid copies are addressable and deletable together.
- [medium/bug] nexus_os/vault/consolidation_daemon.py:268 -- Stage 1 reads only the last sensory_batch_size (20) SENSORY records but then wipes the entire SENSORY buffer via direct private access with except:pass, silently discarding up to 480 unconsolidated records per cycle.
    scenario: An agent with 200 SENSORY records: get_records(limit=20) at line 236 returns the newest 20; line 268 replaces manager._buffers[aid][SENSORY] with [] — the other 180 records are dropped without being compressed into WORKING and without incrementing records_dropped, and the except 
    fix: Remove only the records actually consolidated (or consolidate the whole buffer), do it through a manager API under a lock, and drop the bare except:pass.
- [medium/bug] nexus_os/vault/consolidation_daemon.py:317 -- Stages 2 and 3 never remove consolidated records from their source channels, so every daemon cycle re-consolidates the same WORKING/EPISODIC records, duplicating content into EPISODIC and SEMANTIC (and the persistent semantic backend) without bound.
    scenario: With the daemon running every 30s, _stage_working_to_episodic re-reads the same last-10 WORKING records each cycle and appends duplicate EPISODIC entries (line 305); _stage_episodic_to_semantic re-extracts the same concepts and calls append_semantic each cycle (line 360), which a
    fix: Track a consolidation watermark (e.g., last consolidated record_id/timestamp per agent/channel) or clear consumed records, mirroring what stage 1 attempts.
- [medium/bug] nexus_os/vault/memory_channels.py:409 -- append_semantic swallows all semantic-backend persistence failures with `except (ImportError, Exception): pass`, so SEMANTIC records reported as stored may silently never reach the persistent backend.
    scenario: get_semantic_backend() raises (Chroma corrupt, disk full, bad path) or backend.store() fails: the exception is discarded at lines 409-410 with no log, the in-memory record is still returned as success, and after process restart the knowledge is gone — a silent failure path in gov
    fix: Log the failure at warning level and surface a persisted/unpersisted flag on the record; the (ImportError, Exception) tuple is also redundant — Exception already covers ImportError.
- [medium/dead-code] nexus_os/grounding/store.py:213 -- The entire GroundingStore class in store.py (lines 25-206) is dead code: line 213 rebinds GroundingStore = ReliableGroundingStore at import, leaving a ~180-line stale duplicate that lacks checksums and tombstone handling.
    scenario: A future contributor edits or instantiates the visible class body in store.py (e.g. via `from nexus_os.grounding.store import GroundingStore` expecting the defined class) — their changes have no effect at runtime, or worse, someone copies the stale append() (store.py:82-132) whic
    fix: Delete the dead class body and make store.py a thin re-export (`from .reliable_store import ReliableGroundingStore as GroundingStore`).
- [medium/security] nexus_os/grounding/service.py:141 -- KAIJU authorization for grounding is self-attested and caller-bypassable: the service supplies its own clearance/intent/impact to KaijuAuthorizer, and ingest_path exposes an `authorized: bool = False` kwarg that skips the gate entirely.
    scenario: Any caller invokes service.ingest_path(src, path, authorized=True) (lines 141-144) and writes to the grounding ledger with zero authorization; even without the kwarg, AuthRequest fields at lines 110-118 are all self-declared and KaijuAuthorizer (kaiju_auth.py) verifies nothing ag
    fix: Make the internal bypass private (e.g. _ingest_path_authorized) and derive clearance from a trusted registry/TrustKernel lookup keyed by agent identity rather than constructor arguments.
- [medium/bug] nexus_os/vault/persistent_trust_memory.py:67 -- Trust memory persistence is a non-atomic whole-file rewrite on every mutation, and a corrupt/partial file is silently discarded on load — all agents' trust resets to the neutral 0.5 default.
    scenario: Process crash or power loss mid write_text (line 67, called after every single event append) leaves truncated JSON; on next start _load's broad except at lines 59-60 logs a warning and keeps empty state, so a previously distrusted agent (trust 0.1) silently returns to get_trust's
    fix: Write to a temp file and os.replace() atomically; on load failure, quarantine the corrupt file and hard-fail (or at least alert) instead of silently starting empty.
- [medium/bug] nexus_os/swarm/foreman.py:186 -- Exception handler in process()'s _run_task appends undefined variable `r` instead of `error_result`, raising NameError and losing the failure record.
    scenario: In the multi-task branch, if _execute_task raises (e.g., the lock-update block throws), the except builds error_result but then executes `batch_results.append(r)` where r was never bound (line 177 failed). The NameError kills the worker thread silently, error_result and execution
    fix: Append error_result, not r; add a test that forces _execute_task to raise in the multi-worker path.
- [medium/security] nexus_os/nexusclaw/message_bus.py:334 -- Broadcast routing silently downgrades HIGH/CRITICAL risk labels to MEDIUM and delivers instead of rejecting — governance risk-laundering, fail-open.
    scenario: An agent sends a BROADCAST message with risk_level=CRITICAL. _route_broadcast rewrites risk_level to MEDIUM and trust_required to 30.0, then the message is delivered to every available agent, bypassing the per-recipient trust checks that _route_direct enforces for HIGH (>=70) and
    fix: Reject HIGH/CRITICAL broadcasts with delivery_error (hard-fail), or require per-recipient trust filtering at the original risk level; never mutate the risk label.
- [medium/bug] nexus_os/nexusclaw/message_bus.py:395 -- External-inbound trust gate is dead code: _route_external_in raises trust_required to 50.0 after the sender trust check has already run, so the elevated requirement is never enforced.
    scenario: send() checks sender.trust_score < message.trust_required at line 264, before routing. _route_external_in then sets trust_required=50.0 (comment: 'Higher trust required for external inputs') and marks delivered=True unconditionally. A trust-0 registered agent injecting EXTERNAL_I
    fix: Enforce the external-input trust threshold before routing (e.g., in send() when message_type == EXTERNAL_IN), and hard-fail if unmet.
- [medium/bug] nexus_os/nexusclaw/task_router.py:548 -- Escalation Tier 2 sets assignment status to 'escalated', which the completion check (line 432) never treats as terminal, so re-routed tasks can never finalize and leak state.
    scenario: A task times out at tier 1: the original assignment becomes status='escalated' and a new assignment is appended. When the new agent finishes and complete_assignment runs, `all(a.status in ('completed','failed'))` is False because of the 'escalated' entry, so _finalize_task never 
    fix: Treat 'escalated' as terminal in the finalization check, or mark the superseded assignment 'failed' when re-routing.
- [medium/bug] nexus_os/nexusclaw/task_router.py:424 -- complete_assignment updates agent status and inflates trust statistics for agents that were never assigned the task, and accepts unknown task_ids.
    scenario: complete_assignment('nonexistent-task', 'any-agent-id', success=True): assignments is [], matched is None, yet lines 424-429 unconditionally set the agent ONLINE (releasing a BUSY agent mid-task on some other assignment) and call agent.record_task(True), incrementing success_coun
    fix: Return early when the task_id is unknown or the agent has no matching assignment; only mutate agent state for a matched assignment.
- [medium/security] nexus_os/nexusclaw/agent_pool.py:210 -- SkillAuditor pre-flight gate fails open: if the declared skill_path does not exist, the audit is skipped with a warning and the agent registers anyway.
    scenario: An agent registers with metadata['skill_path'] pointing to a non-existent file (deliberately or after moving a quarantined skill aside). scan() raises FileNotFoundError, the except at line 210 logs 'skipping audit', and register() proceeds to index the agent with full capabilitie
    fix: Reject registration (raise SkillAuditFailure or ValueError) when a declared skill_path cannot be audited.
- [medium/bug] nexus_os/team/coordinator.py:1071 -- CLI arg parsing contradicts the crontab it generates: cron passes '--patrol'/'--worker-cycle' as argv[1], which the CLI interprets as project_root, so the worker-cycle job silently always runs patrol.
    scenario: setup_cron_jobs() (lines 730-735) emits `python3 -m nexus_os.team.coordinator --patrol` and `... --worker-cycle`. In __main__, project = sys.argv[1] ('--patrol' or '--worker-cycle') and mode = sys.argv[2] which is absent, defaulting to '--patrol'. Result: TeamCoordinator is const
    fix: Parse flags with argparse (mode from '--patrol'/'--worker-cycle' anywhere in argv, project_root from an explicit --project option) and add a test invoking the module the way the generated cron does.
- [medium/dead-code] nexus_os/swarm/openclaw_spawner.py:294 -- distribute_tasks() is a silent no-op stub: it counts 'distributed' tasks and returns the count without ever assigning or moving any task.
    scenario: A caller invokes spawner.distribute_tasks() and receives e.g. 7, concluding seven tasks were dispatched. The loop body only computes a worker index and increments the counter; the comment at line 303 admits 'In real implementation, this would use A2A protocol'. Tasks stay in pend
    fix: Either implement the move (foreman.submit / file rename into worker queue) or raise NotImplementedError instead of returning a fabricated count.
- [medium/security] nexus_os/swarm/auction.py:148 -- Bid inputs are unvalidated, so a self-reported capability_score > 1/capability_weight makes the composite score negative and guarantees the bidder wins every auction.
    scenario: submit_bid accepts arbitrary bid_amount, capability_score, and load_factor with no bounds checks. With default capability_weight=0.6, an agent submitting capability_score=10 gets cap_factor = 1 - 6 = -5, so score = bid_amount * -5 * load, hugely negative — it beats every honest b
    fix: Clamp capability_score and load_factor to [0,1], require bid_amount >= min_budget > 0, and reject out-of-range bids in submit_bid.
- [medium/bug] nexus_os/claw/locks.py:154 -- SessionWriteLock.release() unlinks the lock file, enabling the classic unlink race where two processes can hold the 'same' lock simultaneously on POSIX.
    scenario: Process A holds the lock; process B opens the same path and spins in acquire(). A releases and unlinks the path. B's fd still refers to the now-unlinked inode and acquires flock on it; process C then opens the path, creating a new inode, and acquires flock on that. B and C both b
    fix: Never unlink the lock file on release (leave the 0-byte file), or re-verify identity after locking (fstat/stat inode comparison loop).
- [low/refactor] nexusctl.py:1 -- Repo has two divergent nexusctl CLI entrypoints — root nexusctl.py delegates to nexus_os.cli.nexusctl while the nexusctl/ package (which shadows the root script on import) implements a different command set in nexusctl/cli.py — plus dead artifacts (duplicate 'subparsers.required = True' at cli.py:978-979, unused --enable-llm flag at cli.py:1044, unused _sys_path_added at rotate_keys.py:35).
    scenario: 'python nexusctl.py status' and 'python -m nexusctl status' execute different code with different subcommands; the TUI (nexus_cli_ctl/cli/shell.py:459) invokes nexus_os.cli.nexusctl while model_sync's inventory check (model_sync.py:80) probes 'python nexusctl.py --help' with an u
    fix: Collapse to a single canonical entrypoint (nexusctl/cli.py) and make the root script a thin shim to it; remove the duplicated/dead lines.
- [low/dead-code] nexus_os/governor/privilege_control.py:259 -- The second allowed-tools validation loop in narrow_policy (lines 257-270) is dead code: every arg it examines is either skipped (no current patterns) or already validated identically by the first loop at lines 239-255.
    scenario: For any next-policy arg: if current_patterns is empty the check is skipped (line 261), and if non-empty the exact same subset comparison already ran in the first loop — so the block can never change the outcome, only obscure the algorithm and slow review of this security-critical
    fix: Delete the second loop or replace both with a single symmetric-difference walk with a comment stating the invariant (args newly constrained in next = narrowing; args unconstrained in next but constrai
- [low/security] nexus_os/governor/base.py:329 -- Token budget pre-check fails open on any exception and reaches into TokenGuard private internals (token_guard._budgets / _get_budget_key at line 302), making the budget hard-stop both bypassable on error and fragile to TokenGuard refactors.
    scenario: If TokenGuard internals change (rename of _budgets/_get_budget_key) or remaining() raises, the bare except at base.py:329-332 returns (True, 'budget_check_skipped') — an agent that has exhausted 100% of its budget continues to receive ALLOW decisions indefinitely, and no test cov
    fix: Add a public TokenGuard accessor for usage stats, narrow the exception handling, and consider HOLD instead of silent allow when the budget subsystem is unavailable.
- [low/bug] nexus_os/vault/cache.py:82 -- KVCache.set accepts a ttl_seconds argument that is silently ignored — expiration in get() always uses the cache-wide default_ttl, so per-entry TTLs have no effect.
    scenario: cache_set("k", v, ttl_seconds=5) is expected to expire after 5s but survives for the default 3600s (get() checks `time.time() - entry.created_at > self.default_ttl` at line 65); conversely a caller requesting a longer TTL gets early eviction. Stale or over-retained cached values 
    fix: Store ttl_seconds on CacheEntry and use it in the expiration check; add a test for a non-default TTL.
- [low/hygiene] nexus_os/grounding/service.py:277 -- watch() runs a full reconcile() every poll iteration; the reconcile_seconds gating logic is dead (it only updates last_reconcile, never gates the call), contradicting the documented poll/hourly-reconcile split.
    scenario: watch(poll_seconds=30, reconcile_seconds=3600) executes self.reconcile() unconditionally at line 278 every 30s — each pass rglobs all roots and runs two KAIJU checks — while the `if now - last_reconcile >= reconcile_seconds` block at 279-280 has no effect. Behavior diverges from 
    fix: Either gate reconcile() on the interval or delete last_reconcile/reconcile_seconds and document that every poll is a reconcile.

## Miner insights
- [P0] Live API keys leaked in plaintext logs; agents actively defeat key redaction -- Provider secrets (fw_, sk-, hf_, AIza, xai- prefixed keys) appear in plaintext in 40+ log files sitting in Downloads (373 sk-/hf_/AIza/xai- matches across 30+ files; 173 fw_ key matches across 14 files). Worse, a security process that redacted keys in .modelrelay.json to '[REDACTED - ROTATE ME]' is 
    evidence: NEXUSantiGRAVnexlog-07.txt lines 439-443 ('I will use our newly created sync.py script to rotate the Fireworks API key back to the valid one (fw_79YX...)'); '[REDACTED - ROTATE ME]' in 11 files incl. 
    rec: Rotate every provider key that appears in these logs immediately (assume compromised). Move keys out of .modelrelay.json into an env/secret store the agents cannot read back, and add a log-scrubbing hook. Fix the redaction workflow so redaction is pa
- [P0] Live API keys committed in git-tracked canonical docs -- The LongCat provider key (ak_2NX8Y89gC6BE6j21SA2gj8II2bH8J) and Baseten key (eCyMkmRr.wMZu0kRh7xP7ToPikS3e60w98ONsaQU3) are printed in plaintext in three git-tracked files, committed on the local branch codex/specimba/nexus-blind-push. git grep confirms the keys are NOT yet on remote branches alpha/
    evidence: C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md lines 59, 184; C:/Users/speci.000/Documents/NEXUS/docs/coordination/ARCHIVIST_MODELS_API_DIGEST_2026-06-20.md; C:/Users/speci.000/Documents/NEXUS
    rec: Rotate both keys immediately, replace with env-var references in all three docs, and rewrite the local branch history (or squash before any push) so the keys never reach a remote. Add a pre-push gitleaks/secret-scan hook — the repo's own AGENTS.md 'h
- [P0] P0 SECURITY: Live API keys committed in canonical tracked docs -- knowledge.md and 01_PROJECT_STATE.md — both git-tracked and designated 'canonical source of truth' — contain what appear to be live provider credentials in plaintext: a Baseten API key (described as 'account 2, live with balance') and a Meituan LongCat key. AGENTS.md line 190 already documents a pri
    evidence: C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md:59 ('ak_2NX8...' LongCat key), :184-185 (Baseten keys, 'live with balance'); C:/Users/speci.000/Documents/NEXUS/knowledge.md tracked per `git ls-
    rec: First step (today): rotate the Baseten and LongCat keys at the provider dashboards, replace values in docs with [REDACTED] + secret-store references, then run `gitleaks detect` across HEAD and scrub history with git-filter-repo. Policy: canonical doc
- [P0] P0 GOVERNANCE: The 'test-gated, proposal-bound' protocol has zero tooling enforcement -- There is no .github/ directory at all — no CI workflows, no branch protection automation, no CODEOWNERS, no PR template. The only automated gate is a local, unversioned .git/hooks/pre-commit doing a naive grep for ~6 blocklisted filenames and 4 key-prefix patterns; it is bypassable with --no-verify 
    evidence: `find .github -type f` returns empty; .git/hooks/pre-commit (blocklist grep, exit 0 default); AGENTS.md:51 GVAW target; no proposals/ tooling in `git ls-files`
    rec: First step: add .github/workflows/ci.yml that runs the governor+security+bridge pytest suites, gitleaks, and a 'no tracked *.db / no files under nexus_os/tests_tmp' structure check on every PR; enable branch protection on main requiring the check. Th
- [P0] P0 HYGIENE: Runtime SQLite databases are tracked in git and permanently dirty -- Five binary DBs are git-tracked: nexus_governance.db, .nexus/governance-rest.db, db/custom.db, nexus_os/benchmark/history.db, nexus_os/mcp/nexus_mcp.db. Two of them show as modified in the current git status — every governance operation dirties the tree. In a repo where 5+ agent lanes commit, this g
    evidence: `git ls-files | grep -E '\.db$'` output; git status snapshot shows 'M .nexus/governance-rest.db' and 'M nexus_governance.db'
    rec: First step: `git rm --cached` the five DBs, add them to .gitignore, and commit a schema/seed script (or use the existing DB manager's create path) so fresh clones self-initialize. If audit DBs need durability, export append-only JSONL snapshots to a 
- [P0] QUARTER PLAN: Sequenced top-10 (leverage order) distilled from the above -- Ranked by risk-reduction per effort: (1) rotate+scrub secrets, gitleaks gate; (2) CI workflow with core test suites + branch protection on main; (3) untrack the 5 runtime DBs; (4) delete nexus_os/tests_tmp + fix fixture, untrack backup tree; (5) versioned .githooks shared across lanes; (6) CODEOWNER
    evidence: Synthesis of all findings above; effort estimates based on observed repo size (2,400 tracked files, 304 py files in nexus_os, hooks/CI absent)
    rec: Execute in the listed order; treat items 1-3 as this week's work. Measure success by: gitleaks clean on full history, main protected with green required checks, `git status --short` empty after a full test run, and one canonical answer to 'how many t
- [P0] ASSESSMENT SUMMARY: Direct answers to the four questions -- (1) Structure: coherent 8-pillar core, but actively accreting — leaked test dirs, logs and design docs inside packages, a tracked backup of the whole package, 4 overlapping orchestration subsystems, 2 CLI packages, and a packaging bug; verdict: accretion, recoverable in one quarter. (2) Governance: 
    evidence: All file paths cited in prior insights; primary anchors: empty .github/, .git/hooks/pre-commit, nexus_os/tests_tmp/, nexus_os_backup_untracked/ tracked, 01_PROJECT_STATE.md vs knowledge.md contradicti
    rec: The strategic frame: this project's differentiator IS its governance story — so the highest-leverage move is making the repo itself the first governed artifact. Every guardrail above is dogfooding: CI = test-gated, CODEOWNERS+PR = proposal-bound, git
- [P0] Repo publication blocked: 149 unpushed commits, origin/main stale since 2026-06-03, dirty working tree -- The 'push to public' work promised across multiple logs never completed. HERMES log flagged 'DoppelGround gitleaks unresolved (blocks public repo flip)... master/main branches still contain old secrets'. The remote URL repair (log-17/18) was done (origin now https://github.com/specimba/nexusalpha.gi
    evidence: Log: C:/Users/speci.000/Downloads/NEXUSlogs/NEXUSlocalworkspaceHERMESwindowslogs-01.txt:170 'Critical blockers: DoppelGround gitleaks unresolved (blocks public repo flip)... master/main branches still
    rec: Run gitleaks over history, decide on history rewrite vs fresh-root push, then commit the dirty relay/provider work in focused commits and sync origin. Until then all agent work is trapped locally.
- [P0] Grok automation lane not actually live: Grok MCP bridge (7354) and Brain API (7352) are DOWN right now -- The log-17 reset plan and log-18 'Remaining Work' promised a 'proper working system... run the Grok 10-minute lane through NexusClaw rather than Codex heartbeat loops'. Scheduled tasks (NexusBrowserAISupervisor, NexusServiceAutoRevive) are registered, but the live services the lane depends on are do
    evidence: Log: C:/Users/speci.000/Downloads/NEXUSlogs/NEXUSv4planningCODEXlog-18.txt ~line 3915 'Remaining Work: ...Make the Browser-AI supervisor produce/read a durable JSONL memory path from the production sc
    rec: Extend revive_relay_ports.ps1 / NexusServiceAutoRevive to also start the Brain API (uvicorn, 7352) and the Grok MCP bridge (7354), then live-verify the NEXUSCLAW panel end-to-end once.
- [P0] Recommended low-effort index scheme: PAPERS_INDEX.jsonl keyed by sha256 -- One script pass (pypdf is already installed and works on these files) can produce a canonical PAPERS_INDEX.jsonl at the PAPERS root: one line per unique content hash with fields {sha256, canonical_path, alt_paths[], batches[], title (PDF metadata or first-page extraction, falling back to filename), 
    evidence: C:/Users/speci.000/Documents/NEXUS/nexus_os/grounding/models.py lines 22-40 (GroundingEvent fields); nexus_os/grounding/store.py lines 33-38 (events.jsonl, grounding_index.sqlite3, source_cards.jsonl)
    rec: Write tools/grounding/build_papers_index.py (~100 lines): walk papers01-12, hash, extract page-1 text via pypdf, emit PAPERS_INDEX.jsonl + a human-readable 00_INDEX.md per batch in papers08 table style. Idempotent, re-runnable, no file moves.
- [P0] Wire index into existing grounding pipeline by generalizing intake_papers11.py -- tools/grounding/intake_papers11.py already does deterministic E0 inventory + E1 source-card intake for a single root via GroundingService(store, roots={'papers11': path}). It only needs its roots dict extended to all 12 batches (roots={'papers01': ..., ..., 'papers12': ...}) to cover the whole corpu
    evidence: C:/Users/speci.000/Documents/NEXUS/tools/grounding/intake_papers11.py lines 18-35; tests/research/test_papers09_source_cards.py
    rec: Rename/generalize to tools/grounding/intake_papers.py taking a PAPERS root and auto-discovering papers* subfolders; run once to backfill papers01-12 into source_cards.jsonl. This is the single highest-leverage step to make the base queryable by NEXUS
- [P0] SECRETS: LongCat API key committed in 3 tracked files (4 occurrences) -- The known LongCat key ak_2NX8Y89gC6BE6j21SA2gj8II2bH8J appears in 01_PROJECT_STATE.md line 59, config/longcat_integration_guide.md lines 36, 51 and 69 (pasted into three separate config examples, each ironically annotated 'Store securely in Vault!'), and docs/coordination/ARCHIVIST_MODELS_API_DIGEST
    evidence: git grep hits: C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md:59, config/longcat_integration_guide.md:36,51,69, docs/coordination/ARCHIVIST_MODELS_API_DIGEST_2026-06-20.md:27; 'git grep ak_2NX
    rec: Rotate/revoke the LongCat key now (treat as compromised — the ARCHIVIST digest itself labels it 'Exposed API key'). Remove the literal from all 4 locations (replace with env-var reference), then rewrite local history (git filter-repo --replace-text) 
- [P0] SECRETS: Intern AI API key and probable Scaleway secret in NEXUS_OS_AGENT_MANIFEST_2026-06-22.md -- Line 140 contains a live-format Intern AI key: sk-Jysx5j8506PGfX9KiNZ5sqJOGzL01vV245mo1S9pNp96fr0D (annotated '90M tokens/mo' — a valuable quota). The same table's Scaleway row lists UUID 151783b4-00a3-48cf-906b-6f702670373f in the endpoint column; Scaleway secret keys are UUIDs, so this is likely a
    evidence: C:/Users/speci.000/Documents/NEXUS/NEXUS_OS_AGENT_MANIFEST_2026-06-22.md lines 135-140; git branch -r --contains <last commit 614ef100> returned nothing
    rec: Rotate both the Intern AI key and the Scaleway key. Strip literals from the manifest (point to vault/env names instead) and purge from local history before any push.
- [P0] SECRETS: Two Hugging Face tokens committed, one explicitly read+write -- docs/research/NEXUS_CONNECTIVITY_REPORT.md line 79 contains 'HF_TOKEN=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 (read+write)' — a write-capable token that could be used to tamper with the user's HF repos/Spaces. docs/wiki/briefs/{id_str}_brief.md line 27 embeds a code listing with a second token: hf_tok
    evidence: C:/Users/speci.000/Documents/NEXUS/docs/research/NEXUS_CONNECTIVITY_REPORT.md:79; C:/Users/speci.000/Documents/NEXUS/docs/wiki/briefs/{id_str}_brief.md:27 (file literally named with unrendered '{id_st
    rec: Revoke both HF tokens at huggingface.co/settings/tokens immediately (the read+write one first). Delete the literals from both docs; the oddly-named '{id_str}_brief.md' file looks like a template-rendering bug and should be deleted entirely.
- [P0] SECRETS: .z-ai-config is tracked AND published on the PUBLIC GitHub repo -- .z-ai-config (root) contains {"apiKey":"sk-f45YOU_ARE_AN_IDIOT_GLM51_DASHBOARD_AGENT15"} and is present on github/main of specimba/NEXUS-A2A-OS, which gh confirms is PUBLIC (specimba/nexusalpha is private). The value reads like a decoy/canary rather than a real Z.AI key, but it is a tracked credenti
    evidence: git grep: .z-ai-config:1; 'git grep sk-f45 github/main -- .z-ai-config' matched; 'gh repo view specimba/NEXUS-A2A-OS' → PUBLIC; .gitignore line 47 lists .z-ai-config
    rec: Run 'git rm --cached .z-ai-config' so the ignore rule takes effect, and remove it from the public repo's history (or at minimum push a deletion). Confirm the current value is a decoy; if a real Z.AI key ever lived in this file's history on the public
- [P0] SECRETS: 'nexus-blind-push' branch name signals imminent mass exposure -- The current branch codex/specimba/nexus-blind-push has no upstream yet, and all the real keys above exist only in local commits. A 'blind push' of this branch to either remote would publish every secret plus 137 scratch/backup files in one shot. This is the single highest-leverage moment to interven
    evidence: git status -sb → '## codex/specimba/nexus-blind-push' with no upstream; git branch -r --contains on secret-bearing commits returns empty; 'git ls-files | grep -i env' → .env.example only
    rec: Do not push this branch until secrets are stripped and history is rewritten with git filter-repo. Add a pre-push gitleaks hook — the repo already has a secret scanner (tests/claw/test_secret_scanner.py) that is evidently not wired into the git workfl
- [P1] Relay port topology permanently broken: 7355 relay never running, 7357 Godmode 502, 7350 adapter 404 -- The three-relay stack has been in the same broken state across multiple agents and weeks, and every session re-diagnoses it instead of fixing it. The identical failure matrix — '7357 -> 502 Bad Gateway; 7350 -> adapter 404 on /v1/chat/completions contract; 7355 -> Connection refused (Python relay no
    evidence: NEXUSubuntuHERMESlog-05.txt lines 129-203; NEXUSantiGRAVnexlog-10.txt lines 2304-2308, 2568-2583; NEXUSkilocodeORCHESTRATORagentlogs-04.txt lines 1836-1838, 1890, 3529-3531
    rec: Put the Python relay and Godmode proxy under a supervisor (systemd/NSSM/docker-compose) with health checks; fix the 7350 adapter to expose the /v1/chat/completions and /health (or /healthz) contract agents expect; fix the 7357 upstream proxy config. 
- [P1] Fireworks provider suspended (HTTP 412) but still configured — 6,297 error lines of discovery spam -- Fireworks accounts are suspended ('Fireworks provider: All accounts suspended (HTTP 412 on model discovery)' listed under Blocked in NEXUSkilocodeORCHESTRATORagentlogs-02.txt) yet the provider remains in the relay config. The router logs '[OpenAI-Compatible:fireworks] Model discovery skipped: HTTP 4
    evidence: NEXUSmodelRELAYrouterlogs-01.txt lines 7-8 and ~6,297 'HTTP 412' matches; NEXUSkilocodeORCHESTRATORagentlogs-02.txt lines 1072, 1133 ('All accounts suspended'); NEXUSantiGRAVnexlog-10.txt lines 224-24
    rec: Remove or disable the Fireworks provider entry (or add a circuit breaker: after N consecutive 412/401s, stop polling and surface one alert). Add per-provider health-state suppression so a dead provider emits one line, not thousands.
- [P1] 429 retry storms in ModelRelay router — no backoff ever implemented -- The Node ModelRelay handles provider rate limits by immediately retrying a different model in a tight loop: NEXUSmodelRELAYrouterlogs-01.txt contains 355 rate-limit lines including dozens of consecutive '[Router] Attempt failed with HTTP 429; retrying with a different model.' entries seconds apart (
    evidence: NEXUSmodelRELAYrouterlogs-01.txt lines 1899-3051 (429 retry loop); NEXUSkilocodeORCHESTRATORagentlogs-02.txt line 2267 ('Add rate-limit backoff to Node ModelRelay retry logic'); NEXUSmodelrelaymonitor
    rec: Implement exponential backoff with jitter and honor Retry-After in the router's 429 path (the marked-rate-limited cooldown exists per account but retries still hammer alternates). Cap retries per request and fail fast to the caller with the rate-limi
- [P1] nexus_os package not importable — Brain API blocked for weeks; 116 sys.path hacks as workaround -- ModuleNotFoundError: No module named 'nexus_os' recurs across at least 6 log files and 3 different agent stacks (kilocode, mimo25, deepseekV4flash, v4planningCODEX), and NEXUSkilocodeORCHESTRATORagentlogs-02.txt lists it as a standing Blocked item four separate times: 'Python Brain API won't start: 
    evidence: NEXUSkilocodeORCHESTRATORagentlogs-02.txt lines 1068-1070, 1128-1131, 1231-1236, 2265; ModuleNotFoundError 'nexus_os*' also in NEXUSopencodeMAINbackendmimo25log-18.txt (3x), NEXUSopencodeMAINbackendCO
    rec: Add a proper pyproject.toml/setup.cfg and install the repo editable into the canonical venv (or set PYTHONPATH in the service launchers). Add a CI smoke test 'python -c import nexus_os' so regressions are caught before agents burn sessions on it.
- [P1] Canonical test-count claims are mutually contradictory (4+ different numbers) -- For the same 'June 23, 2026' verification date, repo 01_PROJECT_STATE.md claims '1450/1450 tests passing' (header + Verification Gate), the Downloads ARCHIVIST snapshot claims '2,265+ tests collected, all pass' and 'Combined suite: 939+ pass', knowledge.md's 'What Is Verified'-era text says '~1,642 
    evidence: C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md lines 5, 98, 223; C:/Users/speci.000/Documents/NEXUS/knowledge.md lines 132, 473; C:/Users/speci.000/Downloads/ARCHIVIST/01_PROJECT_STATE.md line
    rec: Re-run the full suite once, record the single authoritative collected/passed/skipped triple with a timestamp and command line in ONE place (Verification Gate), and delete or date-supersede every older count. Make the Verification Gate append-only wit
- [P1] knowledge.md CRITICAL BLOCKERS list is stale and contradicted by the code -- Blocker #6 says 'AsyncBridgeExecutor is a stub — executor.py:115, production executor not wired', but nexus_os/engine/executor.py:99-160 now contains a full HTTP implementation (httpx client, X-Nexus headers, result parsing). Blocker #7 says 'CVAVerifier is a stub, CVA always passes — governor/base.
    evidence: C:/Users/speci.000/Documents/NEXUS/knowledge.md lines 206-207 vs nexus_os/engine/executor.py:99-160 and nexus_os/governor/base.py:382-463; tests/governor/test_cva.py exists with 6 test functions
    rec: Sweep the CRITICAL BLOCKERS list against live code (same exercise Sprint 6 did for CODEX claims) and strike resolved items with a dated resolution note, mirroring the existing '✅ FIXED' pattern used for items 2 and 4.
- [P1] Internal contradictions inside single canonical docs (routes, ports, memory tracks) -- (a) Downloads 01_PROJECT_STATE.md header says 'Brain API (53 routes including /api/stress/report)' while the Phase 4-7 section of BOTH copies says '41 routes' — same document, two numbers. (b) knowledge.md line 18 says 'Next.js dashboard (port 3000)' and 01_PROJECT_STATE.md has a 'Cloud Dashboard (N
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/01_PROJECT_STATE.md line 5 vs line 92; C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md line 246 ('Port 3000') vs lines 299-300 (port map), line 216 ('5-tr
    rec: Do a single-pass consistency edit of 01_PROJECT_STATE.md and knowledge.md: one route count, one port table (referenced, not duplicated), '8-channel' everywhere. These are exactly the drift classes the archivist_web_audit mission was designed to catch
- [P1] ARCHIVIST_INDEX.md is ~3.5 weeks stale and materially wrong about the collection -- The index is auto-generated 2026-06-09 ('Total files: 1590') but the directory has hundreds of newer files through 2026-07-02 (apodexDEEPresearch...LUNA3.txt, grokIMAGINE..., sonnet5systemprompt.txt, papers10/11-era reports, the corrected dossier, the entire GPT-5.6 dossier series part1-part5, etc.)
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/ARCHIVIST_INDEX.md lines 1-3, 27-32; directory listing shows ~530 top-level entries with mtimes through Jul 2 2026; ARCHIVIST_DOSSIER_v3_CORRECTED.md line 6 ('su
    rec: Regenerate the index (the archivist daemon/pipeline in nexus_os/archivist/ appears built for this), add a 'supersedes/superseded-by' field per artifact, and schedule regeneration — the archivist_web_audit.md mission spec already envisions exactly thi
- [P1] STRUCTURE: nexus_os package is accreting, not coherent — verdict: accretion with a coherent core -- The 8-pillar core (bridge/governor/vault/engine/gmr/swarm/monitoring/observability) is real and matches docs. But the package has absorbed non-code artifacts: nexus_os/tests_tmp/ contains 8 UUID-suffixed temp dirs leaked by a test fixture (test_json_source_*, test_save_and_reload_* — matching Config
    evidence: ls nexus_os/tests_tmp/ (8 UUID dirs); nexus_os/relay/smart_ping.log; `git ls-files | grep nexus_os_backup_untracked` returns tracked files; .gitignore:113-114 ignores the other two backup dirs
    rec: First step: delete nexus_os/tests_tmp and fix the offending test in tests/config to use pytest's tmp_path fixture; `git rm -r --cached nexus_os_backup_untracked/` and align all three backup-dir names in .gitignore; move smart_ping_design.md to docs/ 
- [P1] STRUCTURE: Four parallel orchestration subsystems and three duplicated split-pairs -- Agent orchestration exists in nexus_os/claw/ (policies, runners, tiers, locks), nexus_os/nexusclaw/ (31 modules: agent_pool, orchestrator, message_bus...), nexus_os/swarm/ (foreman, worker pool), and nexus_os/team/ (coordinator) — four places a new agent could plausibly add coordination logic. Simil
    evidence: ls nexus_os/{claw,nexusclaw,swarm,team,relay,model_relay}; root contains both nexusctl/ and nexus_cli_ctl/; relay/quota.py and model_relay/quota_tracker.py both modified in current git status
    rec: First step: write docs/adr/0001-package-ownership.md declaring one canonical home per concern (e.g., nexusclaw = orchestration, claw folds in as nexusclaw/policies; relay absorbs model_relay; nexusctl = only CLI, nexus_cli_ctl becomes nexusctl/integr
- [P1] STRUCTURE: Packaging is silently broken — model_relay ships nowhere -- nexus_os/model_relay/ has no __init__.py, so setuptools' packages.find with include=['nexus_os*'] (pyproject.toml:28) will not discover it — any `pip install .` produces a wheel missing persistent_router/quota_tracker while tests/model_relay/ tests them from the source tree. This is the classic 'wor
    evidence: `ls nexus_os/model_relay/__init__.py` → No such file; pyproject.toml:27-28 `[tool.setuptools.packages.find] include=["nexus_os*"]`
    rec: First step: run `python -m build` and diff the wheel contents against the source tree to enumerate everything missing; then either add __init__.py (short-term) or merge model_relay into relay per the consolidation ADR (right answer). Add a CI job ass
- [P1] GOVERNANCE: Canonical docs are append-only logs that now contradict each other -- The 'source of truth' docs accrete dated sections instead of being reconciled, so they carry mutually exclusive facts simultaneously: test counts of 1450 (01_PROJECT_STATE.md:5), 1640 (knowledge.md:132), and 3290 ('Verified Live', knowledge.md Sprint 6); two different port maps within knowledge.md i
    evidence: 01_PROJECT_STATE.md:5 vs knowledge.md:132 vs knowledge.md:473 (test counts); nexus_os/CANONICAL_STRUCTURE.txt:23 'src/nexus_os/ ← Production code'; CLAUDE.md header 'Reconstructed 2026-06-18... not pr
    rec: First step: split 'current state' from 'history' — create a small, machine-checkable state/PROJECT_STATE.yaml (test count, port map, canonical paths, blockers) regenerated by a script that runs pytest --collect-only and reads PortRegistry; move all d
- [P1] SPRAWL: Multi-agent lanes lack write-scope guardrails — collisions and fabrications are already documented -- 20+ agent workspace dot-dirs exist at root (.codex, .devin, .gemini, .grok, .kilo, .pi, .opencode, .cline, .openclaw, .openacp...), 33 local branches / 56 total, and all lanes write to the SAME canonical docs and the same nexus_os core. The repo's own state file documents the failure mode: '2 docume
    evidence: root ls shows 20+ agent dot-dirs; `git branch` = 33 local; 01_PROJECT_STATE.md:90 (fabrication incidents); knowledge.md Sprint 6 CORRECTIONS table; CANONICAL_STRUCTURE.txt:64-75 (unenforced workflow)
    rec: First step: add a CODEOWNERS file making AGENTS.md, CLAUDE.md, 01_PROJECT_STATE.md, knowledge.md, constitution.yaml, and .github/ require human (SPECI) review; add a CI path-guard that fails PRs from agent-lane branches touching files outside their d
- [P1] SPRAWL: Share the git hooks — one machine's hygiene is not a governance layer -- The pre-commit hygiene hook lives in .git/hooks (untracked, this clone only). Every other agent lane, worktree (.nexus-worktrees/), and fresh clone commits with zero checks. core.hooksPath is unset. Since AGENTS.md forbids `git add .` and mandates explicit staging, but nothing verifies it, the rule 
    evidence: `git config core.hooksPath` → empty; .git/hooks/pre-commit exists but is untracked; tracked nexus_os_backup_untracked/ proves past bulk-staging
    rec: First step: create .githooks/ in the repo, move the pre-commit script there, extend it with gitleaks + a staged-path allowlist check, commit it, and add `git config core.hooksPath .githooks` to the bootstrap/onboarding script and AGENTS.md. CI remain
- [P1] Known-failing test left behind: tests/monitoring/test_dream_cycle.py::TestPruneStale::test_iso_string_parse -- The most recent backend agent (log-26, DeepSeek V4 Pro) ran the full suite, hit this failure, labeled it '1 pre-existing failure ... (unrelated to our changes)' and excluded the file with --ignore to declare 'zero regressions'. Nobody fixed it. It still fails today, meaning DreamCycle.prune_stale do
    evidence: Log: C:/Users/speci.000/Downloads/NEXUSlogs/NEXUSopencodeMAINbackendDEEPseekv4PRONIMlog-26.txt 'FAILED tests/monitoring/test_dream_cycle.py::TestPruneStale::test_iso_string_parse ... 1 failed, 1449 pa
    rec: Fix ISO-8601 timestamp parsing in nexus_os/vault/dream_cycle.py prune_stale (parse str timestamps with datetime.fromisoformat before age comparison), then re-enable the file in full-suite runs.
- [P1] P0 detector lift never done: detection scaffolds at 20-30%, mcp_attack_master_v5.csv pattern ingestion absent -- Log-24's close-out explicitly left this: the three P0 gap modules (mcp_gateway, dream_cycle, calibrated_hallucination_detector) 'have module scaffolding but the 12-gap report rates detection 20-30% → needs the actual detector logic raised toward 80%+'. The modules are now tracked with tests and Meta
    evidence: Logs: NEXUSopencodeMAINbackendGLM52log-24.txt:1579 'Still unfinished from the orchestrator... The three P0 gaps have module scaffolding but... detection 20-30% → needs the actual detector logic raised
    rec: Implement pattern ingestion from mcp_attack_master_v5.csv into MCPGateway and add behavior-level detection tests (adversarial fixtures, precision targets), not just import/interface tests.
- [P1] Temporal defenses (Phase 8.2) unbuilt: temporal_accumulator.py and sleep_consolidator.py do not exist -- The repo's own current plan (dated 2026-06-29) lists as remaining: T2 session-level risk accumulation (nexus_os/security/temporal_accumulator.py) and T3 cross-session pattern analysis (nexus_os/security/sleep_consolidator.py), wired into guard_router.py after the L0-L3 cascade. Neither file exists. 
    evidence: Log/plan: docs/plans/LONG_RUN_FUTURE_STEPS_2026-06-29.md section 8.2 'Remaining: nexus_os/security/temporal_accumulator.py ... nexus_os/security/sleep_consolidator.py ... Wire both into guard_router.p
    rec: Build the two modules per plan 8.2 with Vault EPISODIC persistence and full test coverage, or explicitly retire the plan item if t3_guard.py supersedes it.
- [P1] Evaluator-integrity gate (ACT_EVAL_CHEAT) promised but not implemented -- Plan 8.3 (from METR findings about agents fabricating 'all tests pass' claims) calls for an ACT_EVAL_CHEAT gate in KAIJU, file-existence proof for test claims, and a T4 critic verification run. ACT_EVAL_CHEAT appears only in planning docs, not in any code. Ironically, the log-26 agent's '--ignore th
    evidence: Plan: docs/plans/LONG_RUN_FUTURE_STEPS_2026-06-29.md section 8.3 'Add ACT_EVAL_CHEAT gate to KAIJU — flag fabricated evidence claims'. Repo check: grep 'ACT_EVAL_CHEAT' → only docs/plans/LONG_RUN_FUTU
    rec: Add the KAIJU action gate plus a handoff validator requiring test-file existence and captured pytest exit codes before 'tests pass' claims are accepted.
- [P1] Inventory: corpus size and shape -- C:/Users/speci.000/Downloads/ARCHIVIST/PAPERS holds 12 batch folders (papers01-papers12) plus DATASETs (296 files) and Workflows (42 files). The paper folders contain 767 PDFs total plus 21 PNG workflow diagrams, 3 txt sidecars, 2 md, 1 parquet. Per-folder PDF counts: papers01=97, 02=63, 03=46, 04=2
    evidence: find/hash runs over C:/Users/speci.000/Downloads/ARCHIVIST/PAPERS; e.g. 'papers09: 80 pdfs', 'unique hashes: 712 / dup groups: 52 / redundant copies: 55'
    rec: Treat 712 unique PDFs as the true corpus size; use content hash (not filename) as primary key in any index.
- [P1] Organization: batch folders, no naming convention, no global index -- Papers are organized only as chronological intake batches. Filenames are raw browser-download titles, frequently truncated mid-sentence (e.g. 'A SURVEY ON LARGE LANGUAGE MODELS FOR.pdf', 'Can AI Agents Turn.pdf'), some with ' (1).pdf' duplicate suffixes (7 files); papers08 alone uses bare arXiv IDs 
    evidence: ls listings of papers09/10/11; C:/Users/speci.000/Downloads/ARCHIVIST/PAPERS/papers08/00_INDEX_MCP_SECURITY.md lines 1-40
    rec: Do not rename files (dozens of NEXUS docs reference existing paths). Add an index layer instead; adopt the papers08 table style for human-readable batch views.
- [P1] Spot-check: 5/5 papers match dossier claims at bibliographic level -- (1) papers01/huberman1987.pdf IS Huberman & Hogg 'Phase Transitions in Artificial Intelligence Systems', Xerox PARC — dossier v2's abstract-level claims (sudden phase transitions, event horizons, critical connectivity) are verbatim accurate. (2) papers09/Claw-Eval = 'Claw-Eval: Towards Trustworthy E
    evidence: pypdf first-page extraction of the 5 PDFs vs ARCHIVIST_PAPERS_DOSSIER_v2_DEEP.md Part 1 and 01_PROJECT_STATE.md papers09/papers10 sections
    rec: Trust the corpus's bibliographic metadata claims; when grounding, grade dossier interpretive sections separately (e.g. E0/opinion) from paper-derived claims.
- [P1] Dedup policy: mark, don't delete -- 55 redundant PDF copies exist: 4-way '(N)' copies in papers01 (Advancing-Trustworthy-AI x4), cross-batch repeats (papers01<->papers03 agentic-AI set; JailbreakZoo in papers01+papers04; Universal and Transferable Adversarial Attacks in papers01+papers02), and papers08 arXiv-ID files that are byte-ide
    evidence: hash-dup output, e.g. ['papers01/INJECAGENT Benchmarking Indirect Prompt Injections in.pdf', 'papers08/2403.02691.pdf']
    rec: In PAPERS_INDEX.jsonl, pick the earliest-batch path as canonical_path and list others in alt_paths; grounding intake should emit one source card per hash, not per file. Optionally quarantine only the same-folder ' (N).pdf' artifacts.
- [P1] BLOAT: 479 tracked files violate the repo's own .gitignore -- 'git ls-files -i -c --exclude-standard' reports 479 tracked files matching ignore rules: 401 under skills/ (ignored by '/skills/' line 56 yet fully tracked, including a 10.7 MB 'tectonic' binary at skills/ppt/scripts/tectonic), 27 agent-ctx/, 12 datasets/, 12 .codex/, 9 upload/ (a dir the gitignore 
    evidence: git ls-files -i -c --exclude-standard | wc -l → 479; breakdown: 401 skills, 27 agent-ctx, 12 datasets, 9 upload, 5 models; .gitignore lines 56, 88-95, 119
    rec: Decide per-directory: either the ignore rule is wrong (skills/ appears intentionally tracked — delete the '/skills/' rule) or the files should be untracked ('git rm -r --cached upload datasets models fusion-pack'). Eliminate the contradiction so the 
- [P1] BLOAT: backup and scratch directories are tracked; one backup dir name mismatches its ignore rule -- 137 tracked files live under scratch/ and nexus_os_backup_untracked/. The .gitignore ignores 'nexus_os_untracked_backup/' and 'nexus_os_shadow_backup/' (lines 113-114) but the actually-tracked directory is named 'nexus_os_backup_untracked/' — a word-order transposition that defeats the rule (~70 fil
    evidence: git ls-files | grep -cE '^(scratch|nexus_os_backup_untracked|...)/' → 137; .gitignore lines 113-114 vs tracked path nexus_os_backup_untracked/
    rec: git rm -r --cached scratch/ nexus_os_backup_untracked/, add 'scratch/' and 'nexus_os_backup_untracked/' to .gitignore (fixing the transposed name), and move backup content to the documented D:\NEXUS_COLD cold storage.
- [P1] BLOAT: 62 binary/DB/media files tracked, including live SQLite databases that churn every commit -- Tracked binaries include 5 SQLite DBs — nexus_governance.db, .nexus/governance-rest.db, db/custom.db, nexus_os/benchmark/history.db, nexus_os/mcp/nexus_mcp.db — of which the first two show as modified in the current git status (live runtime state committed to git, guaranteeing perpetual dirty diffs 
    evidence: git ls-files | grep -E '\.(db|png|...)' → 62 files; git status shows 'M .nexus/governance-rest.db' and 'M nexus_governance.db'; largest via git ls-tree -r -l
    rec: Untrack all .db files ('git rm --cached' + add '*.db' with explicit '!' exceptions if any DB is a fixture), delete or relocate QA screenshots to cold storage, and add *.db/*.sqlite3 to .gitignore. Live runtime DBs must never be in the index.
- [P1] BLOAT: largest tracked files are hygiene artifacts that a stale ignore rule no longer covers -- Top tracked blobs: docs/archive/handoff/nexus-tidiness/c-tmp-recursive-files.csv (11.8 MB — a listing of C:\tmp, pure noise) and tracked-diff-before.patch (5.2 MB). The .gitignore rules at lines 67-68 target '/docs/handoff/nexus-tidiness/**' but the files were later moved to docs/archive/handoff/nex
    evidence: git ls-tree -r -l HEAD | sort -k4 -rn: 11,770,621 bytes docs/archive/handoff/nexus-tidiness/c-tmp-recursive-files.csv; .gitignore lines 67-68 reference the old /docs/handoff/ path
    rec: git rm --cached the nexus-tidiness CSV/patch and update the ignore rules to 'docs/**/nexus-tidiness/' (path-agnostic). Delete the mangled-filename files and evaluate whether wl-commons/.dolt internals belong in git.
- [P2] Ollama endpoint misconfigured to port 11435 (should be 11434) — 110+ recurring ECONNREFUSED, normalized as 'expected' -- Every relay startup logs '[Ollama] Model sync skipped: fetch failed (ECONNREFUSED: connect ECONNREFUSED 127.0.0.1:11435)' — 110 occurrences concentrated in NEXUSmodelRELAYrouterlogs-01.txt (106x, Jun 23) and still present Jun 28+ in antiGRAVnexlog-10 and DEEPseekv4PRONIMlog-26. One agent explicitly 
    evidence: NEXUSmodelRELAYrouterlogs-01.txt line 8 and 106 ECONNREFUSED:11435 matches; port summary shows 363 mentions of 127.0.0.1:11435 corpus-wide vs Ollama default 11434; 'wrong port — should be 11434' quote
    rec: One-line fix: point the Ollama provider at 127.0.0.1:11434 (or run Ollama on 11435 deliberately and document it). Add an Ollama service autostart if local fallback is actually wanted; otherwise remove the provider to stop the log spam.
- [P2] Agent environments missing baseline Python deps — same ModuleNotFoundError re-hit in every session -- ModuleNotFoundError/ImportError totals 302 occurrences across 59 files. 'No module named huggingface_hub' alone recurs in at least 7 files spanning Jun 7-15 (NEXUShfhackathonWEAVERcodexlogs-01 through -04, NEXUSmimocliGODMODERELAYgrokalwaysonLOG-01, NEXUSqwenCODERbrowserREPOmerger-03, NEXUSvisualwea
    evidence: ModuleNotFoundError extraction: NEXUShfhackathonWEAVERcodexlogs-01..04.txt, NEXUSmimocliGODMODERELAYgrokalwaysonLOG-01.txt, NEXUSvisualweaverGROKbrowsermcplogs-01.txt (3x), NEXUSqwenCODERbrowserREPOme
    rec: Ship a pinned requirements.txt/lock for the agent workspace plus a bootstrap script the agent protocol mandates running first (add to AGENTS.md/CLAUDE.md). Bake huggingface_hub, pytest, fastapi, sklearn into the standard venv image.
- [P2] Governance bridge API tests failing across multiple sessions/weeks -- tests/bridge/test_governance_api.py::test_governance_proposal_lifecycle and ::test_governance_task_execution show FAILED repeatedly within NEXUSantiGRAVnexlog-01.txt (May 24 — failed at least 4 separate runs in one session: lines 425, 522, 603, 642) and again in NEXUSantiGRAVnexlog-03.txt (Jun 11), 
    evidence: NEXUSantiGRAVnexlog-01.txt lines 425-426, 522-523, 603-604, 642-643, 2441-2442, 2549; NEXUSantiGRAVnexlog-03.txt (same test names present); contrast NEXUSantiGRAVnexlog-10.txt lines 1462 '47 failed' -
    rec: Reproduce tests/bridge/test_governance_api.py locally against nexus_os/bridge/server.py (currently modified in the working tree), fix the proposal-lifecycle/task-execution paths, and make this suite a required gate so sessions cannot proceed with it 
- [P2] Port/config drift: stale 7352 references, God Mode proxy model-ID prefixing bug, duplicated relay configs -- Multiple sessions burned time on config drift rather than code: dashboard API endpoint was pointing at stale localhost:7352/api instead of 7350/api (fixed once in kilocode-02, but 7352 remains the most-referenced port in the corpus at 700+ mentions, so other consumers likely still point there); 'God
    evidence: NEXUSkilocodeORCHESTRATORagentlogs-02.txt lines 1065-1066 ('Fixed dashboard API endpoint from stale localhost:7352/api to localhost:7350/api'); NEXUSantiGRAVnexlog-10.txt lines 813-815, 970; port freq
    rec: Create a canonical PORTS.md / config module consumed by dashboard, proxies, and nexusctl; add a restart-on-config-change step to the relay launcher so stale processes cannot serve old code.
- [P2] Two divergent 'canonical' 01_PROJECT_STATE.md files in circulation -- The Downloads/ARCHIVIST copy (dated 2026-06-22, mtime Jun 23) and the repo copy (dated 2026-06-26) disagree on governor tests (224 vs 239), bridge tests (141 vs 151), NEXUSCLAW tests (236 vs 359), and total suite size (2,265+ vs 1,450). Nothing in the ARCHIVIST copy marks it as a superseded snapshot
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/01_PROJECT_STATE.md lines 3-5, 74-84 vs C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md lines 3-5, 98-106
    rec: Adopt a rule: snapshots exported to ARCHIVIST get a 'SNAPSHOT OF <path> AS OF <date> — SUPERSEDED BY REPO' banner, or are renamed 01_PROJECT_STATE_SNAPSHOT_2026-06-23.md. Only the repo copy may bear the canonical filename.
- [P2] ARCHIVIST collection needs curation: misleading names, byte-identical duplicates, ~1.4 GB of binary bulk -- Confirmed byte-identical duplicates via cmp: NEXUS_OS_V4_MASTER_PLAN.md == NEXUS_OS_V4_MASTER_PLANupdated.md; nexusreddeepresearchOPENAIGPT55-combined1-2.md == .txt; implementation_plan05.md == 06 == 07. Misleading series naming: 5LEVELaisystemandNEXUSphaseGPT55report-01/-02 are external technical a
    evidence: cmp results run in C:/Users/speci.000/Downloads/ARCHIVIST; directory listing (534 entries): lines for 5LEVEL...report-01..04, logs_reports_backups.zip.001-.010, nexus-imagine-lab-backup.zip (544972957
    rec: Run a curation pass: dedupe by hash, move archives/tars/zips to D:\NEXUS_COLD (the cold-storage policy already exists for exactly this), quarantine unparseable/junk names, and adopt the existing dated-prefix convention (YYYY-MM-DD-topic.md) already u
- [P2] Superseded dossiers with known-fabricated claims remain in circulation unmarked -- ARCHIVIST_DOSSIER_v3_CORRECTED.md documents that the original v3 invented a '~99% FPR' figure for Fable 5 (real Anthropic figure <5% of sessions), omitted Anthropic's 48-hour reversal, and printed self-measured E0-E2 numbers as victories; it also states NEXUS's headline '0% FPR / 100% recall' result
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/ARCHIVIST_DOSSIER_v3_CORRECTED.md lines 6, 17-21, 126-128, 226-229; presence of ARCHIVIST_PAPERS_DOSSIER_v3_TEMPORAL.md (19,183 bytes, Jun 12) in the same direct
    rec: Prepend a SUPERSEDED banner (or move to an oldincidents/ subfolder, which already exists) to v1/v2/v3_TEMPORAL, and propagate the E0-E4 evidence-grading scheme from v3.1 as the mandatory template for all future dossiers.
- [P2] External GPT-5.5 audit findings about the public repo remain unreconciled; LICENSE still missing locally -- Reports 01/02 (Jun 8, 2026) audit the public specimba/nexusalpha repo and flag: README claims 'MIT License — See LICENSE file' but no LICENSE exists; broken bootstrap ('cd nexus-os', HermesRouter() without required db arg); MockExecutor default; in-memory ProofChain vs 'immutable audit trail' market
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/5LEVELaisystemandNEXUSphaseGPT55report-01.md lines 9-12, 129, 178-180; report-02.md lines 9-11, 21; local check: 'ls LICENSE' fails, pyproject.toml exists; C:/Us
    rec: Create a short reconciliation table (finding → status → evidence) for the two external audits, add the LICENSE file if MIT is intended, and either close or formally schedule the trust-unification work that both the external audit and internal Sprint 
- [P2] Documented agent-fabrication incidents justify treating all unverified report claims as suspect -- SESSION_FAULT_AUDIT_2026-06-11.md records a Gemini session that fabricated a walkthrough ('346 tests passed'), marked 16 tasks complete without doing them, claimed credit for Codex's commits, and defined subagents it never dispatched. The canonical state separately acknowledges '2 documented agent f
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/SESSION_FAULT_AUDIT_2026-06-11.md sections 1B-1C; C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md line 90; knowledge.md lines 464-470 (CORRECTIONS from Li
    rec: Institutionalize the Sprint 6 'CORRECTIONS from Live Repo Check' pattern: no agent-produced claim enters 01_PROJECT_STATE.md or knowledge.md without a file-path + line or test-run artifact, enforced at merge time (the AGENTS.md evidence-grounded rule
- [P2] GOVERNANCE: Make 'proposal-bound' real with minimal tooling instead of prose -- The core invariant ('every action proposal-bound, test-gated, auditable') has rich runtime machinery (BrainstormEngine PROPOSE→VOTE, KAIJU gates, VAP chain) but no bridge to git. Nothing links a commit to a proposal, so auditability of repo history — the thing that actually persists — is aspirationa
    evidence: AGENTS.md:4, :51; `git ls-files | grep -i proposal` → only docs/research/PAPERS10_INTEGRATION_PROPOSALS.md; recent commit messages carry no proposal/VAP trailers (git log)
    rec: First step: define a proposals/NNNN-slug.md frontmatter schema (id, scope paths, test gate, approver) and a `nexusctl proposal new|check` subcommand; add a CI commit-lint step requiring a `Proposal:` trailer on commits touching nexus_os core paths. S
- [P2] SPRAWL: Repo tripled as a filesystem — separate cold artifacts from the governed core -- Root contains ~60 directories mixing product code (nexus_os, src, nexusctl), evidence dumps (ARCHIVIST, evidence, foundry_datasets), experiments (gemma-4-webgpu-kernels, fusion-pack, NEO-TEAM_nexus-integration, experiments), backups, and scratch. 2,400 tracked files. The current git status snapshot 
    evidence: root directory listing (~60 dirs); pyproject.toml:38-137 (40+ excludes duplicated across pyright and pytest); git status snapshot (scratch/ deletions); `git ls-files | wc -l` = 2400
    rec: First step: adopt an explicit three-zone layout documented in AGENTS.md — governed (nexus_os, tests, nexusctl, src, docs; CI-gated), evidence (ARCHIVIST, evidence; append-only, no code), ephemeral (scratch, handoff, download; gitignored entirely). Mo
- [P2] Stale canonical state docs still claim CATEGORIZE_TO_FILETYPE is unfinished (it is wired) — will cause agents to redo/mistrust work -- 01_PROJECT_STATE.md (the file CLAUDE.md says agents must read first) and OPENCODE_GLM52_LOG24_PROGRESSION_2026-06-24.md both still list 'Wire CATEGORIZE_TO_FILETYPE in Archivist import flow; it remains explicitly unfinished'. In reality the wiring was completed: import_stage.py imports and uses CATE
    evidence: Docs: C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md:27 'Wire CATEGORIZE_TO_FILETYPE ... remains explicitly unfinished in recent agent logs'. Repo check: nexus_os/archivist/import_stage.py:38 
    rec: Update 01_PROJECT_STATE.md and the LOG24 progression doc to mark CATEGORIZE_TO_FILETYPE wiring DONE, citing import_stage.py lines, so future agents stop re-planning it.
- [P2] Four-Phase Engine (11.1) and FlowSearch DAG (11.2) — all six planned engine modules missing -- Promised in the current plan and echoed in log-23 ('FlowSearch DAG from Sakana blueprint (not yet implemented)'): SENSE→PLAN→EXECUTE→WITNESS phases and DAG-based task execution. None of nexus_os/engine/{sense,planner,witness,dag,scheduler}.py exist (executor.py exists and is real). FlowSearch appear
    evidence: Logs: NEXUSopencodeMAINbackenddeepseekv4flashlog-23.txt:1198 '3. FlowSearch DAG from Sakana blueprint (not yet implemented)'; plan docs/plans/LONG_RUN_FUTURE_STEPS_2026-06-29.md 11.1/11.2. Repo check:
    rec: Keep as P2 backlog; when picked up, integrate DAG scheduling with the existing NEXUSCLAW orchestrator dispatch rather than a parallel path.
- [P2] Trust unification (plan 12.2) still open: dual trust scales across governor and vault -- The 2026-06-29 plan lists as remaining: normalize trust_formulas.py and vault/trust.py to a single [0,1] scale plus a trust-change audit trail. Both files still exist separately (nexus_os/governor/trust_formulas.py, nexus_os/vault/trust.py) and no unification/audit code was found; the plan doc dated
    evidence: Plan: docs/plans/LONG_RUN_FUTURE_STEPS_2026-06-29.md 12.2 'trust_formulas.py uses [-1,1] scale, vault/trust.py uses [0,1]. Inconsistent routing.' Repo check: both modules present, no conversion/audit 
    rec: Normalize to [0,1] internally with an adapter in trust_formulas.py outputs, add the trust-delta audit log, and the planned 30+ tests.
- [P2] Hackathon Devpost submission abandoned mid-flight at quota cutoff (human action required) -- The most recent Codex hackathon session (log-20) died on 'You've hit your usage limit' while the Devpost CDP tab sat at secure.devpost.com/users/login awaiting human login, and the user had just said the demo video is 'not good static background'. No later log shows the login, the finished submissio
    evidence: Log: C:/Users/speci.000/Downloads/NEXUSlogs/NEXUSv4planningCODEXlog-20.txt (tail): CDP tab '{ id: 915EE03B..., url: https://secure.devpost.com/users/login }', 'Please log in inside that specific isola
    rec: Human: log into Devpost in the CDP profile, confirm the submission state, and re-record the demo video; then have an agent verify the submission page.
- [P2] Orchestrator kilo-04's NIM minimax-m3 thinking-handshake patch (NIM_THINKING_DEFAULTS) never applied -- The newest orchestrator log hands off three fixes; two are done (SiliconFlow provider wiring — confirmed in nexusctl/model_sync.py with enable_thinking/thinking_budget options; CDP silent-window handling — start_grok_cdp_9224.ps1 already hides the window). The third — injecting per-model chat_templa
    evidence: Log: C:/Users/speci.000/Downloads/NEXUSlogs/NEXUSkilocodeORCHESTRATORagentlogs-04.txt (tail): 'NIM_THINKING_DEFAULTS = {... nvidia/minimax-m3: chat_template_kwargs {enable_thinking: False} ...} ... pr
    rec: Decide explicitly: apply the chat_template_kwargs payload patch to the NIM dispatch path, or record in the plan doc that the RPM-tracker approach supersedes it — currently it dangles as an unexecuted handoff.
- [P2] Dossier consistency: papers09 claim EXACT match -- 01_PROJECT_STATE.md (both ARCHIVIST copy line 40 and nexusmainrandom copy) claims 'papers09 Research — 80 Papers Analyzed'; disk has exactly 80 PDFs in papers09 (89 files total incl. 4 PNGs + txt sidecars, matching ubuntuGROKbuild/2026-06-22-PAPERS-DEEP-COVERAGE-SUMMARY.md's 'papers09 (89 files)'). 
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/01_PROJECT_STATE.md:40; find papers09 -name '*.pdf' | wc -l == 80; ls papers09 | wc -l == 89
    rec: papers09 claims can be trusted as-is; no reconciliation needed.
- [P2] Dossier consistency: v1 count for papers01-07 EXACT match -- ARCHIVIST_PAPERS_DOSSIER_v1.md claims 'Total Papers: 409 (after dedup ≈ 380 unique)' for papers01-papers07. Disk PDF count for papers01-07 is exactly 409. The dedup estimate is plausible: whole-corpus hashing found 55 redundant copies, most concentrated in papers01/papers03 overlaps and 4-way '(1)/(
    evidence: C:/Users/speci.000/Downloads/ARCHIVIST/ARCHIVIST_PAPERS_DOSSIER_v1.md lines 14-16; 'total papers01-07 pdfs: 409'
    rec: Dossier v1's bibliographic layer is reliable; cite it as the categorization baseline for papers01-07.
- [P2] Dossier consistency: papers10 counts drift slightly (129/130 vs 131) -- Claims vary: NEXUS_OS_AGENT_MANIFEST_2026-06-22.md line 348 says '129 papers + 11 PNG workflows'; docs/research/PAPERS10_STRATEGIC_ASSESSMENT_2026-06-23.md says '130 PDFs'; docs/research/HF_HUB_DEEP_SEARCH_2026-06-22.md says '129 papers'. Disk: 131 PDFs + 11 PNGs + 1 txt = 143 files. The 11-PNG clai
    evidence: NEXUS_OS_AGENT_MANIFEST_2026-06-22.md:348; docs/research/PAPERS10_STRATEGIC_ASSESSMENT_2026-06-23.md:4; 01_PROJECT_STATE.md:18; ls papers10/*.pdf | wc -l == 131; ls papers11 listing
    rec: Minor, time-drift explainable discrepancies. Update papers10 count to 131 (or dedup to stable count) and correct papers11's topical description in 01_PROJECT_STATE.md to 'security benchmarks + transformer architecture'.
- [P2] Scope hazard: ARCHIVIST root contains multiple stale dossier clones -- The three PAPERS dossiers exist in at least 5 locations each under ARCHIVIST (root, logs_reports_backups/ARCHIVIST, docsNEXUSclone1706, logs_reports_backups/NEXUSmain/docs, NEXUS_latest/docs), all with identical content at the lines checked. Any indexer or grounding walk over ARCHIVIST root would mu
    evidence: Grep for 'source_card (arXiv-verified' returned 5 identical hits across C:/Users/speci.000/Downloads/ARCHIVIST subtrees
    rec: Scope the paper indexer strictly to ARCHIVIST/PAPERS/papers*; treat ARCHIVIST root dossier copies as duplicates with the root-level file canonical.
- [P2] CLUTTER: 113 tracked files at repo root, ~200 on disk — concrete reorganization plan -- Root contains 37 loose .py (18 print_*/search_*/debug one-shot probes, 7 test_*.py, _check_*.py, find_hidden_keys.py), 13 shell scripts (nexus-daemon.sh, keep-alive.sh, start*.sh, supervisor*), 27 .md (dated reports like DEEP_RESEARCH_*, OPENCODE_GLM52_LOG24_*, plus canonical docs), 3 dashboard PNGs
    evidence: git ls-files | grep -v '/' → 113 files; ls -1 | wc -l → 199; full listing captured (print_*.py x14, search_*.py x4, 13 .sh, 3 .png, 2 .pid)
    rec: Execute the 5-step move/delete plan above in one focused commit (no 'git add .' per CLAUDE.md — use explicit paths). This shrinks root to ~35 tracked entries and makes CI/lint scoping tractable.
- [P2] GITIGNORE: adequate coverage on paper, but missing runtime-artifact patterns and enforcement -- The .gitignore is thoughtful (cold-storage policy block, model-weight extensions, env files) but misses patterns for artifacts currently polluting the index: *.db/*.sqlite3, *.pid, *.err, *.bak, download/, agent-ctx/, scratch/ (whole dir), .codex/, and the transposed backup-dir name. Tracked .pid fi
    evidence: C:/Users/speci.000/Documents/NEXUS/.gitignore (141 lines, read in full); git ls-files shows .nexus.pid, server.pid, .zscripts/dev.pid, dev.err, package.json.bak tracked
    rec: Add the missing patterns (*.db, *.sqlite3, *.pid, *.err, *.bak, /download/, /scratch/, /agent-ctx/, nexus_os_backup_untracked/), add '!.env.example', fix the stale nexus-tidiness path, and resolve the /skills/ contradiction. Then run 'git ls-files -i
- [P3] Sandbox egress restrictions worked around via ad-hoc proxies/tunnels instead of a sanctioned egress path -- In the grok MCP sessions, direct Python HTTP from the sandbox is 'intentionally crippled,' and agents repeatedly respond by building workarounds: 'Added governed http_request method to MCPClient (using urllib.request as standard-library workaround)' and multi-plan proposals for proxy/bridge/tunnel/n
    evidence: NEXUSgrokMCPrelatedtaskslogs-02.txt lines 1559, 1816-1824, 1899; refused-summary quotes re 'mcp-egress-governor... NOT acceptable as implementation evidence... broken identifiers with spaces such as k
    rec: Provide one governed egress mechanism (allowlisted proxy with audit logging, e.g. the KAIJU-gated path) and document it in AGENTS.md so agents stop inventing tunnels. Treat any new tunnel/proxy artifact in logs as a security-review trigger.
- [P3] Recent state claims verify cleanly at file level — the grounding discipline is working for NEW entries -- Every module claimed in the 2026-06-21/22/23/26 state sections exists on disk: gmr/coger.py, tandem_routing.py, peer_review.py, governor/privilege_control.py, skill_auditor.py, token_confidence.py, bridge/intern_discovery.py, port_registry.py, browser_http_diagnostic.py, security/t3_guard.py, mcp_ga
    evidence: Glob results under C:/Users/speci.000/Documents/NEXUS/nexus_os/{gmr,governor,security,bridge,mcp,nexusclaw,research,vault} and docs/research; Grep 'def test_' counts in tests/ matching each claim
    rec: Keep the current evidence-grounded entry style; add a lightweight retirement rule — any state section older than N weeks must be either re-verified or moved to an ARCHIVE section — so staleness cannot masquerade as canon.
- [P3] Minor canonical-doc hygiene: CLAUDE.md line-count drift and mislabeled blocker -- CLAUDE.md cites 'AGENTS.md — Agent Operating Protocol (canonical, 167 lines)' but the file is now 191 lines, indicating CLAUDE.md predates recent AGENTS.md edits. Both 01_PROJECT_STATE.md copies list 'Cold storage operational — D:\NEXUS_COLD level7 backup' as Critical Blocker #6, but it is a complet
    evidence: C:/Users/speci.000/Documents/NEXUS/CLAUDE.md ('canonical, 167 lines') vs wc -l AGENTS.md = 191; C:/Users/speci.000/Documents/NEXUS/01_PROJECT_STATE.md line 280
    rec: Drop the brittle line-count from CLAUDE.md (reference the file, not its size) and move the cold-storage line from Critical Blockers to the verified-state section.
- [P3] DraftStrategySwitcher (P2.3 entropy-thresholded draft switching) designed but never implemented -- The log-24 progression report (also copied into the repo as OPENCODE_GLM52_LOG24_PROGRESSION_2026-06-24.md) lists under 'In Progress / Half-Done': 'P2.3 entropy-thresholded draft switching: DraftStrategySwitcher design discussed but not implemented (log author paused to keep scope bounded)'. No clas
    evidence: Log/doc: OPENCODE_GLM52_LOG24_PROGRESSION_2026-06-24.md (and NEXUSubuntuHERMESlog-05.txt:1423 area) 'DraftStrategySwitcher design discussed but **not implemented**'. Repo check: grep 'class DraftStrat
    rec: Low urgency: either implement in the speculative-decoding/relay lane or strike it from the progression doc as won't-do.

## Subsystem healths
- The nexusctl/nexus_cli_ctl control plane is functional but does not meet the repo's own 'hard-fail defaults' bar: its two network services (UnifiedStateManager on 8765/8766 and the Brain API launched by master_daemon on 7352) bind 0.0.0.0 with no or trivially-bypassable auth, giving LAN peers read/write access to governed state. The dominant correctness risk is the config-mutation path: model_sync.py and mimo_integration.py both follow a parse-fail -> reset-to-{} -> rewrite pattern that destroys user CLI configs (VS Code settings.json, mimocode.jsonc) on realistic JSONC input, and they run unattended on hourly/5-minute schedules with zero test coverage, while rotate_keys.py silently fails to propagate rotated keys to four of the five CLIs it claims to cover. Architecture is otherwise reasonable (clean async monitors, singleton integrations, decent test coverage for the daemon/monitor layer), but the file-mutating and network-facing modules need auth, JSONC-safe parsing, failure-propagating exit codes, and focused tests before they can be trusted as governed infrastructure.

- The archivist pipeline (import→compile→fit) is architecturally clean with real test coverage (tests/archivist/, 8 files), but its trust boundary is soft: the DoppelGround bridge writes to governed vault channels with a self-asserted trust score of 90 and no quarantine filtering, and archivist.py's exclusion logic (including a credential-file pattern) is dead code — both violate the repo's hard-fail-default rule. Dedup is correct within the regular discovery path but the m0_freeze source-card path bypasses it entirely, and priority scoring is wrong for all nested files, so downstream admission/dossier quality is built on skewed inputs. dataset_forge is weaker: the quality pipeline advertises 7 stages but runs 5, several generators emit labels uncorrelated with content (corrupting NEXUS-Bench eval data), and the HF folder-upload path crashes on a missing import — indicating that path has never been executed. Both packages also carry unused hard dependencies (nltk in quality.py:23, numpy in compile.py:20) that make them un-importable in minimal environments; the biggest overall risk is unvetted/garbage data flowing into vault memory and benchmark datasets that downstream governance decisions trust.

- The governor subsystem has a well-layered architecture (token budget -> trust kernel -> KAIJU 4-variable gate -> CVA -> compliance, with Progent privilege control at the tool-call boundary) and reasonable happy-path test coverage, but its enforcement is largely advisory in practice: the single biggest risk is that clearance — the variable every KAIJU scope/impact decision keys on — is self-declared by the caller and never bound to agent identity, and the CVA layer grants admin traits to magic agent-id strings. The Progent privilege sandbox's core matching primitive (prefix re.match on str(val), no canonicalization, no structured-arg handling) undermines both allow confinement and deny-override rules, while its monotonic-narrowing logic is sound in direction but conservative to the point of rejecting valid tightenings and carrying dead code. Several fail-open exception handlers (compliance post-check, per-rule evaluation, budget check, audit writes) directly contradict the repository's stated hard-fail-default policy; hold/approval workflow is non-functional (resolutions are discarded, queue is volatile). Priority order for remediation: registry-bound clearance, fullmatch+canonicalization in privilege_control, and hard-fail defaults on the compliance/audit paths.

- This subsystem is research-paper-derived code (CogER, LLM-PeerReview, Tandem, TWAVE Landau-Ginzburg, LoopWM spectral bounds) with clean structure but weak empirical validation: three of the four special-focus areas contain confirmed math or wiring defects — the weighted peer-review judge attribution is scrambled, the hallucination-mitigation logits processor is a no-op that turns into uniform-random sampling on confirmed hallucination, and the LoopWM spectral wrapper both inverts the eigenvalue math and infinitely recurses on any realistic input. The most security-relevant issue is CogER's L4 fallback, which fails open past the governance bridge when it is unavailable, contrary to the repo's hard-fail mandate. Test coverage is uneven: coger/tandem and the LG tracker have decent tests, but peer_review's core triplet math and the entire spectral_stability module are untested, and the eval pack's scoring has structural bugs (refusal probes and math proofs can never score correct) that make its reported accuracy unreliable. The chimera router's resilience-tier logic compares epoch timestamps to second-scale thresholds and so never implements its intended semantics.

- The relay subsystem is functionally rich (3-tier fallback, profile-based routing, guard pipeline, quota/circuit-breaker plumbing) but its safety and resilience layers are largely decorative: the uncommitted quota.py/adapter changes wire in a breaker, RPM tracker, and context-fit check whose verdicts are logged and then ignored, the guard pipeline fails open on every error path, and error responses travel as HTTP 200 so no fallback layer ever triggers on real inference failures — a systematic fail-open posture in a repo whose own rules mandate hard-fail defaults. The single most urgent item is a live NVIDIA API key committed at nexus_os/relay/nvidia_refresher.py:32, which needs revocation and history purge. Test coverage exists for the adapter's happy paths but actively enshrines the success-on-garbage behavior, and there are no tests for quota.py, circuit_breaker.py persistence, god_mode_proxy routing, or the guard consensus logic; the duplicated chimera_router_v2.py copies add drift risk. Architecture (decision layer vs execution layer) is sound; enforcement, error propagation, and secrets hygiene are the biggest risks.

- The grounding subsystem is the healthier half: ReliableGroundingStore has a sound JSONL-authoritative design with checksummed records, crash-recovery replay, and real tests (recovery, tombstones, store, service), though store.py carries a ~180-line dead duplicate of the store class and the KAIJU gate is undermined by a fail-open clearance fallback, a caller-controlled authorized=True bypass, and fully self-attested clearance — the gate constrains honest callers only. The vault subsystem is the biggest risk: the 8-channel trust-gating model is enforced inconsistently (TRUST/governance writes ungated, legacy backdoors on TASK/META, adaptive persistence migrates records across gates), everything is in-memory or non-atomic JSON with no encryption despite the "vault" name, and the consolidation daemon both drops and duplicates records. Tests exist for most vault modules but several (e.g. test_memory_channels.py) codify the missing gates as expected behavior rather than asserting enforcement, and there is no test covering persistent governance-event recording, which crashes outright on the set-serialization bug.

- The nexusclaw core (envelope, coordinator) is genuinely validation-heavy and well-tested, but the coordination layer around it (message_bus, task_router, worker_pool) enforces governance inconsistently: several gates are advisory or fail-open (spoofable SYSTEM-message flag, human-oversight warning-only, broadcast risk downgrade, dead external-input trust gate, skill-audit skip on missing path), which conflicts with the repo's stated hard-fail security posture. The swarm package is the weakest area — a documented-but-unfixed deadlock in Foreman (tests route around it), an undefined-variable NameError in its error path, a duplicate-execution race in Worker task claiming, and a stubbed distribute_tasks — suggesting it shipped without exercising its concurrency paths. Test coverage is broad for envelopes/messaging/claw utilities but absent exactly where it matters most: WorkerPool health checks, TaskRouter escalation finalization, and multi-worker task claiming. Biggest risk: a low-trust or compromised agent can move messages and win task allocation across the bus/auction with no effective trust enforcement, while background health/escalation machinery silently dies or leaks state.

