# =============================================================================
# NEXUS OS — .gitignore
# =============================================================================
# Policy: Generated artifacts, large datasets, session dumps, and unused
# static assets must never be committed. Track only canoncial source code,
# documentation, and config templates.
# =============================================================================

# === GENERATED ARTIFACTS (NEVER COMMIT) ===
nexus_api.db*
arena_results.jsonl
arena_report.md
test_scan_report.json
api_scan_report.json
test_grok.py
test_api_direct.py
monitor_a2a.py
*.task.md
nexus-system-status.json
nexus-system-status.csv

# === Kiro / Kilo artifacts ===
.kiro/
.kilo/
.kiloignore
kiro.config.yaml

# === Large research dumps ===
GroundingCollectiveBackup/
research/teamlogs_reports/
*.tar.gz
*.zip

# === Temporary files ===
test_*.py
fix_*.py
*.tmp
*~

# Canonical additions created under the current Nexus doctor workstream.
!tests/monitoring/test_token_policy.py
!tests/security/test_terminal_sanitizer.py
!tests/vault/test_zilliz_client.py
!tests/test_model_relay.py
!tests/bridge/test_governance_api.py
!tests/governor/test_trust_kernel.py
!tests/vault/test_memory_trust_kernel.py

# === Python ===
__pycache__/
.pytest_cache/
*.py[cod]
*$py.class

# === Virtual Environment ===
venv/
ENV/
env/
.venv/

# === IDE ===
.vscode/
.idea/
*.swp

# === OS ===
.DS_Store
Thumbs.db

# === ByteRover ===
.brv/
.brv.backup*/

# === Agent connector artifacts (DO NOT COMMIT) ===
.claude/
.codex/
.cursor/
.kilocode/
.opencode/
.windsurf/
.augment/
.gemini/
.agent/
.agents/
CLAUDE.md
GEMINI.md
WARP.md
QWEN.md
.mcp.json

# === Secrets and environment ===
.env
.env.*
!.env.example
.env*backup*
.env*corrupt*

# === Test temp directories ===
tests_tmp/
.pytest_tmp/
.tmp/
/research_api_cache/
/research_quota_ledger.jsonl
/.nexus_pytest_tmp/
/tmp_pytest_*/
/tmp[0-9a-z]*/

# === NEXUS PI infrastructure ===
.nexus_pi/sessions/
.nexus_pi/backup/
.nexus_pi/archive/
.nexus_pi/checkpoints/
.nexus_pi/state/session_log.jsonl
.nexus_pi/state/session_checkpoints/*.json
.nexus_pi/state/session_compact.json
# Keep tracked: profiles, backup_restore.py, infrastructure doc, README
!.nexus_pi/profiles/
!.nexus_pi/backup_restore.py
!.nexus_pi/NEXUS_PI_INFRASTRUCTURE.md
!.nexus_pi/README.md

# === Session and log files ===
session-*.md
session-ses_*.md
server.log
scan_report.json
pi-session-*.html
*.n8n

# === Databases (local only) ===
nexus.db
nexus_api.db
*.db
*.db-shm
*.db-wal

# === Legacy, sandbox, and stale assets (not canonical) ===
/legacy/
sandbox/
cloud_pack/
GroundingCollectiveBackup/

# === Build artifacts ===
src/nexus_os.egg-info/

# === Large/generated files ===
benchmarks/data/
modelscope_studio_upload/
node_modules/

# === OS-level ===
gitleaks-report.json
trust_state.json
vap_chain.jsonl

# === OpenACP local workspace (contains secrets) ===
.openacp

# === HF Science database (contains API token) ===
data/HF_Science_database.txt
# Raw HF cache (not committed)
benchmarks/data/beavertails.jsonl
benchmarks/data/PKU-Alignment___beaver_tails/
benchmarks/data/hf_cache/
benchmarks/data/gsm8k/
benchmarks/data/bigcode___humanevalpack/

# =============================================================================
# DATASET & GENERATED DATA DIRECTORIES (NEVER COMMIT — Local generation only)
# =============================================================================

# Foundry datasets — 1.5+ GB of generated eval/stress data. Regenerate locally.
foundry_datasets/

# Benchmark generation scripts produce large output files.
benchmarks/__pycache__/

# Research session logs (dynamic, regenerated per work session)
research/session_logs/

# Static archive of unused assets (zips, binaries, old screenshots)
download/

# Agent context work records (historical dev logs — not canonical source)
agent-ctx/

# Handoff task files (agent-to-agent, ephemeral)
handoff/
!docs/handoff/2026-05-17-governance-endpoints.md

# Ollama Modelfile (local model config, machine-specific)
Modelfile

# Windows artifact (created by accident)
nul

# Session log at root (6K+ line work session dump)
session-ses_*.md

# Tectonic LaTeX binary (10 MB — should be installed by package manager, not in git)
skills/ppt/scripts/tectonic

# Stale build artifact directories (egg-info, compiled-only exports)
docs/usefulthings-01/
v4 plans and related doc with codes/
NEO-TEAM_nexus-integration/

# Nexus-fusion-pack (old exported presentations — not canonical source)
nexus-fusion-pack/
nexus-glm5-fusion-pack/
